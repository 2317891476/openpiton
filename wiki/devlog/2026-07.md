# Dev Log -- 2026-07

Entries are append-only, newest first.

---

## 2026-07-18 -- Build 88 direct CSR-leaf ECO fails closed on synthesized width compression

- Build 88 attempted to implement TIME at the semantically preferred
  `csr_rdata_csr_commit` commit-stage leaf input, starting from the clean Build
  86 checkpoint.  Bits 0 through 27 were found and rewired one leaf pin at a
  time, but the script then stopped on logical bit 28 because no corresponding
  post-synthesis leaf pin exists.
- This is not a missing RTL bit.  The baseline design has no TIME case, so
  Vivado compressed the CSR read-data port to 28 distinct dynamic signals and
  constant/alias-folded the remaining logical bits.  A post-route ECO cannot
  reconstruct a new arbitrary 64-bit TIME value through leaf pins that were
  optimized away.  The exact error was `required exact pin matched 0 objects:
  commit-stage CSR read-data bit 28`.
- The run exited before placement and wrote no Build 88 checkpoint, LTX, or
  PDI.  Do not weaken the exact-pin check or guess a logical-to-compressed-bit
  mapping; that would silently corrupt CSR values.
- The next available complete 64-bit ownership boundary is the issue-stage
  `wdata_i` hierarchy port.  A new ECO may mux each of its 64 port pins once,
  qualified by the matching CSR-buffer address plus commit-valid/CSR-FU, while
  leaving every internal scoreboard/regfile sink and every non-CSR result
  untouched.  This differs from rejected Build 87, which disconnected and
  reconnected all sinks across every segmented `wdata_commit_id` net.

## 2026-07-18 -- Build 87 board run rejects the global commit-writeback ECO boundary

- Build 87 programmed twice with the second run using a verified persistent
  UART capture.  Both programming runs reported `DONE bit: HIGH`, debug hub
  `0x3ffc0000000`, and four ILAs.  The second boot copied all 65,536 flat-image
  blocks, entered OpenSBI v1.8, reported one hart and
  `aclint-mtimer @ 234375Hz`, then handed off toward `0x80200000`.
- The reliable UART log is
  `~/p3_uart_logs/ttyUSB0_20260718_115034_build87_time_csr_retry.log`, 71,747
  bytes, SHA-256
  `f817712c52c9c421748c0c8456c086c4ec990ed3cf3b1b0e6cfa1c90f62216c8`.
  It stopped immediately after the OpenSBI summary at 12:01:02.  The Linux
  `rdtime` trigger at linked PC `0xffffffff807d8a9e` did not fire in a
  continuous 261-second arm, so Build 87 did not satisfy its causal gate.
- Three complete-PC snapshots at 12:05:59, 12:06:18, and 12:06:36 are
  byte-identical.  Every one of their 1,024 samples is
  `0xffffffff8063e2e4`, resolved against the exact matching Linux Image to
  `of_device_uevent+0x13c`, the `ld ra,56(sp)` return-epilogue load.  Immediate
  payload snapshots show `commit_valid/commit_ack=0/0`, no committed stores,
  no D-cache request/grant, empty WT transaction state, and no store in flight.
  This is a stable new no-commit boundary, not the prior slow TIME-emulation
  loop and not a store-drain recurrence.
- Build 86 reached changing Linux/OpenSBI PC sequences with the same PDI base,
  SD payload, and retained probes; Build 87's only functional change was the
  TIME substitution inserted on `wdata_commit_id`.  That net fans into the
  global commit/scoreboard result machinery for all instructions, so this
  board A/B rejects it as a safe ECO ownership boundary even though placement,
  route, timing, and DRC were clean.  Do not program Build 87 again.
- The next functional control must start from the clean Build 86 checkpoint
  and mux only the CSR-specific `csr_rdata_csr_commit` input consumed by the
  commit stage, plus the TIME illegal-result signal.  It must not modify the
  global `wdata_commit_id` network.  Selection comes from the CSR buffer's
  address, which is the same transaction ownership boundary as the CSR read
  data.

## 2026-07-18 -- Build 87 TIME-CSR ECO implements cleanly and is ready for board validation

- The fifth implementation attempt used the complete Advanced Flow syntax
  `place_design -eco -no_timing_driven`.  All 70 selector, write-data mux, and
  illegal-result gating cells were placed, and the LOC fail-closed check
  passed.  The generated map records selection only for CSR address `0xc01`
  with commit valid and commit FU `CSR`, returning `cycle_q >> 7`.
- Incremental routing completed successfully.  The final route report contains
  151,539/151,539 fully routed nets, zero routing errors, and zero partially
  routed nets.  Formal timing is WNS/TNS `7.078/0.000 ns` and WHS/THS
  `0.013/0.000 ns`.  DRC contains the same 169 warning-class baseline checks
  as Build 86 and no error or critical-warning class.
- Board-candidate artifacts are:
  - PDI `D:/p3b87_time_csr_eco/p3_top_build87_time_csr_eco.pdi`,
    11,976,704 bytes, SHA-256
    `10e11d8bdb6352a8c52a131356fe2b7cd627707a360f3f448ef5ac113921b3cd`;
  - LTX, 205,955 bytes, SHA-256
    `85a1f33a249fa00340183b59eb8076d7d45ac30423a95235ec8ca9955bed346d`;
  - pre-route DCP, 76,770,578 bytes, SHA-256
    `369587099feac70b4ddbe0b705405a9303054a5cff862d6e86d9f9bcfd173a4c`.
- The retained four-ILA LTX is byte-identical to Build 86, and the SD payload
  remains unchanged.  The next gate is programming with a fresh UART capture,
  then proving that Linux `rdtime` retires without entering the known OpenSBI
  illegal-instruction emulation path.

## 2026-07-18 -- Build 87 fourth ECO attempt identifies the complete Advanced Flow placement syntax

- Replacing the unsupported legacy option with `place_design -eco` allowed the
  run to reach the Advanced Flow ECO placer after all 70 cells and rewired
  nets were created.  Vivado then rejected the invocation with
  `Common 17-69`: Advanced Flow supports `-eco` only together with
  `-no_timing_driven`.
- The placer released its license before moving any cell; no checkpoint, LTX,
  or PDI was generated.  This remains an implementation-command gate rather
  than a TIME-CSR functional result.
- The required invocation for this checkpoint is therefore
  `place_design -eco -no_timing_driven`.  The existing post-command LOC check
  still requires every inserted cell to be placed before the script can write
  the Build 87 pre-route checkpoint or proceed to ECO routing.

## 2026-07-18 -- Build 87 third ECO attempt reaches placement but hits an Advanced Flow command gate

- The commit-qualified 70-cell TIME-CSR ECO opened the Build 86 checkpoint,
  created all selector, write-data, and exception-gating cells, and completed
  the hierarchical segmented-net rewiring.  This confirms that the correction
  for the first attempt's `Coretcl 2-1653` disconnect failure is effective.
- The run then stopped before placement with `Vivado_Tcl 4-2312`: this VP1902
  checkpoint uses Vivado Advanced Flow, where the legacy
  `place_design -post_place_opt` option is unsupported.  No Build 87
  checkpoint, LTX, or PDI was written, so there is still no partial artifact
  that may be programmed.
- This is a flow-command incompatibility, not evidence against the TIME-CSR
  logic.  The next attempt must use an Advanced-Flow-supported placement path,
  verify that all 70 new cells receive locations, and only then run ECO route
  and normal route/timing/DRC signoff.

## 2026-07-18 -- Build 87 selector audit adds commit-valid and CSR-FU qualification

- Before the segmented-net retry reached any netlist edit, a timing/ownership
  audit found that comparing only `csr_addr_ex_csr==0xc01` is insufficient.
  The CSR-address buffer may retain its previous value after the TIME
  instruction, while `wdata_commit_id` is also the normal GPR writeback bus.
  An address-only mux could therefore substitute TIME into a later non-CSR
  write if the stale address remained `0xc01`.
- The active Vivado load was deliberately terminated before checkpoint
  modification; its orphaned Windows process was also closed.  No artifact was
  generated.  Build 87 now requires all three conditions:
  `csr_addr==0xc01`, `commit_instr.valid==1`, and `commit_instr.fu==CSR (6)`.
  A dedicated LUT5 matches valid/FU, a separate address comparator matches
  `0xc01`, and the final select is their AND.  The ECO grows from the earlier
  68-cell draft to 70 cells; data and exception truth tables are otherwise
  unchanged.
- The focused Python truth-table regression now exhaustively checks every
  12-bit address, both valid values, and all 16 FU encodings.  TIME selection
  is asserted only for the single tuple `(0xc01, valid=1, FU=6)`.

## 2026-07-18 -- Build 87 first functional-ECO attempt stops at segmented-net rewiring

- The tracked `P3_TIME_CSR_DIV128` RTL and printf-free `rdtime_p3.c` test were
  built with system Verilator 5.046 in a clean 1x1 Ariane model.  The compiled
  diagnostic contains the real `rdtime` encoding `0xc01027f3`; simulation hit
  the good trap and printed `Simulation -> PASS`.  This validates the guarded
  `/128` CSR implementation before board implementation.
- The first Build 87 Vivado run opened the Build 86 physical DCP successfully
  and passed all preflight checks for the complete CSR address, cycle counter,
  and 64-bit commit write-data nets.  It then stopped on the first write-data
  bit with `Coretcl 2-1653`: the script attempted to disconnect every
  downstream pin by naming the top-level root segment, while hierarchical
  boundary and leaf pins are connected to their own segmented net objects.
  Vivado warned `Netlist 29-66` for those pins and refused the subsequent
  connection because they remained attached to the local segments.
- No placement, route, LTX, or PDI generation was reached, so there is no
  partial Build 87 artifact to program.  The correction queries the current
  net of each sink pin, temporarily clears that segment's `DONT_TOUCH` when
  necessary, disconnects the pin from its actual local segment, then performs
  the hierarchical reconnect to the ECO mux output.  The functional truth
  tables and 68-cell design are unchanged for the retry.

## 2026-07-18 -- Build 86 proves hardware-missing TIME CSR causes the apparent Linux stop

- Build 86 is a probe-only ECO from Build 85 and was fully signed off before
  programming: 151,469/151,469 routable nets, zero routing errors, formal
  WNS/WHS `16.514/0.013 ns`, and only the 169 baseline warning-class DRCs.
  Windows Vivado 2024.2.2 reported `DONE bit: HIGH`, debug hub
  `0x3ffc0000000`, and four ILAs.  The artifacts are:
  - PDI `D:/p3b86_store_queue_eco/p3_top_build86_store_queue_eco.pdi`,
    11,971,072 bytes, SHA-256
    `de9d6ed2066b76e840f301c424f1c05bd2c35a8b1895a674955cb521ff4f386e`;
  - LTX, 205,955 bytes, SHA-256
    `85a1f33a249fa00340183b59eb8076d7d45ac30423a95235ec8ca9955bed346d`;
  - pre-route DCP, 76,698,959 bytes, SHA-256
    `64c4588196c021be2836ebcb59d4e1a5dd50c00a1e9f14c14f413752d3a9b680`.
- The synchronized capture at
  `D:/p3b86_store_queue_eco/captures/ila_capture_build86_20260718_090510_*.csv`
  invalidates the previous persistent-FENCE interpretation.  At Linux PC
  `0xffffffff80075e40`, the store to `0x81485280` has byte enable `0x0f`, the
  committed-store queue contains one valid head, request/grant are `1/1`, the
  WT write buffer is not full, and no NI conflict is present.  On the next
  cycle the queue count drops to zero.  The store and FENCE drain normally;
  this is a transient sample, not a stable hardware stop.
- Three immediate full-PC snapshots at `09:12:21`, `09:12:38`, and `09:12:55`
  contain 333, 393, and 393 distinct PCs.  The machine is therefore executing,
  not frozen.  Their complete sequence resolves to Linux `udelay()` executing
  `rdtime` at `0xffffffff807d8a9e`, then OpenSBI `_trap_handler`,
  `sbi_trap_handler`, `sbi_illegal_insn_handler`, `sbi_emulate_csr_read`,
  `mtimer_time_rd64`, and `mret`.  One emulated read occupies roughly 800
  commit-PC samples, explaining the extremely slow Linux progress and long
  UART silence.
- The RTL root cause is now code-level and board-correlated.
  `core/csr_regfile.sv` implements `CSR_CYCLE` and `CSR_INSTRET` but has no
  `CSR_TIME`/`CSR_TIMEH` read case.  Build 80 already proved
  `mcounteren=0x3f`, including `TM=1`, so this is not a permission denial:
  the legal S-mode `rdtime` falls into the nonexistent-CSR default and raises
  illegal instruction.  OpenSBI then reads CLINT `mtime` in software and
  returns, so correctness is preserved but forward progress is catastrophically
  slow.
- The first hardware control is Build 87: expose `cycle_q >> 7` as P3 TIME,
  exactly matching the implemented 30 MHz / 128 = 234375 Hz contract, and
  suppress the illegal-CSR result for address `0xc01`.  This is a deliberately
  scoped single-hart functional ECO for causal validation.  The tracked RTL
  uses the explicit `P3_TIME_CSR_DIV128` guard and adds a printf-free
  `rdtime_p3.c` regression.  A future shared-mtime multi-hart architecture must
  address the chipset-to-tile 64-bit CDC/coherency contract rather than silently
  treating per-hart cycle counters as the general CLINT time source.
- UART log
  `~/p3_uart_logs/ttyUSB0_20260718_085314_build86_store_queue.log` remains at
  61,072 bytes, SHA-256
  `0155bf298bd87f977bead62ce876848154f719cb86e0386c82e280236f8fe521`;
  its 7200-second background capture is still active.  The SD image is
  unchanged and does not need rewriting for Build 87.

## 2026-07-18 -- Build 83 does not reproduce the orphan return ID in a 1800-second window

- Build 82's late fault-state snapshot exposed a narrower downstream state
  than the earlier two-slot Build 81 stop: the write-buffer return-ID FIFO held
  one TID 0 while transaction slot 0 was invalid and the only valid transaction
  was slot 1, pointing to entry 3.  `stores_inflight_q=0`, request and adapter
  FIFOs were empty, entry 3 was checked and transaction-blocked, and `evict=0`.
  RTL confirms that the return FIFO head indexes `tx_stat_q[rtrn_id]`; the
  orphan TID 0 therefore selects the stale invalid slot 0 rather than checked
  entry 3, so the FIFO cannot pop and slot 1 cannot be cleared.  This is a real
  return-ID/active-slot mismatch state, but it does not by itself prove whether
  the upstream cause was a duplicate ACK, late ACK, or corrupted TID.
- Build 83 is a probe-only ECO from the Build 82 pre-route checkpoint.  It
  duplicates the return-ID FIFO count, read pointer, both stored TIDs, and both
  transaction-valid bits into every ILA.  The board trigger matches the first
  state with count 1 whose selected TID has no valid slot and uses trigger
  position 896, preserving the preceding transaction history.  The initial
  case was the observed `readptr=0, TID=0, tx_valid[0]=0`; the capture script
  also supports the other three read-pointer/TID combinations.
- Vivado 2024.2.2 completed with exit code zero.  All 151,470 routable nets are
  fully routed with zero routing errors.  Formal WNS/TNS is 16.514/0.000 ns
  and WHS/THS is 0.013/0.000 ns.  DRC contains the same 169 warning-class
  checks and no error or critical-warning class.  Artifacts are:
  - PDI `D:/p3b83_orphan_return_eco/p3_top_build83_orphan_return_eco.pdi`,
    11,969,856 bytes, SHA-256
    `0901f52c19b3cf0361be3b4e94cd5a87900bda4dc5d1383acc014f6231982559`;
  - LTX `D:/p3b83_orphan_return_eco/p3_top_build83_orphan_return_eco.ltx`,
    202,004 bytes, SHA-256
    `8e71b940b6f7ad87e3f1eecb4a7c43ea1b3c15d1982b2166de615a17871cf398`;
  - pre-route DCP, 76,723,349 bytes, SHA-256
    `f45d380202868872346fd2e6fe797da4d1db19bd4492c3ed35e173e95182735a`;
  - probe map SHA-256
    `1929d1aa7b4528da7b73db1102e704296612bd432360b020203766482ec1de1b`.
- Board programming reported `DONE bit: HIGH`, debug hub
  `0x3ffc0000000`, and four ILAs.  UART0 log
  `~/p3_uart_logs/ttyUSB0_20260718_024928_build83_orphan_return.log`
  completed the full copy and OpenSBI summary, then remained at 61,072 bytes;
  SHA-256 is
  `0155bf298bd87f977bead62ce876848154f719cb86e0386c82e280236f8fe521`.
  UART1 and the continuation logs beginning at `20260718_033200` remained
  empty.
- The synchronized `rp0_tid0` trigger was armed during boot and again after
  the OpenSBI summary.  The final uninterrupted arm remained active for the
  complete 1800-second timeout without seeing the orphan condition.  Three
  trigger-now snapshot sets over the run show changing return-FIFO/TID/slot
  combinations rather than a stable stop.  The final set still contains
  normal matched return/evict activity; for example one ILA3 sample has a
  non-empty return FIFO whose head TID 0 matches valid slot 0 while `evict=1`.
  The final four snapshot hashes are:
  - `b71bdd55e7b31b95a36a477d9c1e58eab79a0cad67bdb3884d312c7c429e78b3`;
  - `05c49bd195c36484a9d6f0c7d28936f6d660d531a7d20319355088659a4b1656`;
  - `ae307c200490e3131f667f4e7e4e186fa4faf0b63e9ce3595e66d86614bed21e`;
  - `618368dc18c24b0608d7f193161e6148b812272f351c7170990a3a0e1eca931d`.
- Therefore the orphan return ID is a proven state from the Build 82 run but
  is not the active reproducible blocker in this Build 83 run.  UART silence
  alone is not a core-stop proof: the core continued issuing and retiring WT
  stores for at least an hour after OpenSBI stopped printing.  The next round
  must restore complete commit PC/FU/op and privilege/exception state and take
  repeated progress snapshots to distinguish slow Linux progress from a
  software livelock or a console-only failure.  The Build 83 flow and the
  Build 82 trigger-index decoder correction were committed as `5dbcdd9`.


## 2026-07-18 -- Build 82 board capture finds a normal early path and a late orphan return ID

- Windows full Vivado programmed the exact Build 82 PDI/LTX with exit code
  zero and reported `DONE bit: HIGH`, debug hub `0x3ffc0000000`, and four
  ILAs.  UART0 log
  `~/p3_uart_logs/ttyUSB0_20260718_012036_build82_store_return.log` reached
  the complete OpenSBI summary and stopped at 61,072 bytes, SHA-256
  `0155bf298bd87f977bead62ce876848154f719cb86e0386c82e280236f8fe521`;
  UART1 remained empty.
- The four ILAs triggered synchronously at CSV sample 128, but the first
  transaction-slot-1 rising edge occurred in bootrom at PC
  `0x000000fff1010572`, not in Linux.  The 896 post-trigger cycles contain four
  complete store-response paths: request FIFO pops, `L15_ST_ACK` adapter
  ingress/egress, write-buffer return-ID pushes, and four evicts.  The final
  state is `tx_valid=0`, `stores_inflight=0`, and an empty write buffer.  This
  is a valid normal-path control, not the failure edge.  CSV SHA-256 values for
  ILA0 through ILA3 are:
  - `4cc60f5be38bce17accf8b76d36494fde08c2024de63aa26c816f1e8e4a62c7f`;
  - `e7bcb38385be638edafd829f0a08dda6e5d25a2fcabd6ea7677d7c863c8b5597`;
  - `144fd4fd086f96b247412149cea8d8e2590c93588eb60d23f8faa330c008e929`;
  - `f0094373b44a4cb12f107e3204b05e63da9e1b735e14d796481316ee16847641`.
- The first decoder run incorrectly used the CSV sample count tuple field as
  the trigger index and therefore reported sample 1024.  The decoder now uses
  the actual trigger-index field, with a focused regression test; six Build 82
  tests pass.
- A later trigger-now Build 82 snapshot, after UART had stopped, is stable for
  1024/1024 samples in the scoreboard-head PC field at
  `0xffffffff801413c0`, resolving to Linux `__rmqueue_pcplist` and instruction
  `sd a4,24(sp)`.  Build 82 does not retain commit-valid, so this PC is not
  evidence that the store is currently committing or blocking.  Request and adapter return
  FIFOs are empty, but the write-buffer return FIFO has count 1, read/write
  pointers 0/1, and TID 0.  Transaction-valid is `0b10`: slot 0 is invalid,
  while slot 1 points to entry 3.  Entry 3 is checked and
  transaction-blocked, `stores_inflight=0`, and `evict=0`.  These facts define
  the orphan-ID boundary that Build 83 targets.  Snapshot hashes for ILA0
  through ILA3 are:
  - `d691abc268c4ecfd5a51a7fcabb74398b38f18ccf8ac5bcfd310706af75a759e`;
  - `f0ec46c4ec3d2e8a7dd6299b9671fc4c880941b160e576324ac9034b63bb6b74`;
  - `44d4002329c6c784b8c6e13494cb9d28f8651bce1b7a02a4ff82eee74a35c072`;
  - `b44d708eca64dee1718a96eb488ab56184da9dbe7dcfaee14e0beb2dfdd3b7c8`.

## 2026-07-18 -- Build 82 synchronized store-return ECO generated

- A read-only inventory of the Build 81 pre-route DCP established which
  response-path signals survive implementation.  The D-cache request FIFO,
  adapter return FIFO return-type/TID storage, missunit
  `stores_inflight_q`, write-buffer return-ID FIFO, and transaction-slot state
  are retained.  Direct `miss_rtrn_vld[2]` and the source-level L1.5 ACK pin
  are optimized away, so Build 82 uses FIFO occupancy and read/write-pointer
  transitions as equivalent interface evidence instead of guessing names.
- Build 82 is a probe-only ECO from the Build 81 pre-route checkpoint.  It
  does not alter functional RTL, the debug hub, OpenSBI/Linux, or the SD card.
  All four ILAs share the rising edge of transaction slot 1 valid.  ILA0 keeps
  the complete commit PC.  ILA1 captures transaction allocation and D-cache
  request-FIFO state/data; ILA2 captures L1.5 return type/TID at the adapter
  input, adapter FIFO storage/front state, and downstream FIFO context; ILA3
  captures missunit inflight count, write-buffer return-ID FIFO, evict, dirty,
  and entry2/3 transaction-blocked state.
- The board capture must be armed during the long bootrom SD-copy window,
  before Linux allocates the second WT transaction slot.  All four ILAs use a
  single `run_hw_ila` call, depth 1024, and trigger position 128, leaving about
  896 cycles after the second slot allocation to observe request acceptance
  and its store response.
- The Vivado 2024.2.2 build completed with exit code zero.  All 151,470
  routable nets are fully routed with zero routing errors.  Formal WNS/TNS is
  16.514/0.000 ns and WHS/THS is 0.013/0.000 ns; all user timing constraints
  are met.  DRC reports the same 169 warning-class checks as Builds 80/81 and
  no error or critical-warning class.
- Final artifacts:
  - PDI: `D:/p3b82_store_return_eco/p3_top_build82_store_return_eco.pdi`,
    11,970,976 bytes, SHA-256
    `90de3776572b8033f4a63f8eb723bafcd06ce77ba6d5d919f86ee21def1e0ba7`;
  - LTX: `D:/p3b82_store_return_eco/p3_top_build82_store_return_eco.ltx`,
    203,876 bytes, SHA-256
    `98ad8c42fbc5e96afe8dae3e62dfdad524968743d9bd565bcde106e8c3171742`;
  - pre-route restart DCP: 76,694,882 bytes, SHA-256
    `339332929ff715e606c97305fc489782ab441139cd109f0c590b51d7e8ab0315`;
  - probe map SHA-256
    `1f3d35f15116feeaee8bfb40991fde124bc1bef370781d2c23c74bad2b9c4745`.
- `scripts/p3_decode_build82_ila_csv.py` reads LTX port/bit metadata, requires
  the four common trigger waveforms and CSV trigger positions to agree, and
  reports the first proven failing edge as one of: request not accepted by
  L1.5; no `L15_ST_ACK`; adapter return FIFO not popped; missunit fails to
  push the write-buffer return ID; write-buffer return not evicted; or return
  ID/reallocation mismatch.  Build 80/81/82 focused suites pass 12/5/5 tests.
- Build 82 has not yet been programmed.  Do not take a late trigger-now
  snapshot for this diagnostic: programming must be preceded by UART capture,
  and synchronized ILA arming must complete while bootrom is still copying SD.


## 2026-07-17 -- Build 81 proves two WT store transactions never retire

- Windows full Vivado 2024.2.2 programmed the exact Build 81 artifacts below
  with exit code zero.  It reported `DONE bit: HIGH`, debug hub
  `0x3ffc0000000`, and four enumerated ILA cores.
- A persistent UART capture was active before programming.  UART0 log
  `~/p3_uart_logs/ttyUSB0_20260717_203603_build81_wbuffer.log` completed all
  65,536 bootrom blocks and stopped after the full OpenSBI platform/domain
  summary.  It stayed at 61,072 bytes through the two ILA captures; SHA-256 is
  `0155bf298bd87f977bead62ce876848154f719cb86e0386c82e280236f8fe521`.
  UART1 remained empty, SHA-256
  `e3b0c44298fc1c149afbf4c8996fb92427ae41e4649b934ca495991b7852b855`.
- Two four-ILA `trigger_now` snapshot sets were taken about seven minutes
  apart.  Every PC/dirty/txblock/control window is stable for 1024/1024
  samples, and the corresponding CSVs in the two sets are byte-for-byte
  identical.  Axis ILA0-3 SHA-256 values are respectively:
  - `162ece71b2dc36ca46386218145f62676dffb3dad4b703ebf050e340774a444e`;
  - `24c6585c15f52c016639cc82726cbae10f8de8ab95a8e31e930b10e53bf39504`;
  - `6d1a2c43053118ee88639aa3b7d56207060d80c0a576525a95109927640b1864`;
  - `d652bd86de7e20f2b4a4d0c42424f385a449166a0a463de1a4222211b7c14eaf`.
- The persistent WT D-cache state is: write buffer non-empty;
  `checked=0x0d`; entry0 `dirty=0xff`; entries2/3 `txblock=0xff`; both
  transaction slots valid (`valid=0x3`) with pointers 3 and 2 and byte enables
  `0xff/0xff`; no free transaction slot; and
  `miss_req=dirty_rd_en=check_en_q=check_en_q1=evict=load_miss_req=0`.
  The committed-store queue is empty.  Because all transaction slots are
  occupied, `free_tx_slots=0`, so the remaining dirty entry cannot assert a
  new write-buffer miss request.
- The current commit head differs from the earlier Build 80 FENCE run.  It is
  `0xffffffff801413d0`, resolved to Linux `__rmqueue_pcplist+0x94`
  (`mv a0,s5`), but commit valid/ack is 0/0; therefore this is a stale/invalid
  scoreboard-head PC and must not be described as the instruction currently
  committing or blocking.
- RTL establishes the causal boundary.  A transmitted byte remains
  `txblock=1`, and its transaction-slot valid bit remains set, until a
  `DCACHE_STORE_ACK` with the transaction ID is pushed into the write-buffer
  return FIFO and the checked entry reaches `evict`.  The two bit-identical
  snapshots therefore prove that two accepted WT store transactions are never
  retired from the write buffer.  They do not yet distinguish whether L1.5
  fails to produce `L15_ST_ACK`, the ack fails to enter/leave the L15 adapter
  return FIFO, or the D-cache store-return valid/ID path loses it.  Do not call
  this a DDR, L1.5, or ID-routing root cause until that boundary is captured.
- The Build 81 decoder now reports the in-flight/non-evicted state even when a
  nondeterministic run no longer has a valid FENCE at the commit head, while
  explicitly noting that distinction.  Twelve Build 80 and five Build 81
  focused tests pass.


## 2026-07-17 -- Build 81 WT D-cache write-buffer ECO generated

- Build 80's final-state snapshot establishes the next causal boundary.  The
  core is stably in S-mode at commit PC `0xffffffff80075e40`, resolved against
  the exact Linux ELF as `timekeeping_update+0x104`, instruction `fence w,w`.
  All 1024 samples have commit valid 1, commit ack 0,
  `no_st_pending_ex=1`, `dcache_commit_wbuffer_empty=0`, and WFI 0.  The direct
  commit gate is therefore the non-empty WT D-cache write buffer, not the
  committed-store queue.  Snapshot `mepc=0xffffffff80630080`, `mcause=2`, and
  `mtval=0xc0102573` are historical trap CSRs left after an earlier emulated
  `rdtime`; they are not evidence of a repeating trap at the current FENCE.
- Build 81 is a probe-only ECO from the Build 80 pre-route checkpoint.  It does
  not alter the Build 66 functional design, OpenSBI/Linux payload, or SD card.
  ILA0 retains the complete commit PC.  ILA1 records all 8 x 8 dirty-byte
  masks, ILA2 all 8 x 8 transaction-block masks, and ILA3 records commit
  valid/ack/FU/op, both FENCE drain inputs, entry checked bits, both transaction
  slots, write-buffer miss/allocation/tag-check/evict progress, the load-port
  miss request, and the dirty-arbiter pointer.
- Early route attempts failed with `Route 35-4579` because adding ILA loads to
  the legacy `p3_dbg_core_bus` synchronizer cone caused Versal to request an
  unsupported update to `p3_chip_debug_bus_BUFG[0]`.  The final payload removes
  that entire cone and fills the otherwise unused bits with duplicate local
  write-buffer `checked` Q bits.  The decoder rejects a capture if the duplicate
  and primary checked fields disagree.  No ILA data probe is directly connected
  to a clock-classified net.
- The clean Vivado 2024.2.2 run completed with exit code zero.  All 151,469
  routable nets are fully routed with zero routing errors.  Incremental routing
  reused 151,058 nets fully (99.73%), with zero partially reused and 405
  non-reused nets.  Formal WNS/TNS is 16.514/0.000 ns and WHS/THS is
  0.013/0.000 ns; all user timing constraints are met.  The DRC contains the
  same 169 warning-class checks as Build 80 and no error or critical-warning
  class.
- Final artifacts:
  - PDI: `D:/p3b81_wbuffer_eco/p3_top_build81_wbuffer_eco.pdi`, 11,972,784
    bytes, SHA-256
    `18de4d2e111665c0c02c0ac3e587e10b687a9f4d74ab64ecb526bf437da77e0a`;
  - LTX: `D:/p3b81_wbuffer_eco/p3_top_build81_wbuffer_eco.ltx`, 204,077
    bytes, SHA-256
    `90a6096c94e5de6a342e4b75c224f9ad0773cb1eea091cc3a15a0eed38043b83`;
  - pre-route restart DCP: 76,685,362 bytes, SHA-256
    `600b57a631f05cb3de1abd12a7f1830c7e37124d18de34df3c212597ee6724e9`;
  - probe map: 1,023 bytes, SHA-256
    `1811482d7a33788fe627482daa1fb265ae9d92845a2c26029dadfb816f50e2a9`.
- LTX inspection finds the expected four cores: ILA0 has 65 bits across ports
  0-4 and ILA1-3 each have 64 bits on port 0.  Twelve Build 80 decoder tests and
  four Build 81 decoder tests pass.  Build 81 has not yet been programmed; its
  board test must use this exact PDI/LTX pair, wait for UART progress to stop,
  take the four persistent-state snapshots, and run
  `scripts/p3_decode_build81_ila_csv.py` before assigning the blocked write
  buffer to an unaccepted miss or an in-flight transaction.


## 2026-07-17 -- Build 80 final state is a write-buffer-blocked Linux FENCE

- Resolving the synchronized S-to-M capture against the hash-matched Linux and
  OpenSBI ELFs identifies the original ECALL at Linux `sbi_get_mvendorid`.
  Its SBI arguments are `a7=0x10` (BASE extension) and `a6=4`
  (`GET_MVENDORID`).  The earlier capture therefore shows normal SBI BASE
  dispatch, not a timer CSR permission failure.
- A later trigger-now capture, taken after UART had stopped, is stable for all
  1024 samples at Linux `timekeeping_update+0x104`, `fence w,w`, with commit
  valid 1 and ack 0.  The exact Build 80 map gives `no_st_pending_ex=1` and
  `dcache_commit_wbuffer_empty=0`, which narrows the live blocker to the WT
  D-cache write buffer.  This supersedes the interim statement that the final
  boundary was unresolved inside the SBI call; the machine returned from that
  call and later reached Linux timekeeping code.


## 2026-07-17 -- Build 80 proves S-mode ECALL, retires rdtime-denial hypothesis

- Windows full Vivado 2024.2.2 programmed the hash-verified Build 80 PDI and
  reported `DONE bit: HIGH`, debug hub `0x3ffc0000000`, and four ILA cores.
  The matching LTX was loaded.  A 2400-second remote UART capture was started
  before programming and remained alive throughout boot.
- UART0 log
  `~/p3_uart_logs/ttyUSB0_20260717_180300_build80_trap_csr.log` reached the
  complete 65,536-block copy, `done!`, OpenSBI v1.8, the 234375 Hz mtimer,
  and the complete platform/domain summary.  It was 61,072 bytes when the ILA
  capture completed.  UART1 remained at zero bytes.
- All four existing ILAs were armed in one `run_hw_ila` command on the common
  `priv_lvl_q[1]` rising edge.  Each exported 1024 samples with trigger index
  512, and the four common-trigger waveforms were identical.  CSV SHA-256
  values for axis_ila_0 through axis_ila_3 are respectively:
  - `b9de1394b685890108c1adf05b99f3e5eccf38b6b41e0fd34945a28309da13dd`;
  - `e7124fa93ad284c1730d2f9529eb55020c3a27f6d55ab43f726ad5d7d3de76f0`;
  - `9f9bc10a842739160c3829c616840eca096d432221100d56a76046f908c9f78a`;
  - `60774d5b7086645a55cc3efb17e3bff39ec9a83a384ab882223f312087333f25`.
- The synchronized transition is not a TIME CSR permission failure.  The last
  S-mode exception sample has commit PC `0x80207bf4`, FU=CSR, and
  `ex_commit.valid=1`.  On the M-mode trigger sample, `mepc=0x80207bf4`,
  `mcause=9` (environment call from S-mode), and `mtval=0x00000073` (ECALL).
  The decoded CSR address is zero, `csr_illegal=0`, and `mcounteren=0x3f`, so
  `mcounteren.TM` is enabled.  This directly retires the earlier
  `S-mode rdtime + mcounteren.TM=0` hypothesis.
- The post-trigger half is not a static core stop: it contains 275
  commit-valid samples, 35 commit-ack samples, and 41 distinct commit-PC
  values, all in M-mode.  Build 80 therefore localizes the active boundary to
  OpenSBI handling of a normal Linux SBI ECALL; the exact extension/function
  and the reason it does not return to S-mode within 512 cycles still require
  PC and register/argument resolution.  Do not return to CSR 0xc01 or
  mcounteren fixes without new contradictory evidence.


## 2026-07-17 -- Build 80 incremental trap/CSR PDI and LTX generated

- The corrected pre-route resume run completed with Vivado 2024.2.2 exit code
  zero.  All 151,469 routable nets are fully routed and the route-status report
  has zero routing errors.  Incremental routing retained 151,030 fully reused
  nets (99.71%), zero partially reused nets, and 433 non-reused probe branches.
- Formal timing is WNS/TNS 16.514/0.000 ns and WHS/THS 0.013/0.000 ns; all user
  timing constraints are met.  The 169 DRC warnings have the same rule names
  and counts as Build 79, with no new error or critical-warning class.
- The final artifacts are:
  - PDI: `D:/p3b80_trap_csr_eco/p3_top_build80_trap_csr_eco.pdi`,
    11,972,000 bytes, SHA-256
    `60de526cdaa5876b8cbb71c2506c0a431317efa89f0a009b931571cb65576123`;
  - LTX: `D:/p3b80_trap_csr_eco/p3_top_build80_trap_csr_eco.ltx`,
    203,673 bytes, SHA-256
    `1741b94f7727bb2329b88cf719979c014386bca6ff1e72bdd8f5cfdea75f6001`;
  - pre-route restart DCP: 76,622,772 bytes, SHA-256
    `abbaf0239f5fdcafe0eebf6a34385f629910a9384294ac1ba5e13e5fda97fc2f`;
  - probe map SHA-256
    `5dcad544378ebff1f69b869285d86a25bf8e08b2496ed10172d81da0a136ba73`.
- The LTX contains axis_ila_0 with 65 bits across five ports and axis_ila_1,
  axis_ila_2, and axis_ila_3 with 64 bits each.  Its four common trigger
  branches resolve to the expected retained `priv_lvl_q[1]` implementation
  names.  The decoder now reads LTX `portIndex`/`leftIndex` metadata to map CSV
  columns, so historical Vivado net basenames do not alter bit semantics.
  Seven focused decoder tests pass.
- Build 80 is generated but has not yet been programmed on the board.  Board
  validation must start a UART capture long enough to cover the complete
  65,536-sector bootrom copy, program this exact PDI/LTX pair, arm all four ILAs
  in one call on the common rising-edge trigger, and decode the synchronized
  CSVs before claiming an rdtime/mcounteren root cause.


## 2026-07-17 -- Build 80 second ECO run stops on over-strict LTX marker

- The exact pre-route resume path worked and the second incremental route also
  completed with zero failed, unrouted, partially routed, or overlapping nets.
  Estimated WNS/WHS remained 16.331/0.012 ns.  This run reported 151,030 fully
  reused nets (99.71%) and 433 non-reused nets.
- `write_debug_probes` successfully created the 203,673-byte Build 80 LTX, but
  the fail-closed marker check stopped on the expected string
  `p3_build80_ila0_trigger_0`, so no PDI was written.  Inspection proves this
  is a metadata-name assumption, not a missing connection: reconnecting an
  already named implementation net to another ILA pin does not rename that
  net.  The common `priv_lvl_q[1]` trigger therefore retains its earlier
  `p3_build79_irq_2_1` name, and ILA0's PC bits retain their Build 76 net names.
  New, previously unprobed nets do have Build 80 names.
- Recovery will validate the actual retained/new LTX endpoints instead of
  requiring every net to acquire a Build 80 basename.  The capture script will
  select the shared trigger by its actual retained name within each ILA.  The
  decoder will use the LTX core/port/bit metadata to map CSV columns, rather
  than inferring bit positions from implementation net-name prefixes.  This
  keeps the decoder correct even when Vivado preserves historical net names.


## 2026-07-17 -- Build 80 first ECO run exits while serializing routed DCP

- The first `scripts/p3_build80_trap_csr_eco.tcl` run passed all fail-closed
  net, debug-port, and clock checks.  All four existing ILAs use
  `u_bd/openpiton_top_i/chipset_clk_o`; every planned probe was reconnected,
  and the 76,622,772-byte Build 80 pre-route checkpoint was written to
  `D:/p3b80_trap_csr_eco/p3_top_build80_trap_csr_eco_pre_route.dcp`.
- `route_design -eco` completed successfully.  Final route verification had
  zero failed, unrouted, partially routed, or overlapping nets.  Estimated
  WNS/TNS was 16.331/0.000 ns and WHS/THS was 0.012/0.000 ns.  The final reuse
  report contained 151,038 fully reused nets (99.72%), zero partially reused,
  and 425 non-reused nets.
- Immediately after the successful route, Vivado terminated without a Tcl
  error at `write_checkpoint -force ..._routed.dcp`; the Windows wrapper
  returned exit code 116.  No routed DCP, LTX, or PDI was created.  The Vivado
  log ends on that command, there is no `hs_err` file, D: has 2.9 TiB free,
  and host/WSL memory was not exhausted.  This is a post-route checkpoint
  serialization/process gate, not an implementation failure.
- Recovery will reuse the saved Build 80 pre-route DCP, rerun the same ECO
  route, and omit the nonessential post-route DCP serialization.  The flow
  will retain the pre-route DCP as its reproducible restart point and proceed
  directly to route/timing reports, LTX validation, and PDI generation after
  routing succeeds.


## 2026-07-17 -- Build 80 incremental trap/CSR diagnostic prepared

- Build 80 is a probe-only ECO from the board-validated Build 79 pre-route
  checkpoint,
  `D:/p3b79_wfi_irq_eco/p3_top_build79_wfi_irq_eco_pre_route.dcp`.  It does
  not resynthesize or change the Build 66 functional logic, create another
  debug hub, or change the current one-hart OpenSBI/Linux SD image.
- A Vivado 2024.2.2 checkpoint inventory completed successfully before the ECO
  was defined.  The implemented netlist retains all 64 register cells for
  `mepc_q`, `mcause_q`, `mtval_q`, and `mcounteren_q`; all 64 commit-PC bits;
  all four commit-FU bits; all eight commit-op bits; commit valid/ack; and the
  exact `ex_commit.valid` net.  The 12 CSR-address bits are also present, but
  synthesis split bits 0-3 and 9 at the CVA6 top level and bits 4-8, 10, and
  11 under `ex_stage_i/csr_buffer_i`.
- The same inventory rejected several source-level names as implementation
  probes: the raw commit `ex.tval`, `ex_commit.cause/tval`, CSR-op bus,
  `csr_exception.valid`, `privilege_violation`, and `trap_to_priv_lvl` names
  were optimized or renamed.  Build 80 therefore uses retained CSR registers,
  the split CSR-address bus, and
  `csr_exception_csr_commit.cause[1]` rather than guessing those names.
- `scripts/p3_build80_trap_csr_eco.tcl` explicitly reconnects every bit in all
  four existing ILAs.  ILA0 captures a common `priv_lvl_q[1]` rising-edge
  trigger plus the complete 64-bit commit PC.  ILA1 captures the same trigger
  plus `mepc[63:1]` (`mepc[0]` is architecturally zero).  ILA2 captures
  `ex_commit.valid`, the CSR-illegal condition, commit valid/ack/FU/op, the
  12-bit CSR address, privilege, mcause, `mcounteren`, WFI/interrupt-enable
  context, and the same trigger.  ILA3 captures `mtval[62:0]` plus the trigger;
  an illegal instruction is only 32 bits, so its complete encoding is retained.
- `scripts/p3_ila_capture_build80_trap_csr_eco.tcl` configures all four ILAs
  for `eq1'bR`, trigger position 512, and arms them in one `run_hw_ila` call.
  `scripts/p3_decode_build80_ila_csv.py` requires the four trigger vectors and
  CSV trigger indices to agree, decodes the synchronized window, decodes a CSR
  instruction from mtval, and cross-checks its CSR number against the
  implementation-level CSR-address bus.  Six focused decoder tests pass.
- The diagnostic target is precise: determine whether an S-mode CSR read,
  especially `time` (`0xc01`), enters an M-mode illegal-instruction trap while
  `mcounteren.TM=0`, and whether the same mepc/mtval pair repeats.  No such
  root cause is claimed until the Build 80 PDI is routed, programmed, and a
  synchronized capture satisfies those checks.


## 2026-07-17 -- Build 79 evidence correction: active OpenSBI trap/CSR-emulation loop, not commit stall

The 2026-07-16 Build 79 entry below is retained as the original journal entry,
but its `commit_valid=0 (1024/1024)` decode and resulting commit-stage-stall
conclusion are superseded.  Direct CSV-column recount of the original capture
at
`D:/p3b79_wfi_irq_eco/captures/ila_snapshot_build79_20260716_211847_*`
found `commit_valid=1` in 467/1024 samples, `commit_ack=1` in 429/1024,
and only 38 valid-without-ack samples.  The longest run without commit-valid
was 75 cycles.  The core was actively retiring instructions, so that capture
did not observe a terminal commit stall.

The original UART evidence was also weaker than stated.  The two Build 79
captures ended at 68% and 74% of Build 66's fixed 65,536-sector bootrom copy;
neither contains `done!`, an OpenSBI banner, or a Linux banner.  The ILA
snapshot was taken about 18 minutes after the UART capture ended.  Its S-mode
activity proves execution had later passed the bootrom, but UART did not
identify or synchronize the final software boundary.

A new non-resetting, non-programming live snapshot on 2026-07-17 connected to
the still-programmed Build 79 image through the Windows Vivado 2024.2.2 client.
The VP1902, debug hub `0x3ffc0000000`, and all four expected ILAs enumerated;
Vivado exited zero.  The new files are
`D:/p3b79_wfi_irq_eco/captures/ila_snapshot_build79_20260717_153118_*`.
SHA-256 values for axis_ila_0 through axis_ila_3 are respectively:

- `564923867137a0c2322ca8e68921c6b73f8c3fec1a44421491179a6027a99c2e`
- `d71788921e2c2870afeac3ac3b4c5eeb723c93bba11dfd0ad126ca52db833707`
- `8f39fcd4055439f40b329a709fb92a3ff550169a22b5dc3e4b7799b0748fd1c2`
- `77a3dad5ed394a6a63d05d3c4c6864386d7116b8d88451e0d2a8318d210c281a`

The live ILA2 recount shows `commit_valid=1` in 517/1024 samples and
`commit_ack=1` in 444/1024.  `wfi_q=0` for all 1024 samples.  Privilege level
is M-mode for 907 samples and S-mode for one contiguous 117-sample interval,
with M-to-S and S-to-M transitions.  The original capture has exactly the same
907/117 privilege pattern at a different circular phase.  MIE is 0, SIE is 1,
`mip.{SSIP,STIP,SEIP}` are all 0, and the wrapper-level timer, IPI, external
IRQ, and debug inputs are all low (`p3_ariane_debug_bus[15:8]=0xf0`).

ILA1 is also active: 233 distinct bus values across 88 request addresses,
135 live core-to-L1.5 request samples, 119 request-ack samples, 118 return
samples, and no L1.5 error.  The recurring addresses include:

- `0x80000520`, which resolves in the exact one-hart OpenSBI ELF to
  `_trap_handler_hyp`;
- `0x80021940`, within `sbi_emulate_csr_read()`;
- `0xfff102bff8`, exactly the OpenPiton CLINT `mtime` address
  (`0xfff1020000 + 0x4000 + 0x7ff8`).

The DDR sticky record retains OKAY BRESP/RRESP.  A subsequent ten-second live
UART read returned zero bytes.  The evidence therefore supports an active,
repeating S/M trap and CSR-emulation path, likely a synchronous CSR-read loop,
not WFI, a pending-interrupt wake failure, a persistent L1.5 request stall, or
a commit-stage deadlock.  It does not yet identify the S-mode instruction or
prove that the emulated CSR is TIME; Build 79 has no PC, mcause, mepc, mtval,
or decoded CSR-number probe.

Build 79's generated probe map has a second error.  The script reconnects only
probe0[28:44].  Bits [45:63] retain Build 78 diagnostic indices 17 through 35,
not `no_st_pending_ex` and `dcache_commit_wbuffer_empty` as the map claims.
Build 79 consequently cannot prove that either of those two signals is true.

The next causal diagnostic must preserve the current Build 66 functional
netlist and trigger on the repeating S-to-M transition while capturing commit
PC plus mcause/mepc/mtval and the illegal-instruction/CSR-emulation selector.
Do not start a commit-kill/flush ECO from the superseded commit-stall premise.
UART capture must run long enough to pass the complete 65,536-sector copy.


## 2026-07-16 -- Build 79 WFI/IRQ ECO: core is NOT in WFI, active but commit-blocked

Build 79 extends the Build 78 post-route ECO series to probe the CVA6
WFI and interrupt-wake state.  The ECO reconnects 17 bits on
axis_ila_2/probe0[28:44] to follow the Q outputs of the exact CSR
register cells: wfi_q, priv_lvl_q[1:0], mstatus_q[mie],
mstatus_q[sie], mie_q[1,3,5,7,9,11], mip_q[1,5,9], and
mideleg_q[1,5,9].  It keeps the Build 77 commit/L1.5 status
(probe0[0:27]) and Build 78 store-path detail (probe0[45:63]).

The ECO was compiled from the Build 78 pre-route DCP.  Routing reused
151,352/151,557 nets (99.86%), with 205 non-reused.  WNS=16.331 ns,
WHS=0.012 ns, zero failed nets.  PDI SHA-256
899ad1d614499ad2e75e4210cd88572576bb74fcff97511e9d33ccb7da746325,
LTX SHA-256
a3301fc866e55e97f1bfc00c12d7edc669b15469b6a9162cfe975d33bade8424.

Board programming reached DONE bit: HIGH, debug hub 0x3ffc0000000,
and four ILAs.  UART showed bootrom SD copy to 74% before the capture
was taken; the boot had reached the Linux/OpenSBI stop region.

ILA2 decode (1024 samples, immediate trigger):

- wfi_q = 0 (1024/1024 stable): the core is NOT in WFI
- commit_valid = 0 (1024/1024 stable): no instruction committing
- mstatus.MIE = 0 (stable): M-mode interrupts globally disabled
- mstatus.SIE = 1 (stable): S-mode interrupts enabled
- priv_lvl = M-mode(3) 88.6%, S-mode(1) 11.4%; exactly 2 transitions
  (M->S at sample 607, S->M at sample 724)
- mie[1,3,5,7,9,11] = SSIE=1, MSIE=1, STIE=1, MTIE=1, SEIE=1, MEIE=0
- mip[1,5,9] = SSIP=0, STIP=0, SEIP=0: no pending interrupts in CSR
- mideleg[1,5,9] = all 1 (interrupts delegated to S-mode)

ILA1 decode (core/L1.5 history bus, 1024 samples): the L1.5
last-request address bus is actively toggling with 108 unique
64-bit patterns across 38 distinct DDR addresses.  Top addresses:
0x80274500 (11.1%), 0xfefd7050 (11.0%), 0x80044058 (5.8%),
0x80043d80 (8.8%), 0x80043f08 (5.3%).  The L1.5 handshake low byte is
mostly 0x07 (idle, no active request/ack).

Interpretation: the core is NOT in WFI and NOT waiting for an
interrupt.  No instruction commits despite active L1.5 memory traffic
(108 unique request patterns).  priv_lvl toggles M<->S exactly twice,
consistent with an OpenSBI mret return to Linux (M->S) then a trap
back to OpenSBI (S->M).  mstatus.MIE=0 with all mip bits = 0 means no
interrupt is pending in the CSR at capture time.  The Build 78
time_irq_i=1 (wrapper-level timer input) does not match mip.STIP=0,
suggesting OpenSBI already reprogrammed mtimecmp forward before
returning to Linux.  The combination points to a commit-stage stall
in the trap/mret path: the core does OpenSBI work, mrets to S-mode,
then traps back, but the mret or post-mret instructions cannot commit.
This is not a store-path deadlock, not a WFI stall, and not a
pending-interrupt wake failure.

Next diagnostic boundary: the commit-stage kill/stall nets in CVA6
commit_stage.sv and controller.sv.  The next ECO should probe
commit_stage_i/commit_kill, scoreboard stalls, controller fence and
flush state to identify which condition blocks commit while the
front-end is active.


## 2026-07-16 -- Builds 75-78 localize the one-hart OpenSBI/Linux stop beyond the store path

- Build 75 completed synthesis, implementation, timing, and PDI/LTX generation
  from the clean self-contained tree.  The PDI SHA-256 is
  `a890167336e76d056823b2107e9de6d7e7e6a2235f0831c5bda4c1ba8d8475d3`
  and the LTX SHA-256 is
  `fa8c19fe3ac1742aba87e2ac1f762977457e2e9c53e3660c1b538ceb9a7c4cff`.
  Board programming reached `DONE bit: HIGH`, the debug hub, and five ILAs,
  but two UART captures stayed empty and the 256-cycle store trigger never
  fired.  An immediate ILA capture showed the first bootrom instruction-fill
  request instead of the later Linux stop.  Build 75 therefore proves the
  clean build/debug flow, but its changed implementation does not reproduce
  the validated Build 66 boot behavior and is not root-cause evidence.
- Versal/Rubik rejected reference-checkpoint incremental implementation, so
  Builds 76-78 use post-route ECOs on the board-validated Build 66 routed DCP.
  They create no functional logic or new debug hub; they only move loads of
  the four existing BD-owned ILAs and reroute those branches.  Vivado 2024.2.2
  can exit with code 116 while saving a final routed Versal DCP even after
  successful route/PDI/report generation, so the production ECO scripts save
  a pre-route DCP and deliberately omit the unnecessary final routed-DCP
  write.
- Build 76 first restored the exact CVA6 commit PC and packed commit/L1.5
  state.  Its initial bit 0 accidentally observed the nested exception-valid
  field rather than the outer commit-entry valid field; that capture at
  `init_dl_task_timer: sd s0,0(sp)` was therefore explicitly inconclusive.
  Build 77 changed only that ILA bit to the exact outer valid net.  Its PDI
  SHA-256 is
  `41490a1f912c9c787e164e93b77620f1ddc82804a67d77c38eab08d98029beed`
  and its LTX SHA-256 is
  `b9d6ba925d32609c59fc7de5b34fda526a55442a834269aa4a9e757f79fb7496`.
  Programming reached `DONE bit: HIGH`, hub `0x3ffc0000000`, and four ILAs;
  UART stopped at 61,072 bytes after the OpenSBI summary.
- Build 77 captured a 1024/1024-cycle stable commit head at
  `0xffffffff80075e40`, Linux `timekeeping_update+0x104`, instruction
  `fence w,w`.  The simultaneously stable state was commit valid 1, ack 0,
  FU `CSR`, op `FENCE`, and aggregate L1.5 `stall_s1=1`.  CVA6
  `commit_stage.sv` makes a FENCE ack equal to `no_st_pending_i`, so this
  proves that the FENCE was waiting for an earlier store to drain in that
  run.  It does not prove which individual L1.5 S1 condition was responsible;
  tag/MSHR, same-index/S2-S3, allocation, and NoC1-credit subconditions are
  optimized away in the retained Build 66 netlist.
- Build 78 split that pending-store condition into the CVA6 committed-store
  queue, WT D-cache write buffer, transaction slots, and the original sticky
  core-to-L1.5 history bus.  Its ECO routed all 151,563 routable nets with
  zero routing errors, WNS 16.514 ns, WHS 0.013 ns, and all constraints met.
  The PDI SHA-256 is
  `5e8405d7af506cafbadbff74518bff579653c18e5669bb0b067e305c518988d7`;
  the LTX SHA-256 is
  `f63346d08f861176eea77911c5bbc9bd7687934f290418a5c205dba2b4802792`.
  Board programming again reached `DONE bit: HIGH`, the same hub, and four
  ILAs, and UART again stopped at exactly 61,072 bytes.
- Two Build 78 snapshots separated by several minutes were bit-for-bit
  identical, but they refuted a persistent store/FENCE stop in that run:
  commit valid was 0, the non-speculative committed-store queue and WT
  D-cache write buffer were empty, all eight write-buffer valid/dirty bits
  and both transaction-valid bits were 0, aggregate L1.5
  stalls were 0, and no live core-to-L1.5 request was asserted.  The displayed
  address `0x81898020` is only the sticky most-recent request and must not be
  described as an unacknowledged current request.  Heartbeat continued,
  L1.5/SD/DDR error bits stayed clear, and the wrapper debug value `0xf4`
  showed synchronized `time_irq_i=1` with IPI/external/debug requests low.
  The current evidence therefore says the final stop is non-deterministic
  across probe-only ECO runs and is not proven to be a permanent store-path
  deadlock.  The next minimal boundary is the WFI/interrupt-wake state:
  `wfi_q`, `halt_ctrl`, timer input, and the relevant `mip`/`mie` enable bits.

---

## 2026-07-15 -- Build 75 synthesis: JVM crash recovery and restart

The first Vivado synthesis attempt (session 76088, ~21:12) crashed with an
EXCEPTION_ACCESS_VIOLATION (JVM segfault) while waiting for `synth_1`.
The four ILA OOC runs (`axis_ila_0..3`) had completed with valid `.dcp`
checkpoints, but the four BD-dependency IP runs (`clk_wizard`, `axi_noc`,
`proc_sys_reset`, `uart_16550`) were killed mid-synthesis by the crash,
leaving them orphaned in a permanent `Running synth_design...` state.
`synth_1` never started.

Two issues compounded the recovery:

1. Orphaned run state: Vivado still believed the four IP runs were active.
   `reset_run synth_1; launch_runs synth_1` would hang indefinitely because
   it saw the dependencies as still running.  Fixed by opening the project
   in a separate batch session and explicitly `reset_run`-ing each orphaned
   IP run (Vivado printed `Couldn't access process for termination` —
   expected since the PIDs no longer exist).

2. PTY hang: subsequent attempts to restart the build through the WSL
   `vivado` wrapper (which allocates a PTY) hung at JVM startup with
   `java.exe` stuck at ~123 MB.  Root cause: `pushd \\wsl.localhost\...`
   fails with `system cannot find the path` when the UNC path is already
   mapped as the Z: drive, and the PTY interaction with Vivado batch mode
   adds a second hang vector.  Bypassed by invoking `vivado.bat` directly
   via `cmd.exe /c` with pipe I/O (`2>&1`) instead of the wrapper/PTY path.

The restart at 22:04 used `-skip_create -skip_prepare -jobs 8` on the
existing project, correctly re-launched the four dependency IP runs and
`synth_1`, and is in progress.  The completed ILA OOC runs were preserved
and skipped.  Once synthesis finishes, the Build 75 post-synthesis hook
(`p3_build75_insert_debug.tcl`) will wire the exact CVA6 commit-head
PC/FU/op and L1.5 request/stall probes, then implementation will produce
the PDI and LTX.

## 2026-07-15 -- Closed Build 66 companion-bootrom source gap

- The second Build 75 attempt started from commit `196ddab` in another clean
  work tree with zero pre-existing `.tmp.v`/`.tmp.h` files.  It proved the
  Windows/WSL PyHP repair: all required RTL and include outputs, including
  `bram_sdp_wrapper.tmp.v`, were generated, and the legacy helper consumed
  them as `Using pre-existing` without another direct-exec failure.
- Project creation then reported that
  `bootrom/baremetal/bootrom.sv` was missing.  Static RTL inspection confirms
  `riscv_peripherals.sv` unconditionally instantiates both `bootrom` and
  `bootrom_linux` before selecting their data, so the missing module would
  make fresh synthesis invalid even though the Linux ROM exists.  The run was
  stopped before synthesis rather than waiting for that deterministic failure.
- The Build 66 bootrom rebuild now generates the companion baremetal module
  from an inline minimal DTS, assembly bootrom, and repo-local ROM generator,
  following the already validated Build 68/72/73 pattern.  It verifies that
  the generated SystemVerilog defines module `bootrom`; no ignored generated
  source is treated as an input.
- The Build 52-derived self-contained validator now requires both generated
  ROM modules in `source_snapshot`.  Build 75 still has no synthesis, PDI/LTX,
  programming, or board result; the next attempt must again start from a clean
  committed tree.

---

## 2026-07-15 -- Fixed clean-tree PyHP generation for Windows Vivado

- The first Build 75 attempt passed bootrom regeneration, all forced
  tile/L1.5 PyHP generation, the 1x1 define check, and the 2 GiB CVA6 aperture
  check, then stopped during self-contained project creation before synthesis.
  The first missing unrelated output was `bram_sdp_wrapper.tmp.v`; the legacy
  common helper tried `exec pyhp.py` inside Windows Vivado and returned
  `couldn't execute "pyhp.py": no such file or directory`.
- This was a clean-source closure bug in the P3 create flow.  Adding the Unix
  tool directory to Windows Tcl `PATH` does not make a shebang-based `.py`
  script directly executable, while old work trees masked the defect with
  ignored generated outputs.
- `scripts/p3_create_bd.tcl` now removes stale outputs and explicitly
  generates each missing PyHP result from the actual RTL and global-include
  lists before the common helper runs.  Windows full Vivado invokes repo-local
  `pyhp.py` through WSL `python3`; native Linux invokes `python3` directly.
  Failed or empty generation stops project creation, and generated `.tmp`
  files remain build products rather than committed source.
- No Build 75 synthesis, PDI, LTX, or board result exists from the failed
  attempt.  The rerun must start from the clean committed tree and pass the
  exact post-synthesis probe gate before implementation.

---

## 2026-07-15 -- Prepared Build 75 causal PC/L1.5 diagnostic flow

- Inspected the synthesized Build 66 checkpoint before defining another PDI.
  Commit-head PC/FU/op, LSU commit state, and aggregate L1.5 stage stalls still
  exist after synthesis, but the individual tag/index/MSHR/NoC1 blockers and
  the original PCX acknowledgement are optimized away.  A new synthesis is
  therefore required; reusing the Build 66 synthesis checkpoint cannot answer
  the causal question.
- Added conditional `P3_BUILD75_PC_L15_DEBUG` instrumentation to the L1.5
  pipeline.  It retains two explicitly packed 64-bit buses containing the
  accepted request/stage state and the detailed tag/index/MSHR/NoC1 credit
  blockers.  A saturating counter asserts a trigger only after an ordinary
  store remains valid without L1.5 acknowledgement for 256 cycles.  Builds
  without this define are logically unchanged.
- Added the Build 75 one-hart wrapper and a fail-closed post-synthesis hook.
  The hook requires every exact L1.5 and CVA6 scoreboard/LSU net, inserts an
  8192-sample `u_ila_build75` with the trigger at sample 4096, and rejects an
  ambiguous, renamed, or optimized probe before implementation.  The common
  Build 52 runner now supports forced extra PyHP templates, a post-synthesis
  hook, and build-specific mandatory LTX tokens.
- Added a persistent capture script and a format-tagged decoder.  The decoder
  requires format `0x75`, the 256-cycle no-ack condition, a valid ordinary
  store, a valid CVA6 STORE at the commit head, and at least one active
  tag/index/S2-S3/MSHR/NoC1 blocker.  It can resolve the captured PC against a
  Linux `vmlinux`; its focused unit tests pass.
- Build 75 has not yet completed synthesis or implementation.  There is no
  Build 75 PDI/LTX, programming result, ILA capture, or exact stuck-instruction
  claim yet.  The existing one-hart OpenSBI/Linux 6.6 flat image remains on the
  SD card and must be reused unchanged.  Per the agreed plan, do not build the
  OpenSBI/Linux 5.1 control image and do not repeat the unchanged failing
  Build 66/OpenSBI/Linux 6.6 baseline.

---

## 2026-07-15 -- Build 66 single-hart control reproduces the later L1.5 store-accept stop

- Programmed the unchanged board-validated Build 66 single-core PDI
  `p3_top_build66_normal_spi_sd_boot.pdi` through the Windows full Vivado
  2024.2.2 client and remote hw_server/XVC path.  Vivado completed with
  `DONE bit: HIGH`, debug hub `0x3ffc0000000`, and all four ILAs enumerated;
  this was not a programming failure.
- The physically readback-verified one-hart flat image passed the complete
  legacy bootrom path: UART reported a 1x1/one-core design with 2048 MiB DDR,
  initialized SD, parsed GPT, copied all 65,536 sectors, and printed `done!`.
  The two persistent UART files are
  `ttyUSB0_20260715_170943_build66_1hart_opensbi.log` (27,226 bytes) and
  `ttyUSB0_20260715_171943_build66_1hart_opensbi_cont.log` (33,762 bytes);
  both ttyUSB1 counterparts remained empty.
- OpenSBI v1.8 reported `Platform HART Count : 1`,
  `aclint-mtimer @ 234375Hz`, next address `0x80200000`, and FDT argument
  `0x81600000`.  UART then remained unchanged after the complete OpenSBI
  platform summary; no Linux banner or shell is claimed.
- A live non-resetting Build 66 ILA capture found a stable 8-byte store at
  `0x8189b780`: request type `1`, `transducer_l15_val=1`, and the actual
  request-accept signal `l15_transducer_ack=0`.  Heartbeat and resets remained
  active, while the sticky L1.5 error and DDR BRESP/RRESP error bits were
  clear.  Ariane status `0xf4` means timer pending with IPI, external IRQ, and
  debug request all low.
- This is the same observable L1.5 S1 acceptance/backpressure class as the
  later Build 73 two-hart store at `0x8185dc80`, now reproduced without a
  second hart or IPI.  It therefore rejects SMP/IPI concurrency as a necessary
  trigger, but does not yet identify the internal S1 blocker.  The remaining
  candidates exposed by RTL are matched-MSHR/tag conflict, S2/S3 or
  same-index backpressure, no free MSHR, and unavailable NoC1 command/data
  credits; resolving them requires a new ILA payload with those individual
  conditions and hart PC.

---

## 2026-07-15 -- Wrote and fully read back the one-hart Build 66 image

- Reidentified the remotely inserted card before writing as `/dev/sdc`, model
  `Multi-Reader -1`, USB transport, removable flag set, capacity
  31,914,983,424 bytes, with no mounted filesystem.  The other multi-reader
  slots remained 0 B, and no system disk was touched.
- Reverified the uploaded 268,435,456-byte candidate in `/tmp` at SHA-256
  `75daffba0f36a393f62f9222deb93a38753f06fec9fd2a09b5b16181363152b2`,
  wrote it to `/dev/sdc` with a 4 MiB block size and `conv=fsync`, then
  synchronized and reread the partition table.
- Read the complete written 256 MiB span back from the physical card.  Its
  SHA-256 was
  `75daffba0f36a393f62f9222deb93a38753f06fec9fd2a09b5b16181363152b2`,
  exactly matching the source image.  Media preparation is therefore closed;
  the remaining gate is to return the card to the FPGA, start UART capture,
  and program the unchanged validated Build 66 PDI.

---

## 2026-07-15 -- Built and audited the Build 66 one-hart OpenSBI/Linux image

- Completed the formal local build in a clean
  `build/p3_1hart_build66/` work tree.  OpenSBI v1.8 was rebuilt after applying
  the current P3 platform patch; the source contains no CSR `0x701` write and
  parses the DT timebase before `aclint_mtimer_cold_init()`.  The firmware ELF
  entry is `0x80000000`, `fw_next_addr` contains `0x80200000`, and
  `fw_next_arg1` resolves to the Build 66 flat-layout DTB address
  `0x81600000`.
- Reused only the previously board-tested Linux 6.6 Image after exact hash
  verification.  The final component SHA-256 values are OpenSBI
  `dbe9800a1ce2cff31342ff4f7cfb4e607d739ccaa33c16cc85c7f6519898d0dc`,
  Linux `47c9daa86019503459071e38ac4a54624a40062401eae4ff64b948bf16a6441b`,
  one-hart DTB
  `731f65c42fb38a01fbfa8501c8800c57da09b4bb95ae8e1785abf7395efbdbfa`,
  and XSBench v2 initramfs
  `60aaf85d266a77cce0c2cf59a22221bcc321c2f15fd36ce242a092e0b516f142`.
- The compiled DTB independently passes the P3 validator with exactly
  `cpu@0`, 2 GiB memory `[0x80000000,0x100000000)`, 234375 Hz timebase, and
  initramfs range `[0x81700000,0x81807d9c)`.  All four components were read
  back byte-for-byte from their final offsets inside the fixed Build 66
  `[0x80000000,0x82000000)` copy window.
- The board candidate is
  `build/huaprop3/opensbi1_build66/p3_opensbi_linux_1hart_build66_flat.img`,
  exactly 268,435,456 bytes, SHA-256
  `75daffba0f36a393f62f9222deb93a38753f06fec9fd2a09b5b16181363152b2`.
  Its first GPT partition starts at LBA 2048; the exact 65,536-sector payload
  span hashes to
  `084dd0395df6ed2fd4f544b8d7e352f91e622c52f33f640266e29bd1368909a6`.
  SD write/readback and unchanged-Build-66 board boot remain pending.

---

## 2026-07-15 -- Single-hart flow adds hash-gated Linux Image reuse

- The first formal Build 66 one-hart build stopped before compilation because
  the local shell has `riscv64-unknown-elf-gcc` but no
  `riscv64-unknown-linux-gnu-gcc`; no candidate image was emitted from that
  attempt.
- Added an explicit `P3_64CORE_LINUX_IMAGE` override to the common preparation
  flow, gated by mandatory `P3_64CORE_LINUX_IMAGE_SHA256`.  The Build 66
  wrapper exposes the corresponding `P3_BUILD66_*` variables.  This permits
  the local host to rebuild the corrected OpenSBI with the bare-metal compiler
  while reusing only the already board-tested Linux 6.6 Image, SHA-256
  `47c9daa86019503459071e38ac4a54624a40062401eae4ff64b948bf16a6441b`.
- This is deliberately different from `P3_64CORE_USE_PREBUILT=1`: OpenSBI is
  still rebuilt for `FW_JUMP_ADDR=0x80200000` and the Build 66 flat-layout FDT
  address `0x81600000`.  The single-hart DTB and initramfs are also regenerated;
  only the hardware-independent Linux kernel Image is hash-verified and
  copied.  Formal image generation and board testing remain pending.

---

## 2026-07-15 -- Prepared unchanged-Build-66 one-hart OpenSBI/Linux control

- Selected the board-validated Build 66 PDI as the single-core hardware
  control without changing its synthesized RTL or bootrom.  The published PDI
  is `p3_top_build66_normal_spi_sd_boot.pdi`, SHA-256
  `ee30fe4d052c763c9fc2fa72bf71cc8363173ad210ea8fb106533ac3081dd62d`;
  its earlier board proof reached `DONE bit: HIGH`, four ILAs, and an
  interactive Linux shell through the legacy BBL stack.
- Audited the embedded normal bootrom instead of assuming it accepts the newer
  P3OS bundle.  It copies exactly 65,536 sectors from the first GPT partition
  to `[0x80000000,0x82000000)` and jumps to `0x80000000`.  A normal
  `P3OS/BI64` image would place its header at the jump target and is therefore
  incompatible with the unchanged PDI.
- Added `p3_prepare_build66_1hart_opensbi_image.sh` and the fail-closed
  `p3_make_build66_opensbi_flat_image.py`.  The new path rebuilds the corrected
  OpenSBI/Linux 6.6 stack for a one-hart DTB and packs firmware at
  `0x80000000`, Linux at `0x80200000`, DTB at `0x81600000`, and initramfs at
  `0x81700000`.  It checks the P3 hardware memory map, exact DTB initrd range,
  fixed 32 MiB copy window, component overlap, and each component's packed
  SHA-256 readback.
- Expanded the platform regression from five to seven tests and added harts=1
  to the DTB matrix.  All seven pass, including positive Build 66 component
  placement/readback and negative out-of-window rejection.  This records a
  generated-flow milestone only; the formal one-hart image build, SD write,
  UART capture, and OpenSBI/Linux board result remain pending.

---

## 2026-07-15 -- P3 DTB, bundle, and CVA6 aperture generation now fail closed

- Replaced the OpenSBI DTS generator's duplicated P3 memory/peripheral
  constants with values read directly from
  `piton/design/xilinx/huaprop3/devices_ariane.xml`.  The generated memory,
  SD, UART, CLINT, and PLIC nodes therefore share the hardware device map as
  their address source.  CPU and UART clocks remain explicit inputs, while
  the DT timebase is derived as `CPU frequency / 128`; an optional explicit
  timebase value is treated as an assertion and a stale `1000000` Hz value is
  rejected against the current `30000000 / 128 = 234375` Hz contract.
- Reordered `p3_prepare_64core_opensbi_image.sh` so it selects the actual
  initramfs before generating the DTB.  The generator now writes
  `linux,initrd-start` and computes the exclusive `linux,initrd-end` from the
  real file size.  With the current 1,080,732-byte XSBench v2 rootfs the
  automatically generated range is exactly
  `[0x90000000,0x90107d9c)`, eliminating the manual post-generation patch that
  previously allowed the truncated-initrd regression.
- Added a DTB validator that round-trips the compiled blob with `fdtget` and
  checks the complete CPU-node set, per-CPU frequency, CLINT/PLIC interrupt
  tuple counts, PLIC source count, bootargs, device-map ranges, and exact
  initramfs span.  The SD bundle packer independently rejects a DTB whose DDR
  or initramfs range disagrees with the bundle, and rejects empty,
  overlapping, out-of-DDR, or overflowing component load ranges before
  creating the image.
- Closed the separate Build 72/73 hardware-generation hole in the common
  Build 52-derived Vivado runner.  `tile.v.pyv` is now in the forced PyHP set,
  generated under `PROTOSYN_RUNTIME_BOARD=huaprop3`, and both the live
  `tile.tmp.v` and self-contained source snapshot must match the `mem` entry
  from `devices_ariane.xml` in CVA6's execute and cacheable apertures.  The
  validator accepts the known-good Build 66 2 GiB snapshot and rejects the
  stale Build 73 `0x40000000` snapshot.
- Added five focused regression tests.  Both 2-hart and 64-hart generated DTBs
  pass; intentional 1 GiB device-map mismatch, 1 MHz timebase mismatch,
  component overlap/out-of-range, and stale 1 GiB tile aperture cases fail.
  Full prebuilt 2-hart and 64-hart flows also completed through DTB compile,
  validation, 256 MiB GPT/P3OS bundle generation, and manifest hashing in
  clean temporary output directories.  This validates the generation flow;
  it does not yet constitute a new PDI build or board boot.

---

## 2026-07-15 -- Corrected Build 73 handshake decode narrows the later stop to L1.5 S1 backpressure

- Corrected the direction of one signal in the earlier `0x87` handshake-byte
  description.  In `wt_l15_adapter.sv`, `transducer_l15_req_ack` is
  `l15_req_o.l15_req_ack`: Ariane accepting an L1.5 **return** packet when the
  return FIFO has space.  It is not L1.5 accepting the outstanding request.
  The actual request acknowledgement is `l15_transducer_ack`, driven by
  `l15_pipeline.l15_pcxdecoder_ack`.
- The corrected `0x87` decode is still a real unaccepted-request observation:
  bit 7 has `transducer_l15_val=1`, bit 5 has
  `l15_transducer_val=0`, and, decisively, bit 4 has
  `l15_transducer_ack=0`; the error bit is clear and reset/clock bits are all
  high.  Bit 6 (`transducer_l15_req_ack=0`) is expected while no return packet
  is valid and is not evidence about request acceptance.
- Request type `1` is an ordinary store.  The L1.5 decoder assigns ordinary
  stores to `L15_ACK_STAGE_S1`, and S1 only asserts the request ack when
  `val_s1 && !stall_s1`.  Therefore the persistent store at `0x8185dc80`
  localizes the later forward-progress loss to the L1.5 S1 acceptance path.
  RTL shows the still-unresolved S1 causes are matched-MSHR/tag conflict,
  S2/S3 backpressure or same-index conflict, no free MSHR, and unavailable
  NoC1 command/data credits.  The retained Build 73 ILAs do not expose those
  subconditions, so none is yet the proven transaction-level root cause.
- Timer and IPI pending are simultaneous observations, not causal proof: a
  timer naturally becomes pending after a long stall, and the IPI may reflect
  two-hart activity.  Likewise, the last printk near `pty_init` identifies the
  workload phase but not the store's owning instruction or object.  The next
  discriminators remain a `maxcpus=1` software A/B and an L1.5 ILA containing
  the S1 stall vector, S2/S3 downstream stalls, MSHR/NoC credits, hart, and PC.

---

## 2026-07-15 -- `mem=1G` clears the Build 73 Linux page-table-stage stop

- Wrote the prepared 256 MiB control image
  `p3_opensbi_linux_2hart_timerfix_mem1g.img` to the remotely attached SD
  card and read the full written span back.  The source and readback SHA-256
  both matched
  `7a61b8e7426a79f628b84331faec0642ae722e745ea11905f78419b56a5e60d9`.
  The unchanged Build 73 PDI matched
  `52e1400a5c457020212c2d3329ee341163d776a66b9117344a5a96aa0add2c65`.
- Two programming attempts before the board was reconfigured completed with
  `DONE bit: HIGH` and four ILAs but produced 0-byte UART logs.  Both immediate
  captures were identical: the core had reached bootrom `sd_copy` near
  `0xfff10106b0`, while the UART debug state showed only an AXI write-address
  handshake and no TX-low event.  After the board was configured and the FTDI
  interfaces re-enumerated, the next unchanged-PDI attempt restored normal
  UART.  Treat the first two attempts as a board-startup/UART-AXI transient,
  not an SD-image result.
- The valid run is
  `~/p3_uart_logs/ttyUSB0_20260715_155614_build73_mem1g_boardreset.log`
  (94097 bytes in the completed 600-second capture).  Bootrom passed DDR,
  SD initialization, GPT/`P3OS` parsing, and all four component copies.
  OpenSBI v1.8 again reported
  `Platform Timer Device : aclint-mtimer @ 234375Hz`.
- Linux printed both decisive control markers:
  `Memory limited to 1024MB` and a kernel command line ending in `mem=1G`.
  It reported `992124K/1048576K available`, crossed the former silent point
  after reserved-memory setup, ran a large set of initcalls, and reached
  `Freeing initrd memory: 1052K`.  This is a positive A/B result: keeping
  Linux out of physical `[0xc0000000,0x100000000)` removes the old
  `setup_vm_final()`-region stop.  It proves that upper-GiB use or the extra
  mapping is the trigger; a corrected PDI with 2 GiB CVA6 execute/cacheable
  apertures is still required to attribute the mechanism specifically to the
  stale `0x40000000` lengths.
- The run did not reach a shell.  UART stopped after `calling pty_init` and the
  later asynchronous initrd-free message, so the last printk must not be
  treated as an exact function-level root cause.  A non-resetting ILA capture
  showed `p3_dbg_core_bus64=0x008185dc800cf687`: last L1.5 address
  `0x8185dc80` (inside the low 1 GiB), Ariane request type `1` (store), size
  `4`, and handshake byte `0x87` (`transducer_l15_val=1` with
  `transducer_l15_req_ack=0`; resets and clock released).  The Ariane status
  byte `0xf6` had timer and IPI pending, while the sticky L1.5 error bit and
  DDR BRESP/RRESP errors remained clear.  This is a distinct, later
  store-accept/forward-progress problem, not a recurrence of the upper-GiB
  page-table access.
- Next controls are: (1) rebuild the formal corrected PDI with both aperture
  lengths at `0x80000000` and boot the unbounded timer-fix image, and (2) for
  the later stop, add hart/PC and L1.5 request-ack state or run a `maxcpus=1`
  software control before assigning it to SMP/IPI, `pty_init`, or a specific
  L1.5 transaction.

---

## 2026-07-13 -- Delivered the Build 73 `mem=1G` Linux-early discriminator

- Built a controlled SD-image variant from the board-verified two-hart timer
  fix.  The only DTB semantic change is appending `mem=1G` to the existing
  Linux command line.  The DT still describes the physical 2 GiB DDR, two
  harts, 234375 Hz timebase, UART/CLINT/PLIC topology, and initrd range
  `[0x90000000,0x90107d9c)` exactly as before.  RISC-V Linux parses the early
  memory limit before `paging_init()`, so its memblock allocations and final
  linear map are confined to `[0x80000000,0xc0000000)` without changing what
  OpenSBI sees.
- The diagnostic DTB is
  `build/huaprop3/opensbi64/p3_opensbi_2hart_initrd_mem1g.dtb`, 2228 bytes,
  SHA-256
  `17bfda300c2298ef3bd78391f4c6b627503df4da8e57c57330e8623da2e97c73`.
  A decompile comparison against the timer-fix DTB showed only the intended
  bootarg addition.
- The resulting 256 MiB GPT/P3OS image is
  `build/huaprop3/opensbi64/p3_opensbi_linux_2hart_timerfix_mem1g.img`,
  SHA-256
  `7a61b8e7426a79f628b84331faec0642ae722e745ea11905f78419b56a5e60d9`.
  Independent header parsing and full component-span reads verified all four
  load addresses and hashes.  OpenSBI (`69d3549f...`), Linux
  (`47c9daa8...`), and initramfs (`60aaf85d...`) are byte-identical to the
  board-tested timer-fix image; only the DTB differs.  The newly generated GPT
  GUID is expected and is not consumed by the bootrom.
- The image, DTB, and manifest are uploaded under the same basenames in the
  remote host's `/tmp`.  Remote sizes and complete SHA-256 values exactly
  match the local artifacts.  No SD write occurred; the card remains in the
  FPGA slot with the previous timer-fix image.
- Board interpretation: success must show the command line containing
  `mem=1G`, `Memory limited to 1024MB`, and progress beyond the current
  reserved-memory boundary; an ILA capture should no longer show Linux access
  at or above `0xc0000000`.  Such a success proves that upper-GiB use or the
  extra mapping triggers the stop, but the corrected-PDI control is still
  needed to attribute it specifically to CVA6's stale cacheable aperture.

---

## 2026-07-13 -- Found Build 73's 1 GiB/2 GiB CVA6 DDR aperture mismatch

- The timer-fix board run still stopped after Linux's two reserved-memory
  messages.  A fresh, non-resetting capture of all four live Build 73 ILAs
  completed through local Windows Vivado.  The core probe repeatedly issued
  and completed an 8-byte load at physical `0xffe5e000`; the matching DDR-side
  address was `0x7fe5e000`.  No L1.5 or DDR response error was asserted, reset
  remained released, and the heartbeat continued.  This refines the earlier
  one-shot observation: the core is polling or repeatedly walking the same
  location, not sitting behind one permanently unacknowledged NoC request.
- The synthesized Build 73 XPR explicitly consumes
  `p3b73_2x1_diag/source_snapshot/.../tile.tmp.v`.  That file configures both
  `ExecuteRegionLength` and `CachedRegionLength` as `0x40000000`, so CVA6
  treats only `0x80000000` through `0xbfffffff` as executable/cacheable RAM.
  The HuaPro P3 device map and the timer-fix DTB both declare the real range as
  `0x80000000` bytes (2 GiB, through `0xffffffff`).  The observed
  `0xffe5e000` access is therefore in the incorrectly classified upper half.
- This is a confirmed PDI configuration defect and a high-confidence cause of
  the current Linux-early stop, but causal closure still requires a control
  run.  The last UART message is emitted before RISC-V `setup_vm_final()`,
  which allocates final page tables from memblock, builds the linear mapping,
  writes `satp`, and flushes the TLB.  A page-table/PTE access near the top of
  the declared 2 GiB RAM is consistent with that silent window.  Linux has not
  yet reached its timer, IRQ, or SMP initialization, so the already verified
  234375 Hz timer fix is not the immediate explanation.
- The stale value entered the self-contained project through the PyHP flow.
  The Build 52-derived runner regenerates `define`, chip, chipset, and XY-map
  outputs, but omits `tile.v.pyv`; project creation then reuses any existing
  `tile.tmp.v` unless the `.pyv` file has a newer timestamp.  Build 72 and 73
  therefore copied the same July 8 stale 1 GiB output, while validated Build
  66 and 67 snapshots contain the correct `0x80000000` lengths.
- The cheapest causal discriminator is the unchanged Build 73 PDI with a DTB
  or bootarg limiting Linux to `mem=1G`; the formal repair is a new PDI that
  regenerates `tile.tmp.v` in the HuaPro P3 device-map context and fails its
  snapshot preflight unless both DDR lengths are `0x80000000`.  The current
  timer-fix SD image does not need to be rewritten merely to repair the PDI.

---

## 2026-07-13 -- Board verified the corrected 234375 Hz OpenSBI timer

- A persistent UART capture was started before programming at
  `~/p3_uart_logs/ttyUSB0_20260713_202255_build73_timerfix.log` on the remote
  host.  Local Windows full Vivado 2024.2.2 then programmed the unchanged
  Build 73 PDI (SHA-256 `52e1400a...2c65`) through remote hw_server/XVC.
- Programming exited zero with `DONE bit: HIGH`, debug hub
  `0x3ffc0000000`, and all four ILAs (`hw_ila_1` through `hw_ila_4`)
  enumerated.  The bootrom subsequently passed DDR, SD initialization,
  GPT/`P3OS` parsing, and all four component copies from the newly written
  card.
- The real board UART printed OpenSBI v1.8 and the decisive line
  `Platform Timer Device : aclint-mtimer @ 234375Hz`.  The prior erroneous
  `1000000Hz` value is absent.  This closes the mtimer registration-order bug:
  parsing the DTB inside `openpiton_timer_init()` before
  `aclint_mtimer_cold_init()` updates the registered timer as intended.
- OpenSBI handed off successfully and Linux 6.6.0 printed its banner plus the
  SBI TIME/IPI/RFENCE and early-console lines.  The 39,980-byte UART log then
  remained unchanged for 91 seconds after the two reserved-memory lines at
  `[0.000000]`; its SHA-256 is `9ed6e96c...56ccd0`.  Timer-frequency closure
  therefore does not claim Linux SMP or shell closure; that Linux-early stop
  remains a separate investigation.

---

## 2026-07-13 -- Wrote and fully read back the OpenSBI timer-fix image

- Immediately before writing, `/dev/sdc` was revalidated as the expected
  unmounted 31,914,983,424-byte USB removable disk, model
  `Multi-Reader -1`; no stale device name or size was assumed.
- Wrote all 268,435,456 bytes of
  `/tmp/p3_opensbi_linux_2hart_timerfix.img` to `/dev/sdc`, synchronized and
  flushed the device, then read back the same complete 256 MiB span.
- The full-card-span readback SHA-256 was
  `c420d81dcbec5f637d8ebcdeb5fab8f5aaba16badc2ee49d2797c096f334f55e`,
  exactly matching the locally built and remotely staged image.  Partition
  reread exposed `/dev/sdc1` at 267,369,984 bytes.
- This closes only the image-media integrity gate.  Return the card to the
  FPGA, start persistent UART capture, and program the matching Build 73 PDI
  with local full Vivado through the remote hw_server/XVC path.  Board closure
  requires the OpenSBI banner to report `aclint-mtimer @ 234375Hz`; Linux SMP
  and shell progress remain subsequent gates.

---

## 2026-07-13 -- Corrected the post-delivery SD-reader state

- A final read-only remote check after image delivery showed `/dev/sdc` had
  re-enumerated as the expected 31,914,983,424-byte USB removable device,
  model `Multi-Reader -1`, with no mountpoint.  The preceding size-zero result
  was transient and must not be treated as the current physical state.
- No SD write command was issued during the timer-fix build or transfer.  The
  card still contains the previous image; the new candidate remains staged at
  `/tmp/p3_opensbi_linux_2hart_timerfix.img`, SHA-256 `c420d81d...f55e`.
  Revalidate the same device identity immediately before any subsequent write.

---

## 2026-07-13 -- Fixed OpenSBI mtimer registration and delivered the two-hart image

- The tracked P3 OpenSBI patch now parses the DTB `timebase-frequency` inside
  `openpiton_timer_init()` before `aclint_mtimer_cold_init()` registers the
  timer device.  The ineffective late parse was removed from `early_init`.
  The preparation script also rejects an already-patched work tree that lacks
  this new signature, preventing a stale 1 MHz firmware rebuild.
- The complete patch applied to a pristine supplied OpenSBI archive with no
  fuzz.  Two independent clean builds produced the same 270048-byte
  `fw_jump.bin`, SHA-256
  `69d3549ff1ada4faa09666247126b22656d32c9d8cc66ad79a4f5be018e00b7d`.
  Disassembly confirms the call order is DTB frequency parse, successful
  nonzero update of `mtimer.mtime_freq`, then `aclint_mtimer_cold_init()`.
- The firmware was bundled with the unchanged board-exercised Linux Image,
  two-hart DTB, and initramfs as
  `build/huaprop3/opensbi64/p3_opensbi_linux_2hart_timerfix.img`.  The image is
  268435456 bytes with SHA-256
  `c420d81dcbec5f637d8ebcdeb5fab8f5aaba16badc2ee49d2797c096f334f55e`.
  GPT verification and the `P3OS`/`BI64` four-component header passed.
- Full-span reads from the image reproduced the component hashes: OpenSBI
  `69d3549f...`, Linux `47c9daa8...`, DTB `7ae1ed74...`, and initramfs
  `60aaf85d...`.  The DTB still declares 234375 Hz; only OpenSBI changed from
  the previously board-tested no-CSR image.
- The complete image is uploaded to the remote host as
  `/tmp/p3_opensbi_linux_2hart_timerfix.img`; remote size and SHA-256 exactly
  match the local artifact.  It has not been written to SD because the reader
  currently reports `/dev/sdc` with size zero while the card remains in the
  FPGA.  Final closure still requires an SD write/readback and board UART proof
  of `aclint-mtimer @ 234375Hz`.

---

## 2026-07-13 -- Found the persistent OpenSBI mtimer frequency registration bug

- Build 73's current two-hart UART log reports
  `Platform Timer Device : aclint-mtimer @ 1000000Hz`, while the implemented
  CLINT `mtime` rate is 30 MHz / 128 = 234375 Hz and the supplied DTB correctly
  declares `/cpus/timebase-frequency = <234375>`.
- This is not a regression from the no-CSR-0x701 image.  Every retained P3
  OpenSBI banner log checked, from the old 64-hart dbg8 through dbg26 images to
  the current two-hart image, reports the same 1 MHz value.  The reliable
  64-hart `ev_retry2.log` shell run also carried this mismatch.  Build 66/67
  used BBL rather than OpenSBI and are not instances of this OpenSBI bug.
- The OpenPiton platform data starts with a hard-coded 1 MHz default.
  `sbi_timer_init()` registers and copies that value before
  `sbi_platform_early_init()` later parses the DTB's 234375 Hz value into the
  platform-local structure.  The late update therefore cannot change the
  already registered timer device or its banner value.
- This is a real firmware-platform bug, but it is not sufficient evidence for
  the current Linux-early stop.  SBI TIME programs Linux's absolute tick value
  directly into `mtimecmp`, without scaling by the banner frequency, and the
  old 64-hart image reached a shell with the same mismatch.  The wrong value
  does affect OpenSBI delay/timeout helpers, whose elapsed-time conversions are
  short by a factor of 1000000 / 234375 = 64 / 15 (about 4.27).
- The corrective implementation will parse `timebase-frequency` inside the
  OpenPiton `timer_init` callback immediately before
  `aclint_mtimer_cold_init()`, retaining the platform default only as an
  explicit fallback when the DTB property is unavailable.  A fresh two-hart
  image and board UART proof of `aclint-mtimer @ 234375Hz` are still required.

---

## 2026-07-13 -- Build 73 ILA localizes the post-OpenSBI Linux-early stop

- After the successful final-image boot reached the Linux 6.6 early console,
  a Windows-Vivado immediate capture of all four live Build 73 ILAs completed
  successfully.  The four CSVs are temporary analysis artifacts under
  `/tmp/b73_linux_early_*.csv`; no PDI, RTL, or SD-card content changed.
- Across each 1024-sample capture, the heartbeat continued to toggle while
  `core_seen16=0x7fff`, `uart_seen16=0xfeff`, and `ddr_seen16=0xef7f` stayed
  constant.  The last recorded L1.5 and DDR address was `0x00ffe5e000`.
  The core-bus low state was `0x07`: both request-valid signals and both L1.5
  handshake signals were low, no L1.5 error was asserted, and reset/clock
  enables remained high.  The sampled Ariane interrupt flags were also idle.
- This rules out a visibly outstanding L1.5/NoC/DDR transaction at the stopped
  point, but the retained probes expose no PC or pipeline state.  It is valid
  evidence of a later Linux-early internal forward-progress boundary, not an
  exact root cause and not a revival of the retired CSR 0x701/OpenSBI gate.

---

## 2026-07-13 -- Build 73 clears the CSR-disabled OpenSBI gate

- With the final SD image in the FPGA slot, local Windows full Vivado 2024.2.2
  programmed Build 73 PDI
  `52e1400a5c457020212c2d3329ee341163d776a66b9117344a5a96aa0add2c65`
  through hw_server `100.93.77.36:3121` and XVC `202.197.4.99:2540`.
  Vivado reported `Successfully programmed PDI`, `DONE bit: HIGH`, debug hub
  `0x3ffc0000000`, and all four expected ILAs.
- The pre-started persistent UART capture recorded the diagnostic bootrom
  parsing the four-component P3OS/BI64 bundle and completing the large Linux
  copy.  It then printed `OpenSBI v1.8` and the Linux 6.6.0 banner, including
  SBI TIME, IPI, and RFENCE extension discovery.  This is direct board evidence
  that leaving the Ariane L1 D-cache enabled (omitting `csrw 0x701, zero`)
  clears the earlier OpenSBI `sbi_hart_init()` forward-progress gate.
- UART stopped after Linux's initial reserved-memory lines at timestamp
  18:32:34 +0800 and remained unchanged for a further 35-second observation.
  Thus this run does not claim Linux SMP completion or a shell; the remaining
  issue is later than the now-retired CSR-disabled OpenSBI gate and needs a
  separate Linux-early-boot/SMP localization pass.

---

## 2026-07-13 -- Windows Vivado recovered the P3 read-only XVC path

- Re-ran the hardware-manager check with the required local Windows full
  Vivado 2024.2.2 client, connecting to hw_server `100.93.77.36:3121` and
  then opening XVC `202.197.4.99:2540`.  The read-only session exited 0 and
  enumerated `arm_dap_0 xcvp1902_1` (`PART=xcvp1902`).
- `refresh_hw_device` connected through DPC JTAG and completed successfully.
  This contradicts the preceding 120-second timeout: the XVC service is
  currently usable through the documented Windows-client route, so that
  timeout was transient/session-specific rather than proof of an SD, OpenSBI,
  or persistent board fault.
- No PDI was assigned or programmed in this probe.  The next action is the
  hash-verified Build 73 PDI with a fresh UART capture already running; only
  `DONE bit: HIGH` followed by UART evidence can validate the final image.

---

## 2026-07-13 -- Final-image board run blocked at XVC protocol enumeration

- The verified final SD image was returned to the FPGA and raw UART capture was
  started before programming. Build 73 PDI
  52e1400a5c457020212c2d3329ee341163d776a66b9117344a5a96aa0add2c65
  was submitted with a 900-second programming limit, but the command expired
  with exit code 124. It produced neither DONE bit HIGH nor a Vivado success or
  failure line, and the UART capture remained zero bytes.
- This is not an SD or OpenSBI result. The remote hw_server process was alive
  and TCP 3121 was reachable. Direct Windows and WSL TCP access to the XVC
  target 202.197.4.99:2540 failed, while the remote Ubuntu host could connect.
- An SSH local forward through that Ubuntu host made Windows TCP access to the
  XVC port succeed. Even through the forward, a read-only Vivado script could
  not enumerate devices within 120 seconds. Restarting hw_server cleared stale
  client sessions and restored its listener, but did not change the XVC
  protocol timeout.
- Treat the active blocker as the XVC target/service protocol layer. Do not
  reprogram the same PDI or interpret the zero-byte UART file as a firmware
  failure. Restore or reset the XVC target at 202.197.4.99:2540, then perform
  a read-only device enumeration before another PDI attempt.

---

## 2026-07-13 -- Wrote and read back the all-hart L1-enabled final image

- The remote target was revalidated immediately before writing as /dev/sdc:
  31,914,983,424 bytes, USB, removable, unmounted, model Multi-Reader -1.
  The uploaded 268,435,456-byte image matched local SHA-256
  bf8fca762d7cd9a599850e1d795fde664edcdd0cc2ec869964d413ebbc0360ff.
- A direct-I/O 4 MiB-block write completed for all 256 MiB at approximately
  23.2 MB/s, followed by flush and partition-table reread. An independent
  direct-I/O read of the same 64 blocks completed at approximately 63.6 MB/s.
- The complete card readback SHA-256 was
  bf8fca762d7cd9a599850e1d795fde664edcdd0cc2ec869964d413ebbc0360ff,
  exactly matching p3_opensbi_linux_2hart_nocsr701_final.img. No write, flush,
  read, or device-identity error occurred.
- The card is ready to move from the remote reader to the FPGA slot. Board
  validation still requires UART capture started before Build 73 programming;
  no OpenSBI/Linux result is claimed from the media check alone.

---

## 2026-07-13 -- Packaged all-hart L1-enabled final 2-core image

- The corrected complete P3 OpenSBI patch applied to a pristine source archive,
  contained no CSR 0x701 write, and completed a fresh generic FW_JUMP build.
  The resulting 270,048-byte firmware has SHA-256
  ba46ef13674cc2c7bb418b426f8d4b19a00495023d15b92acd67b892ff1e8bb8.
- The new 268,435,456-byte board candidate is
  build/huaprop3/opensbi64/p3_opensbi_linux_2hart_nocsr701_final.img, SHA-256
  bf8fca762d7cd9a599850e1d795fde664edcdd0cc2ec869964d413ebbc0360ff.
  GPT verification reports no problems and the partition begins at LBA 2048
  with the expected P3OS/BI64 header.
- Independent reads of all four embedded component spans matched their source
  hashes: OpenSBI ba46ef13..., Linux 47c9daa8..., DTB 7ae1ed74..., and initramfs
  60aaf85d.... The superseded hart-gated candidate was deleted to prevent an
  accidental SD write.
- This remains a local image validation. It has not yet been written to the SD
  card; full-card readback and Build 73 UART boot evidence are still required.

---

## 2026-07-13 -- Corrected CSR 0x701 handling for every P3 hart count

- The earlier greater-than-two-hart gate is superseded. The 2026-06-27 Build
  68 dbg23 board run already showed the same failure on 64 harts: adding only
  the CSR 0x701 zero write to the bootable dbg21 lineage froze immediately
  after the bootrom's OpenSBI jump, with no banner for 30 minutes. The new
  two-hart marker control independently localizes the same mode change inside
  sbi_hart_init().
- A correct cacheless CPU would remain architecturally functional, only slower.
  Here the custom CSR changes normal DDR traffic to the Ariane write-through
  D-cache's non-cacheable L1.5/NoC path and loses forward progress. This is an
  unsupported or defective integration path, not expected two-hart behavior.
  The exact blocked request/state still needs transaction-level ILA evidence.
- The tracked clean-package patch now omits the CSR write for all P3 hart
  counts and keeps L1 enabled. The preparation flow rejects both the temporary
  two-core isolation tree and the superseded hart-gated tree, so an old
  extracted source directory cannot silently recreate the bad 64-hart path.
- The previously packaged p3_opensbi_linux_2hart_csr701_gate_final.img is
  obsolete and must not be written. A fresh no-CSR final image and complete SD
  write/readback are required before the next board boot.

---

## 2026-07-13 -- Bound the CSR 0x701 finding to the evidence

- RTL identifies CSR `0x701` as Ariane's D-cache-enable bit. Writing zero
  immediately disables the private L1; the write-through cache controller then
  labels subsequent loads and stores non-cacheable, sending them through the
  L1.5/NoC path rather than serving or allocating them in L1.
- The two-image board control establishes the causal software boundary: the
  two-hart cold-boot path reaches no completion marker after that mode change,
  while the otherwise equivalent image that omits it reaches OpenSBI and Linux
  early console. It does **not** establish that the `csrw` instruction itself
  stalls, nor identify the exact downstream transaction or RTL state that
  loses forward progress.
- The remaining microarchitectural hypothesis is an unvalidated early-boot
  interaction in the D-cache-disabled/non-cacheable L1.5 adapter path. It
  needs targeted ILA/transaction evidence before being called a cache-drain,
  coherence, or response-path root cause. The 2-core gate remains the minimal
  evidence-based workaround; the greater-than-two-hart behavior is preserved.

---

## 2026-07-13 -- Packaged clean 2-core CSR-gated final SD candidate

- A clean OpenSBI package tree accepted the complete tracked P3 platform-fix
  patch and completed a fresh generic `FW_JUMP` build with
  `riscv64-linux-gnu-`. The resulting firmware uses the runtime CSR `0x701`
  gate: two-hart hardware omits the proven-bad write, while larger meshes keep
  it.
- The firmware was bundled with the already board-exercised two-hart Linux
  Image, DTB, and initramfs into
  `build/huaprop3/opensbi64/p3_opensbi_linux_2hart_csr701_gate_final.img`.
  The 268,435,456-byte image has SHA-256
  `7ecb6ccc611d8881b16c72bb1d20cccbaace904dd9a4fdb54ac69d32a047b579`.
- Local structure checks confirm a 256 MiB GPT image, partition 1 at LBA 2048,
  and a `P3OS`/`BI64` bundle with the expected firmware (`0x80000000`), Linux
  (`0x80200000`), DTB (`0x88000000`), and initramfs (`0x90000000`) addresses.
- This is a packaging-only result. The image has not yet been copied to or
  written onto the SD card, and no final board boot claim is made.

---

## 2026-07-13 -- Consolidated complete clean-package P3 OpenSBI fix set

The initial tracked CSR-only patch was insufficient as a clean-package
reproduction mechanism: comparison against the supplied OpenSBI archive found
nine P3-modified files outside that archive. The preparation flow now uses
`scripts/p3_opensbi_p3_platform_fixes.patch`, which carries the established
non-PIE, firmware-base, mtimer-alignment, PMP, trap-barrier, MSWI, and
OpenPiton 64-context fixes together with the proven CSR `0x701` hart-count
gate. It deliberately contains no raw UART probe instrumentation.

The former single-file CSR patch is superseded. The patch artifact retains
standard unified-diff whitespace and has a file-specific `.gitattributes`
whitespace exemption; source code emitted by it retains normal OpenSBI tabs.
The complete patch applied cleanly to an untouched package tree and a fresh
generic `FW_JUMP` build completed with `riscv64-linux-gnu-`.

This closes the clean-source reproducibility gap for the P3 OpenSBI fixes. A
new two-hart non-probe SD image and a board run remain required; the current
board evidence validates the diagnostic `nocsr701` image through OpenSBI and
Linux early console only, not a Linux shell.

---

## 2026-07-13 -- Implemented reproducible OpenSBI CSR 0x701 hart-count gate

The board control proved that CSR `0x701` blocks the two-hart OpenSBI
cold-boot path, while the prior 64-hart flow relies on that D-cache-bypass
workaround for coherent shared-memory experiments. The tracked preparation
flow now applies `scripts/p3_opensbi_csr701_hart_gate.patch` to a clean
OpenSBI package tree. In `sbi_hart_init()`, the write is executed only when
`sbi_hart_count() > 2`; `sbi_scratch_init()` sets that count before this call.
Thus one- and two-hart P3 images omit the proven-bad write, while larger meshes
retain the established workaround.

`scripts/p3_prepare_64core_opensbi_image.sh` now requires `patch`, applies the
marker-tagged patch idempotently, and fails explicitly if pointed at the old
temporary probe tree rather than silently packaging diagnostic edits. A
clean-package `patch --dry-run` succeeded, and an isolated clean OpenSBI
`FW_JUMP` build with `riscv64-linux-gnu-` completed successfully.

This is a source/build-flow fix, not yet a production board validation: the
currently demonstrated image still contains raw probe markers. A clean
two-hart final image must be packaged from the patched flow and pass a fresh
SD write/readback plus board boot before it replaces the diagnostic image.

---

## 2026-07-13 -- Board control proves CSR 0x701 blocks 2-core OpenSBI hart init

The Build 73 2-core board control completed successfully with the same PDI
(SHA-256 `52e1400a5c457020212c2d3329ee341163d776a66b9117344a5a96aa0add2c65`),
`DONE bit: HIGH`, debug hub `0x3ffc0000000`, and four enumerated ILAs. The SD
image was the independently write/readback-verified
`p3_opensbi_linux_2hart_nocsr701_probefw.img` (SHA-256
`b8717abaef07f221c2eea185690825e15786bbb49ae2a67e5e732832c16ae4fc`).

After bootrom copied all components and jumped to `0x80000000`, the raw UART
trace was exactly `II[SHDMWRTEPB`, followed by `OpenSBI v1.8`. OpenSBI reported
two harts, then Linux 6.6 reached its early console output. The earlier,
otherwise equivalent probe trace stopped permanently at `II[SHDMW`. Since the
control image's relevant change is removal of the `sbi_hart_init()` CSR `0x701`
write, this board result isolates that write as the blocker on the 2-core
cold-boot path; it is no longer merely a hypothesis.

The first 600-second UART capture ended during Linux early boot. A fresh
60-second continuation capture contained no new UART bytes, so this result
does **not** yet demonstrate SMP activation or an interactive Linux shell.
That later Linux-progress observation is a separate gate from the now-proven
OpenSBI CSR `0x701` failure.

Next: encode a minimal 2-core-specific omission/gate for CSR `0x701`, produce
a non-probe final image, and preserve the current marker evidence. Do not
re-enable the write for the 2-core final image solely because it is required by
some 64-core coherence experiments.

---

## 2026-07-13 -- Re-seated SD card passes nocsr701 probe-image write and readback

After the reader/card was physically re-seated, `/dev/sdc` again presented as
the expected 31,914,983,424-byte removable USB `Multi-Reader -1` disk and was
unmounted. A root direct-I/O raw-read preflight completed successfully. The
same 268,435,456-byte
`p3_opensbi_linux_2hart_nocsr701_probefw.img` was then written with direct I/O
and final `fsync` at approximately 23.2 MB/s.

An independent direct read of the first 64 x 4 MiB blocks produced SHA-256
`b8717abaef07f221c2eea185690825e15786bbb49ae2a67e5e732832c16ae4fc`, exactly
matching the image. The SD media is therefore valid for the next Build 73
board boot. The earlier write failure is cleared for this attempt, though the
available evidence does not distinguish a transient reader contact issue from
a card contact issue.

The card must now be moved from the reader to the FPGA slot and fully seated.
No OpenSBI/CSR conclusion is available until the Build 73 PDI is programmed
and the UART marker sequence is captured.

---

## 2026-07-13 -- 2-core nocsr701 probe write blocked by SD reader/medium I/O failure

The controlled CSR `0x701` comparison image was not booted. Before writing,
the remote target was revalidated as `/dev/sdc`: a 31,914,983,424-byte,
removable USB `Multi-Reader -1` disk with no mounted partitions. The local and
remote copies of `p3_opensbi_linux_2hart_nocsr701_probefw.img` both matched
SHA-256 `b8717abaef07f221c2eea185690825e15786bbb49ae2a67e5e732832c16ae4fc`
at 268,435,456 bytes.

The write command submitted all 256 MiB, but the required flush failed:
`dd: fsync failed for '/dev/sdc': Input/output error`. During the operation the
remote kernel reported repeated `Hardware Error` / `I/O error` write failures,
beginning at sector 70120, followed by lost asynchronous page writes. The card
was removed to release the stuck I/O; the kernel then reported `Medium not
present` and `/dev/sdc` capacity changed to zero. No readback hash was run or
passed.

Treat the card contents as invalid and do not boot them. This is a
reader/medium write-path failure, not evidence for or against the CSR `0x701`
hypothesis. The next attempt requires re-seating the card in the reader,
revalidating its nonzero capacity and identity, rewriting the image, and
passing the complete 256 MiB readback hash before returning it to the FPGA.

---

## 2026-07-13 -- Board probe localizes 2-core OpenSBI hang to sbi_hart_init before completion

Board evidence supersedes the earlier static-only conclusion that the dbg21
firmware lineage was sufficient to resolve the 2-core no-banner regression.
The Build 73 diagnostic PDI was programmed successfully with `DONE bit: HIGH`,
debug hub `0x3ffc0000000`, and four live ILAs. The probe SD image
`p3_opensbi_linux_2hart_probefw.img` (SHA-256
`b9bfcedb9b16095658e29efd4f570f26df379977ad6fc322e3af431dfc608c52`) was
written to the confirmed removable card and passed a 256 MiB readback hash
check before boot.

UART showed a complete bootrom path: GPT/bundle reads, OpenSBI/Linux/DTB/initrd
copies, `B69 releasing harts and jumping OpenSBI`, and the jump to
`0x80000000` with DTB at `0x88000000`. The raw OpenSBI trace then stopped at:

```
II[SHDMW
```

It remained unchanged for an additional 30-second observation window. `W` is
emitted immediately after `wake_coldboot_harts()` and `R` is emitted only after
`sbi_hart_init(scratch, true)` returns successfully. The failure is therefore
inside `sbi_hart_init()` or its error-to-`sbi_hart_hang()` path, before its
completion marker. This does **not** yet prove that CSR `0x701` is the cause:
the function first clears `mip`, then the probe firmware writes CSR `0x701`,
then performs feature/PMP/trap initialization.

Next controlled board test: boot
`p3_opensbi_linux_2hart_nocsr701_probefw.img` (SHA-256 `b8717aba...`). It is the
probe variant with the CSR `0x701` write removed. A subsequent `R` marker would
isolate that write; another stop after `W` requires instrumenting or auditing
the preceding `mip` and subsequent `sbi_hart_init()` substeps. No RTL or
OpenSBI source change was made by this board run.

---

## 2026-07-13 -- FIX APPLIED: 2-core final image now uses dbg21 fw (verified banner-era) instead of unverified dbg28b_noprobe

Static resolution of the 2-core OpenSBI no-banner regression, without needing a
fresh source fix:

Diagnosis (fully static):
- DTB structure diff vs the working 64-core DTB: IDENTICAL (same nodes
  clint/plic/uart/sdhci/memory, same compatible, same #cells; only cpu@N count
  differs). So the DTB is not the cause.
- The current fw in the 2-core image was `fw_jump_p3_64core.bin` = byte-identical
  to `dbg28b_noprobe` (sha `12b4ff22`), built 7/6 during the dbg28 mswi-probe
  experiments and NEVER hardware-verified to print the OpenSBI banner (the session
  ended and the SD card went unseated before any verification).
- dbg21 fw (sha `ab543745`, built 6/26) is from the verified-banner era: 64-core
  logs show the OpenSBI v banner reliably from dbg9 (6/22) through ev_retry2 (7/3),
  and dbg21 is within that range.
- OpenSBI cold boot runs on hart 0 (single-hart coldboot path); the 2-core and
  64-core hart-0 code paths are identical. Therefore a fw that prints the banner
  on 64-core should print it on 2-core too, absent a 2-core-hardware-specific
  issue.

Fix applied: rebuilt the 2-core final image with dbg21 fw:
- `p3_opensbi_linux_2hart_final.img` now sha `910f390e` (was `8cb7c3ba` with the
  broken dbg28b_noprobe fw). fw entry bytes = dbg21's (33 04 05 00 ...).
- Old broken image backed up as `p3_opensbi_linux_2hart_final.dbg28b_broken.img`.
- Components unchanged except fw: Image_p3_2hart + p3_opensbi_2hart_initrd.dtb
  (initrd-end=0x90107d9c) + p3_rootfs_64hart_xsbench_v2.cpio.gz.

Why this is the right static fix: the regression was introduced by switching the
2-core image to the unverified dbg28b_noprobe fw lineage; reverting the fw to the
verified-banner-era dbg21 removes the regression. dbg21 lacks CSR 0x701 (L1
D-cache disable), but that is only needed for 64-core XSBench coherency, not for
2-core bring-up to a shell.

Verification status: fix applied; board verification pending a physical SD card
swap (card is in the FPGA; must move to reader to write, then back to FPGA to
boot). Probe/test images retained for the case dbg21 also fails on 2-core:
- `p3_opensbi_linux_2hart_probefw.img` (b9bfcedb) — current source + probes
- `p3_opensbi_linux_2hart_nocsr701_probefw.img` (b8717aba) — probes + CSR0x701 reverted

If dbg21 fw prints the banner on 2-core → regression confirmed as the fw lineage,
fix verified. If dbg21 also prints no banner → a genuine 2-core-hardware OpenSBI
cold-boot issue; the probe image then localizes which init step hangs.

---

## 2026-07-13 -- Regression NARROWED to sbi_hart.c (CSR 0x701) or sbi_trap.c via source mtime

Source-file modification times pin the no-banner regression to a tiny window.
Between the verified-banner era (dbg21 fw, 2026-06-26 15:05) and the broken fw
(dbg28b_noprobe = current source, 12b4ff22), the ONLY OpenSBI source files
changed are:
- `lib/sbi/sbi_trap.c`  (2026-06-27 16:26) — the non-IRQ UART-LSR coherency barrier (Gate 9)
- `lib/sbi/sbi_hart.c`  (2026-06-27 16:26) — CSR 0x701 L1 D-cache disable in sbi_hart_init (Gate 8)

The dbg28-era edits to `aclint_mswi.c` and `openpiton.c` (2026-07-08 20:02-20:04)
were NET-NO-OPS: a clean rebuild from the current (post-revert) source reproduces
the 7/6 dbg28b_noprobe binary byte-identical (12b4ff22), proving the probe
add+revert left no residue. So those files are NOT the regression.

Therefore the regression that breaks 2-core OpenSBI cold-boot banner is one of:
1. **CSR 0x701 L1 D-cache disable (sbi_hart.c)** — changes EVERY load/store to
   bypass L1 and hit L1.5/L2/NoC directly. Most likely 2-core-specific culprit
   because it fundamentally alters the memory-access path; a 2-tile fabric may
   not handle the L1-disabled access pattern the way an 8x8 mesh does.
2. The sbi_trap.c coherency barrier refinement — only affects trap handling,
   less likely to gate the banner (banner path has no trap unless something faults).

The dbg21 fw image (`p3_opensbi_linux_2hart_dbg21fw.img`, sha 517d9b87) predates
BOTH changes and is the direct test: if dbg21 fw prints the banner on 2-core, the
regression is confirmed to be CSR 0x701 and/or the barrier refinement, and the
fix is to gate/revert CSR 0x701 for the 2-core build (CSR 0x701 was only needed
for 64-core XSBench coherency per Gate 8).

The probe image (`p3_opensbi_linux_2hart_probefw.img`, sha b9bfcedb) localizes
which init step hangs if dbg21 also fails (would then point to a 2-core-hw issue
in CLINT/UART/console MMIO rather than a source regression).

Both images built and ready. Awaiting SD card swap (card is in the FPGA; must
move to reader to write either image, then back to FPGA to boot).

---

## 2026-07-13 -- Added OpenSBI cold-boot raw-UART probes to localize the no-banner hang

Static audit found all 64-core OpenSBI fixes present in 2-core source, and the
PLIC/CLINT init loops are DTB-driven (`sbi_for_each_hartindex`), so the hardcoded
`hart_count=64` is safe on 2-core (does not touch non-existent contexts). So the
no-banner hang is NOT a missing known fix and NOT a hart-count overflow.

To localize it definitively, added B69-style raw-UART probes to OpenSBI
`init_coldboot` (lib/sbi/sbi_init.c). A `p3_probe_putc(char)` helper writes
directly to AXI16550 THR (0xfff0c2c000) with LSR (0xfff0c2c005) THRE poll,
bypassing console init — same path the bootrom proved working. Probes inserted:
- `I` at sbi_init() entry (confirms OpenSBI C code starts at all)
- `[` at init_coldboot() entry
- `S H D` after scratch/heap/domain init
- `M` after hsm_init, `W` after wake_coldboot_harts
- `R` after sbi_hart_init (incl. CSR 0x701 L1 disable)
- `T` after sbi_timer_init
- `E` after sbi_platform_early_init (= generic_early_init = console/fdt init)
- `P B` after pmu/dbtr init, then sbi_boot_print_banner (the missing banner)

Reading the probe trace pinpoints the hang to exactly one init step, e.g.:
- no `I` → OpenSBI _start/assembly never reaches C (lottery or reloc hang)
- `I` but no `[` → coldboot lottery hang
- `[S` then stop → sbi_heap_init
- `[SHDMWR` then stop before `T` → sbi_timer_init (CLINT mtimer access)
- up to `R` then stop at `T`/`E` → timer/console-init region

Artifacts (in build/huaprop3/opensbi64/, untracked — extracted source tree):
- probe fw `fw_jump_p3_64core_probe.bin` (sha `fd024769`)
- probe image `p3_opensbi_linux_2hart_probefw.img` (sha `b9bfcedb`)
- baseline dbg21 image `p3_opensbi_linux_2hart_dbg21fw.img` (sha `517d9b87`)

Next: write probe image to SD (card must move FPGA→reader), program the unchanged
Build 73 diag PDI, capture UART, read the probe marker sequence to localize.

---

## 2026-07-13 -- Static audit of 64-core OpenSBI gates vs 2-core: ALL fixes present; fw = unverified dbg28b_noprobe

Audited every 64-core OpenSBI cold-boot problem (extracted via subagent from
2026-05/06/07 devlogs) against the current 2-core OpenSBI source tree
(`build/p3_64core/riscv64-linux-64core-src-20260610/opensbi`). Result: **every
known fix is present in the source.** The 2-core fw binary is NOT a stale build —
a clean `make clean && make` from current source reproduces it byte-identical.

Gate-by-gate (all in source):
- Gate 1 (PIE relocation, dbg3): Makefile `-fno-pie`/`-no-pie` + fw_base linker
  `0x80000000` — PRESENT.
- Gate 2 (mhpmevent4 illegal instr): `sbi_hart_mhpm_mask()` returns 0 — PRESENT.
- Gate 3 (3-hart CLINT/PLIC → timer ENODEV, dbg6/7): `OPENPITON_DEFAULT_HART_COUNT=64`
  in openpiton.c — PRESENT (and 2-core bypasses this gate anyway since coldboot
  is hart 0).
- Gate 4 (plic context_map stack overflow, dbg7): `openpiton_early_init` calls
  `fdt_parse_plic(fdt, &plic, ...)` into the STATIC `plic` (no stack-local copy) —
  PRESENT.
- Gate 5/6 (PMP exhaustion + layout race, dbg8/12): `sbi_hart_oldpmp_configure` —
  PRESENT.
- Gate 7 (DTB initrd-end truncation, dbg26): 2-core DTB has
  `linux,initrd-end = 0x90107d9c` matching v2 rootfs size — PRESENT.
- Gate 8 (CSR 0x701 L1 D-cache disable, dbg23): `sbi_hart.c:783 csrw 0x701,zero`
  — PRESENT (confirmed in disasm at 0x8000332e).
- Gate 9 (coherency barrier, dbg21): `sbi_trap.c` non-IRQ UART LSR read — PRESENT.
- dbg28 probe remnants (aclint_mswi.c / openpiton.c / extra includes): NONE —
  source is clean.

The decisive finding: the fw in the 2-core image (`fw_jump_p3_64core.bin`,
sha `12b4ff22`) is byte-identical to `fw_jump_p3_64core_dbg28b_noprobe.bin`
(7/6). A clean rebuild from current source reproduces `12b4ff22` exactly. So the
current source genuinely compiles to the dbg28b_noprobe binary.

Hypothesis (most likely): dbg28b_noprobe was NEVER hardware-verified to print the
OpenSBI banner. The dbg28 series (7/6) added a mswi probe that broke the banner;
the "revert" (dbg28b_noprobe) was assumed to restore it but the session ended and
the SD card went unseated before verification. So the current source may carry an
incomplete revert that breaks cold-boot banner on BOTH 2-core and 64-core.

The OpenSBI banner was reliably observed on 64-core from dbg9 (6/22) through
ev_retry2.log (7/3). dbg21 fw (`ab543745`, 6/26) is from that verified-banner era
and is a DIFFERENT binary from current (`12b4ff22`).

Decisive test prepared (zero code change): repacked a 2-core image with dbg21 fw
(`p3_opensbi_linux_2hart_dbg21fw.img`, sha `517d9b87`, fw entry bytes 33 04 05 00
confirmed). If dbg21 fw prints the OpenSBI banner on 2-core → current source has
a post-dbg21 regression (narrow to the incomplete dbg28 revert). If dbg21 also
prints no banner → 2-core-hardware-specific OpenSBI cold-boot issue (then add
early raw-UART probes between init_coldboot steps, B69-style).

This audit satisfies the "statically check all 64-core OpenSBI problems against
2-core" goal: all gates checked, all present, no missing fix. The remaining issue
is a fw-build-lineage question resolved by the dbg21 board test.

---

## 2026-07-13 -- 2-core OpenSBI cold-boot hangs (no banner) after bootrom jump — new blocker layer

After confirming the SD card was the 07-09 issue (re-seated, bootrom now fully
succeeds), the 2-core boot advances to the NEXT layer and reveals a NEW blocker:
**OpenSBI cold boot prints no banner** after the bootrom jumps to it.

Reproduced twice, both with fresh captures that stayed alive (ruling out the
earlier capture-timeout artifact):
- Build 73 diag bootrom runs to completion: `B69 DDR OK` → `B69 GPT sig=` → copies
  fw(528)/image(38831)/dtb/initrd → `B69 releasing harts and jumping OpenSBI` →
  `B69 jump fw=0000000080000000` `B69 jump dtb=0000000088000000`.
- After that jump: UART goes STATIC. 80s growth-sampling delta=0 at 35721 bytes.
  No `OpenSBI v1.x` banner, no Platform Name, nothing.
- Capture process confirmed alive during the static period.

So the bootrom handoff is correct (`a0=mhartid, a1=fdt@0x88000000, jr 0x80000000`,
identical to the 64-core Build 68/69 path), and OpenSBI's fw_jump entry bytes
match the known-good dbg21 fw. Yet OpenSBI does not reach its first `sbi_printf`
("OpenSBI v..."). That puts the hang in the OpenSBI cold-boot sequence BEFORE
console init (`init_coldboot`: scratch_init → heap_init → domain_init → hsm_init
→ hart_init → timer_init → platform_early_init[=generic_early_init→fdt_serial_init
= console]). Any of these hangs → no banner.

Difference vs 64-core (where OpenSBI boots): the OpenSBI was compiled with
`OPENPITON_DEFAULT_HART_COUNT=64` (hardcoded in openpiton.c), but this DTB
describes only 2 harts. HSM/timer/mswi structs are sized for 64. Cold-boot init
iterates per the DTB hart count, so 64-vs-2 shouldn't hang cold init by itself,
but the static `mswi.hart_count=64` / `mtimer.hart_count=64` and the 64-entry
PLIC context_map are present in the binary.

Likely cold-boot hang candidates to investigate next:
1. `sbi_hsm_init` / secondary-hart release path at 2 harts.
2. `aclint_mtimer_cold_init` / `aclint_mswi_cold_init` with hart_count=64 on a
   2-tile fabric (does it touch a CLINT register for a non-existent hart and
   stall? earlier analysis said cold_init only sets scratch pointers, but the
   memrange add and the 64-context PLIC init touch more state).
3. fdt parsing / console (uart8250) init with the 2-hart DTB.

Next step: add an OpenSBI early raw-UART probe (print a single marker before each
init_coldboot step, directly writing THR — bypassing console init) to localize
the hang to one of scratch/heap/domain/hsm/hart/timer/early_init. This mirrors
how B69 localized the SD hang.

---

## 2026-07-13 -- CONFIRMED: 2-core "SD read hang" was an UNSEATED SD CARD — not a 2x1 RTL bug

User re-seated the SD card firmly in the P3 FPGA slot, then programmed the same
Build 73 diag PDI (sha `52e1400a`). The B69 diag bootrom now SAILS THROUGH the
point that hung on 07-09:

```
B69 DDR OK
B69 SD read GPT header
copying block 0 of 1 blocks (0 %)
B69 GPT sig=5452415020494645          <-- was the hard stop on 07-09; now passes
B69 boot partition lba=0000000000000800
B69 magic=34364942534F3350            (P3OS/BI64)
B69 entry=0000000080000000  components=00000004
B69 copy fw ... blocks=528 dst=0x80000000  -> copied (OpenSBI fw_jump)
B69 copy image ... blocks=38831 dst=0x80200000  -> copying Linux Image (~19MB)
```

This CONFIRMS the 07-09 hypothesis: the 2-core SD-read "hang" (`copying block 0
of 1` permanent silence) was the SD card not being properly seated in the FPGA
slot, NOT a 2x1 RTL defect. With the card firmly clicked in:
- SD MMIO reads return data normally (GPT sig, bundle header, all payloads).
- The SPI-SD controller, its NoC path, io_xbar, and response routing are all fine
  at 2x1. No WMT/L1.5 race, no SD-controller bug, no tile-count dependence.
- The 07-09 single-core bootrom hung because with no card responding on MISO,
  the first real CMD17 read never completes and the core's load stalls forever
  (no NoC timeout). `B69 SD init OK` was misleading because bootrom `init_sd()`
  only prints + return 0 without touching hardware.

Implication for the project: the entire 2-core apparatus (Build 72 + Build 73)
is VALID. The earlier 07-09 "DDR proven GOOD but SD dead at 2x1" conclusion is
correct on the DDR point but WRONG on the SD point — SD is fine. Bring-up can now
proceed past bootrom into OpenSBI / Linux on 2 cores.

Lesson (added for future runs): before debugging SD-read RTL on any board, ALWAYS
first re-seat the SD card and confirm a clean bootrom read trace. A loose
push-push socket produces exactly the "prints banner, hangs at first SD block"
signature that looks like a hardware/RTL fault.

Bootrom is now copying the Linux Image payload to DDR; next expected markers are
`B69 releasing harts and jumping OpenSBI`, then the OpenSBI banner, then Linux
SMP bring-up for 2 harts. Capturing from `~/p3_uart_logs/b73_reseat_20260713_105012.log`.

---

## 2026-07-13 -- Rewrote 2-core SD image fresh; about to test the "unseated card" hypothesis

Picked up the 2-core bring-up again after 4 days (last work 2026-07-09). The
unseated-SD-card hypothesis from 07-09 was never tested because the session
ended. Resuming by eliminating that variable first.

Fresh SD write (remote card reader `/dev/sdc`, Kingston Multi-Reader -1, 29.7 GB,
RM=1 — confirmed the card was IN THE READER today at 10:24, not in the FPGA):
- Re-transferred `build/huaprop3/opensbi64/p3_opensbi_linux_2hart_final.img`
  (256 MiB) to `illya@100.93.77.36:/tmp/` (scp ~430 KB/s, ~10 min; first attempt
  was killed early by too-short pexpect timeout — used 1800s the second time).
- Remote file verified: 268435456 bytes, sha
  `8cb7c3baf23ca0446dce18369333109d351608657e316909834f6b4679d332ad`.
- `dd if=image of=/dev/sdc bs=4M conv=fsync` (64 blocks, 22.7 MB/s).
- Readback of written span (count=64) sha = `8cb7c3ba` — **matches image**.
- Re-verified card content later: still `8cb7c3ba`. Card image is good.

Next (waiting on user): physically move the card from the reader back into the P3
FPGA SD slot, push-push fully clicked in (the suspected 07-09 fault). Then program
the already-built Build 73 diag PDI (sha `52e1400a`) with UART capture and read
the B69 diag trace:
- If it advances past `copying block 0 of 1` to `B69 GPT sig=5452415020494645` →
  the 07-09 "2-core SD-read hang" was an unseated card, NOT a 2x1 RTL bug.
- If it still hangs at `copying block 0 of 1` with a confirmed-seated card → a
  Build 74 with SD-AXI ILA probes is warranted.

Build 73 artifacts unchanged (no rebuild): PDI
`huaprop3_build73_2x1_opensbi_diag/debug_build/p3_top_build73_2x1_opensbi_diag.pdi`
+ matching `.ltx`.

---

## 2026-07-09 -- KEY HYPOTHESIS: the "SD read hang" may simply be the SD card not seated in the FPGA slot

User raised the simplest possible cause of the isolated SD-read hang: **is the SD
card actually inserted / seated in the FPGA slot?** This is highly plausible and
was NOT yet excluded. Reassessing the evidence, it fits perfectly:

Timeline of the physical card:
- 2026-06-26..28: 64-core dbg21/dbg26 boots READ the SD fine (card seated in the
  P3 FPGA SD slot, same board, same slot, same SPI-SD RTL).
- Between 06-28 and 07-09: the card was removed, written with the 2-core image
  (`p3_opensbi_linux_2hart_final.img`) in a card reader, then "put back into the
  FPGA".
- 2026-07-09: 2-core boots hang at the first SD data read.

Why a missing/poorly-seated card produces EXACTLY this signature:
- `B69 DDR OK` — DDR needs no SD card. Works.
- UART prints fully — needs no SD card. Works.
- `B69 SD init OK` — MISLEADING: bootrom `init_sd()` (`sd.c:12-16`) only prints
  and `return 0`; it does NOT touch hardware or check card presence. So it prints
  OK whether or not a card is present.
- `copying block 0 of 1 blocks (0 %)` then permanent silence — this is the FIRST
  real hardware SD read (CMD17 via the SPI-SD controller for GPT LBA 1). With no
  card responding on MISO, the SPI-SD controller's command/response state machine
  waits forever, the AXI read never returns `rvalid`, the core's load never
  completes, and there is no timeout in the NoC/cache path → permanent hang.

Crucially this does NOT contradict "64-core reads SD fine": the 64-core runs used
the SAME board and SAME slot with the card seated. The only thing that changed for
the 2-core runs is the card's physical insertion state after it was pulled to
write the image. So the SD-read-path RTL is not necessarily tile-count-dependent
at all — the far more likely variable is a poorly-seated / not-clicked-in card.

This also revises the earlier "root cause isolated to SD-data-read path at 2x1"
conclusion: the SD-data-read path IS where it hangs, but the cause may be physical
(card not seated), not a 2x1 RTL bug. Static RTL analysis had already found no
tile-count dependence in the SD response routing (noc_axilite_bridge:436-439),
clock, reset, or base address — consistent with "not an RTL bug".

Next (zero-cost, do FIRST before any rebuild): re-seat the SD card firmly in the
FPGA slot (push-push socket must click fully in), reset/re-trigger the board with
the already-programmed Build 73 diag PDI, and re-capture UART. If it then advances
past `copying block 0 of 1` to `B69 GPT sig=5452415020494645`, the "hang" was a
loose card, and the real 2-core bring-up (OpenSBI/Linux) can proceed. Only if it
STILL hangs with a confirmed-seated card is a 2x1 SD-path RTL/ILA investigation
(Build 74 with SD-AXI probes) warranted.

---

## 2026-07-09 -- ROOT CAUSE ISOLATED: 2-core hang is SD-DATA-READ path; DDR proven GOOD (Build 73 diag)

Build 73 (2x1 + B69 diagnostic bootrom) programmed successfully (DONE bit HIGH,
debug hub 0x3ffc0000000, ILAs enumerated) and produced a DECISIVE 204-byte diag
trace (`~/p3_uart_logs/b73_diag3_20260709_212217.log`):

```
B69 ASM                              <- no-stack ASM startup OK
B69 OpenSBI bundle diag bootrom      <- C runtime OK
B69 DDR probe addr=0000000084001000  <- DDR probe begins
B69 DDR OK                           <- DDR MMIO read+write WORKS (write pattern, read back, verify @0x84001000)
B69 SD init start
initializing SD...
B69 SD init OK                       <- SD controller init returns OK
B69 SD read GPT header               <- first real SD data read begins
copying block 0 of 1 blocks (0 %)    <- HANGS HERE, permanent silence
```

This ISOLATES the root cause layer definitively:
- UART:  GOOD (entire trace printed; write_serial LSR-poll reads all succeed)
- DDR:   GOOD — `B69 DDR OK` proves MMIO read AND write to 0x84001000 complete
         and verify. So the generic NoC/io_xbar/MMIO **read-response** path is
         fine at 2x1. This kills the "broader MMIO/NoC broken at 2x1" hypothesis.
- SD init: GOOD — `B69 SD init OK` (though init_sd() just prints+returns 0, so
         this only means code reached it).
- SD DATA READ: **THE BLOCKER**. The first `sd_copy()` — reading the GPT header
         at LBA 1 via load from `PITON_SD_BASE_ADDR 0xf000000000 + (1<<9)` —
         never returns. The load to the SD MMIO window stalls the core forever
         (no timeout in the NoC/cache path).

This EXACTLY matches the Build 72 (normal bootrom) hang at `copying block 0 of 1`.
Two independent builds (normal + diag), same failure point. Fully reproducible.

What this means for the investigation:
- The WMT / L1.5-invalidation hypothesis is NOT what's blocking 2-core bring-up.
  This hang is single-core (hart 0 only), pre-OpenSBI, pre-SMP. No second core,
  no stop_machine, no coherence traffic involved.
- The blocker is the **SD read datapath specifically**: reads from the
  `0xf000000000` SD MMIO window hang, while reads from DDR (`0x8xxxxxxx`) and
  UART (`0xfff0c2c000`) succeed. Same io_xbar (9 ports incl. SD), same NoC.
- Since DDR reads work through the identical NoC→io_xbar→chipset fabric, the
  fault is downstream of the crossbar: the SD controller's response path
  (noc_sd_bridge / piton_sd_* transaction manager) does not return read data at
  2x1 — or the SD controller is wedged after `init_sd` (which does nothing) when
  the real first block read is issued.

Next: capture the Build 73 ILAs (SD/NoC probes) at the hang, and trace the
noc_sd_bridge read path. The B69 8x8 (64-core) diag boot passes THROUGH this same
`copying block 0 of 1` line and continues to `B69 GPT sig=5452415020494645`, so
the SD read demonstrably works at 8x8 — the difference at 2x1 must be in how the
SD controller / its NoC endpoint is addressed or clocked when only 2 tiles exist.

Artifacts: PDI sha `52e1400a5c457020212c2d3329ee341163d776a66b9117344a5a96aa0add2c65`.
Programming note: XVC upload of the 15.5 MB PDI to VP1902 needed a 900s timeout
(prior 400/500s attempts were killed mid-`program_hw_devices`); the link is slow
but functional — not a build problem.

---

## 2026-07-09 -- Build 73 PDI recovery SUCCEEDED; programming for DDR/SD isolation

Recovered the Build 73 PDI from `p3_top_physopt.dcp` without rerunning
synth+impl. Minimal Tcl (`/tmp/recover_build73_pdi.tcl`): open physopt →
`route_design` → `write_checkpoint` (routed) → `write_device_image` →
`write_debug_probes`.

Result — full success this time (the prior run crashed at report_power/PDI):
- `route_design completed successfully`, 0 Errors, 0 Critical Warnings.
- Timing MET again: "All user specified timing constraints are met.", slack
  16.045 ns.
- `write_device_image completed successfully` (bootgen -arch versal, elapsed
  12 min, peak 18 GB) — this is exactly the step that vanished last time.
- Artifacts published:
  - PDI `huaprop3_build73_2x1_opensbi_diag/debug_build/p3_top_build73_2x1_opensbi_diag.pdi`
    (15.5 MB, sha `52e1400a5c457020212c2d3329ee341163d776a66b9117344a5a96aa0add2c65`)
  - LTX `...p3_top_build73_2x1_opensbi_diag.ltx` (63 KB)

So the earlier crash was a one-time WSL↔Windows Vivado hiccup at PDI write, not a
reproducible design/env failure. Recovery total ~30 min vs a full ~4 h rebuild.

Now programming this diag PDI to the P3 board with UART capture. The B69
diagnostic bootrom will print, in order: `B69 DDR probe addr=0x84001000` →
`B69 DDR OK` → `B69 SD init OK` → `B69 SD read GPT header` →
`B69 GPT sig=5452415020494645` → bundle copies → `B69 releasing harts`. Where the
2-core boot stops will isolate whether the earlier Build 72 hang (at the first SD
read, `copying block 0 of 1`) is a DDR-path, SD-path, or broader-MMIO problem.

---

## 2026-07-09 -- Build 73 impl SUCCEEDED (timing met) but crashed at write_device_image; resuming PDI-only

Build 73 (2x1 diag) ran ~3.5 h locally. The IMPLEMENTATION fully succeeded, but
Vivado crashed silently at the final PDI-generation step, so no `.pdi` was
produced. This is an ENVIRONMENT failure, not a design failure.

Evidence that implementation is 100% good:
- `route_design completed successfully` (elapsed 6.4 min), 0 Errors.
- `route_report_timing_summary_0.rpt`: **"All user specified timing constraints
  are met."** — WNS/TNS/WHS/THS all pass (setup AND hold clean).
- `route_report_drc_0.rpt`: no unrouted nets, no critical DRC.
- `route_report_route_status_0.rpt` (651 B, 20:08): route 100% complete.
- Checkpoints present: `p3_top_opt.dcp` (19:46), `p3_top_placed.dcp` (19:58),
  `p3_top_physopt.dcp` (20:00).

Where it died:
- `runme.log` ends abruptly at `Command: report_power ...` with NO completion
  line, NO crash dump, NO exception text — the Windows-side Vivado process
  vanished after the route reports were written but during/after report_power,
  before `write_device_image`.
- No `p3_top_routed.dcp` was saved (the post-route checkpoint write did not
  finish), and no `.pdi` exists.
- NOT an OOM: WSL has 94 GB, Vivado peak was only 16.4 GB, 87 GB free at crash.
- The 4 CRITICAL WARNINGs (`sys_clk` clock override; `if`/`unset` unsupported in
  XDC at constraints.xdc:277,280) are pre-existing and present in Build 72's
  successful run too — not the cause.
- Likely a WSL↔Windows Vivado file-I/O / process hiccup during the large-device
  report_power/PDI write on VP1902. Transient.

Recovery decision: do NOT rerun synth+impl from scratch (~4 h wasted). The
framework `p3_build52_sd_cd_mask.tcl:687` does `reset_run impl_1` then
`launch_runs -to_step write_device_image`, and supports
`-reuse_synth -skip_create -skip_prepare` to reuse the 1 h synthesis and rerun
only implementation (opt→place→route→PDI, ~1.5 h). Relaunching that way to
regenerate the routed checkpoint and PDI.

---

## 2026-07-09 -- Build 73 = 2x1 + B69 diag bootrom, to isolate DDR vs SD hang

Chose the DDR/SD-separation diagnostic path (user option 3) after a key
build-free finding narrowed the 2-core hang to the SD read specifically.

Build-free evidence gathered before committing to a 5.5 h build:

1. **UART read path is PROVEN GOOD** (not just the write path). `bootrom/linux/src/uart.c`
   `write_serial()` polls the 16550 LSR (`read_reg_u8(UART_LINE_STATUS) & 0x20`)
   before EVERY character (`sd.c` prints go through this). The 82-byte trace =
   82 successful LSR reads at `0xfff0c2c000+5`. So MMIO **reads** through
   io_xbar→UART return fine; the NoC read-response mechanism works. The hang is
   NOT a generic broken-read-path problem — it is SD-specific.

2. **Known-good 64-core boot passes through the identical string.** The dbg21/B69
   log shows `copying block 0 of 1 blocks (0 %)` too — it's just `sd_copy`'s first
   progress print. The 64-core boot immediately continues with
   `B69 GPT sig=5452415020494645` (sd_copy returned, GPT read OK) and proceeds
   through `copying block ... of 528`, `... of 38836`, etc. The 2-core boot emits
   the same progress line and then NOTHING — its first `sd_copy` never returns.

3. Image, io_xbar routing (9 ports incl. SD), and bootrom compile flags are all
   verified identical/valid — so the difference is in the SD hardware path at 2x1.

Why Verilator can't answer this: the manycore TB uses `fake_mem_ctrl` and does
not model the real SD controller / chipset CDC. Only a board diagnostic bootrom
can separate DDR MMIO from SD MMIO on real hardware.

Build 73 apparatus (committed `188b8a3`):
- `scripts/p3_build73_2x1_opensbi_diag.tcl` — 2x1 tiles, p3_build52 framework,
  `EXTRA_DEFINES=P3_SPI_SD_BOOT P3_SPI_SD_BLOCK_DEBUG`.
- `scripts/p3_rebuild_build73_2x1_opensbi_diag.sh` — `BOOTROM_MODE=opensbi_bundle_diag`.
- Derived from Build 69 8x8 diag; only tile counts changed (8→2,8→1,64→2).
- Bootrom rebuild tested standalone: `MAX_HARTS=2`, `bootrom_linux.sv` (585 lines)
  generated, diag strings confirmed embedded (`B69 DDR probe addr=`, `B69 DDR OK`,
  `B69 SD init start/OK`, GPT/bundle markers).

The 2x1 PyHP tmp set from Build 72 (01:21) is complete and reused (no zero-byte
tmp, io_xbar 9 ports, tile tmp all 2x1) — Build 73 changes only the bootrom, not
the hardware RTL, so a fresh tmp regen isn't needed.

Expected outcome that will decide the direction:
- `B69 DDR probe → B69 DDR OK` then hang at `B69 SD read GPT header` →
  DDR works, SD read is the blocker (SD controller / sd_sys_clk CDC / reset).
- Hang at/before `B69 DDR probe` → broader MMIO/NoC/chipset-reset issue at 2x1
  (would be surprising given UART reads work).
- Full trace to `B69 releasing harts` → SD works; the earlier 82-byte hang was a
  transient (SD power-up timing / half-reset boot), and the real blocker is later.

Building locally (WSL+Windows Vivado 2024.2.2, -jobs 8), same as Build 72.

---

## 2026-07-09 -- 2-core (Build 72) HANGS in bootrom SD read, BEFORE OpenSBI — peripheral-layer blocker, not WMT

Board-tested the 2x1 Build 72 PDI on P3/VP1902. `DONE bit: HIGH`, 4 ILAs, debug
hub `0x3ffc0000000`. Fresh UART capture during a real board boot (log
`2core_boot_20260709_160352.log`, 82 bytes, full hexdump verified):

```
B68 OpenSBI bundle bootrom
initializing SD...
copying block 0 of 1 blocks (0 %)
```

...and then permanent silence. This is a DECISIVE and SURPRISING result: the
2-core system hangs at the **earliest SD MMIO read**, far below OpenSBI, Linux,
or any multicore coherence.

Exact hang point (traced through source, not guessed):
- The banner `B68 OpenSBI bundle bootrom` comes from
  `bootrom/linux/src/main_opensbi_bundle.c:140`.
- `initializing SD...` comes from `sd.c:14` (`init_sd()` — just prints, returns 0).
- `copying block 0 of 1 blocks (0 %)` comes from the progress print **inside**
  `sd_copy()` (`sd.c:31-37`), NOT from `main`'s `B68 copy fw ...` line.
- The FIRST `sd_copy` call is `find_first_partition_lba()`'s
  `sd_copy(lba1_buf, 1, 1)` (`main_opensbi_bundle.c:90`) — read GPT header at
  LBA 1, size=1 block. `of 1` confirms `size==1`, i.e. this exact call.
- The hang is in the MMIO copy loop `sd.c:39-41`:
  `for(offset<64) *(p++) = *(addr++);` where
  `addr = PITON_SD_BASE_ADDR(0xf000000000) + (src_lba<<9)`. A read to the SD
  MMIO window never returns → the load instruction stalls forever → no timeout
  in the NoC/cache path → permanent silence.

Why this matters / what it rules OUT:
- The old `B68 jump fw=...0080000000 dtb=...088000000` tail seen while validating
  the image was **residual 64-core log** in the UART buffer, NOT 2-core output.
  The fresh 2-core boot never gets past the first GPT sector read.
- This is single-core bootrom code (hart 0 only). The WMT / L1.5-invalidation
  hypothesis cannot apply here — there is no second core active yet, no
  stop_machine, no XSBench. So the 2-core apparatus has surfaced a DIFFERENT,
  more fundamental blocker first: the SD-read MMIO path is dead in the 2x1 build.
- Image itself is structurally VALID (verified independently): GPT at LBA1,
  P3OS/BI64 bundle at sector 2048, header `component_count=4`, OpenSBI
  (load 0x80000000, entry bytes identical to known-good dbg21 fw), Linux Image
  (MZ), DTB (d00dfeed, correct 2-hart UART/CLINT/PLIC/mem addrs), initrd (gzip).
  So the image is not the cause; the read path is.
- io_xbar in the Build 72 self-contained snapshot has 9 ports (dataOut_0..8),
  matching P3's 9 devices incl. `sd` at `0xf000000000` — so the SD route EXISTS
  at the crossbar RTL level. The failure is deeper: SD controller response,
  SD NoC path, or an SD-read address/handshake that differs at 2x1.

Peripheral-layer debug plan (per user request: uart→ddr→sd, then image, then
add debug info):
1. UART: PROVEN GOOD — banner + two progress lines printed cleanly.
2. DDR: not yet exercised (bootrom copies to DDR only AFTER the SD read that
   hangs). Need a separate DDR probe.
3. SD: THIS IS THE BLOCKER. Next step is to isolate whether the SD MMIO read
   returns/hangs — compare 2x1 vs the known-good 64-core SD path, and check the
   SD controller instantiation + reset/clock in the 2x1 chipset.

Note: after re-programming the PDI (16:09, DONE HIGH) the board went fully
silent (0-byte capture) — Versal PDI reload did not re-trigger the bootrom on
this board without a CPU/POR reset. The 82-byte trace above is from the boot
that was already running before reprogramming; it is the authoritative 2-core
evidence.

---

## 2026-07-09 -- 2x1 OpenSBI PDI BUILD SUCCESS (Build 72); ready for 2-core WMT-hypothesis repro

The 2-core OpenSBI/Linux PDI (Build 72, same stack as 64-core Build 68 but
tiles 2/1/2) built successfully locally on WSL+Windows Vivado 2024.2.2, `-jobs 8`,
after clearing the three PyHP-tmp pitfalls (stale io_xbar ports, WSL pyhp exec,
zero-byte truncated packet_filter).

Result:
- `write_device_image completed successfully`, 0 Errors, 4 critical warnings.
- Timing MET: `WNS=17.331 ns, TNS=0, WHS=0.010 ns, THS=0` (both setup and hold clean).
- Artifacts: `huaprop3_build72_2x1_opensbi_linux/debug_build/p3_top_build72_2x1_opensbi_linux.pdi`
  (15.5 MB, sha `9725063f`) + matching `.ltx`.
- Total wall time ~5.5 h (VP1902 large-device fixed overhead: device load, PS9
  hard-processor synth, IP OOC; the 2x1 logic itself is tiny).

This gives a clean 2-core apparatus on the SAME OpenSBI/Linux-6.6 stack as the
64-core path (only tile count differs), which is exactly the controlled variable
needed to test the WMT-write-guard hypothesis:
- Software image ready: `p3_opensbi_linux_2hart_final.img` (sha `8cb7c3ba`),
  OpenSBI clean (dbg28 probes reverted) + Linux NR_CPUS=2 + XSBench rootfs +
  DTB with `initrd-end=0x90107d9c`.
- Next: write SD, program the 2x1 PDI, boot, and observe whether the 2-core
  system ALSO hangs intermittently under stop_machine/XSBench.
  - If 2 cores hang too -> the WMT/L1.5 race is present even at minimal
    multicore, not a 64-core-scale-only effect (narrows to the single-tile
    L1.5 pipeline hazard).
  - If 2 cores are stable through XSBench -> establishes a known-good multicore
    baseline and supports the "load/congestion-gated at scale" reading; then
    the guard-restore test on 64-core is the direct fix check.

New reusable scripts committed: `p3_build72_2x1_opensbi_linux.tcl`,
`p3_rebuild_build72_2x1_opensbi_linux.sh`.


## 2026-07-09 -- 2x1 build pitfall #2: WSL Vivado can't exec pyhp.py; pre-generate ALL tmp on the Linux side

After purging the stale io_xbar tmp (pitfall #1), the 2x1 build failed EARLIER,
in PyHP preprocessing (`p3_create_bd.tcl:526`):
```
Info: piton/design/chipset/rtl/packet_filter.v.pyv exists! Preprocessing...
couldn't execute "pyhp.py": no such file or directory
```
Root cause: this is a WSL repo built by the **Windows** Vivado 2024.2.2 (the
`/home/illya/bin/vivado` wrapper). `pyhp_preprocess.tcl` uses a pre-existing
`.tmp` if present, else `exec pyhp.py <file.pyv>`. Windows Vivado cannot execute
the WSL python script `piton/tools/bin/pyhp.py`, so any `.pyv` WITHOUT a
pre-existing `.tmp` breaks the build. As long as every `.tmp` exists, Vivado
takes the "Using pre-existing" path and never calls pyhp.py -- which is why the
64-core Verilator flow (which pre-builds all tmp on the Linux side) worked, and
why deleting tmp for pitfall #1 then exposed this.

Fix: pre-generate the FULL consistent tmp set in WSL with the 2x1 + P3 env
before launching Vivado:
```
export PITON_X_TILES=2 PITON_Y_TILES=1 PITON_NUM_TILES=2 PITON_ARIANE=1        PITON_RV64_PLATFORM=1 PITON_NETWORK_CONFIG=2dmesh_config        CONFIG_SYS_FREQ=30000000 CONFIG_L15_SIZE=8192 CONFIG_L15_ASSOCIATIVITY=4        CONFIG_L1D_SIZE=8192 CONFIG_L1D_ASSOCIATIVITY=4 CONFIG_L1I_SIZE=16384        CONFIG_L1I_ASSOCIATIVITY=4 CONFIG_L2_SIZE=65536 CONFIG_L2_ASSOCIATIVITY=4        PROTOSYN_RUNTIME_BOARD=huaprop3        PROTOSYN_RUNTIME_DESIGN_PATH=$DV_ROOT/design/xilinx        PATH=$PITON_ROOT/piton/tools/bin:$PATH
# for each missing .tmp: (cd $(dirname pyv) && python3 pyhp.py $(basename pyv) > $tmp)
#   .v.pyv -> .tmp.v ; .h.pyv -> .tmp.h  (get the extension mapping right!)
```
Generated 9 missing chipset/io_xbar tmp; verified `chipset_impl.tmp.v` and
`io_xbar_top_wrap.tmp.v` both reference ports up to `dataIn_8` (9 P3 devices),
and no `.tmp` retained the 09:50 sim-era (8-port) generation.

RESULT: 2x1 synthesis passed -- `Synthesis finished with 0 errors, 0 critical
warnings`, `synth_design completed successfully`. Build moved into
implementation. Both PDI-build pitfalls (stale-tmp port mismatch, WSL pyhp exec)
were pure build-infra issues, unrelated to the 2-core RTL or the WMT hypothesis
under test.

Combined lesson for local WSL P3 builds after a different flow: (1) purge the
full chipset/io_xbar/tile tmp set, then (2) pre-generate ALL of them in WSL with
the target tile+board env, so Windows Vivado only ever reads pre-existing tmp.


## 2026-07-09 -- 2-core (2x1) OpenSBI PDI build: stale io_xbar/chipset PyHP tmp caused `dataIn_8 does not exist`

Starting the 2-core repro of the WMT hypothesis (same OpenSBI/Linux 6.6 stack as
64-core, only harts=2). Software image done (`p3_opensbi_linux_2hart_final.img`,
sha `8cb7c3ba`, initrd-end patched to `0x90107d9c`). Created 2x1 build scripts
from build68 (`p3_build72_2x1_opensbi_linux.tcl` +
`p3_rebuild_build72_2x1_opensbi_linux.sh`, tiles 2/1/2), building locally with
Vivado 2024.2.2, `-jobs 8`.

First synth_1 FAILED:
```
ERROR: [Synth 8-11365] instance 'io_xbar_noc2/noc3' of 'io_xbar_top_wrap':
       named port connection 'dataIn_8' does not exist
ERROR: [Synth 8-6156] failed synthesizing 'valrdy_to_credit__parameterized1' ->
       chipset_impl -> chipset -> system -> openpiton_wrapper -> p3_top
```

Root cause: **inconsistent PyHP `.tmp.v` generation across two builds.** The IO
crossbar port count comes from the devices table (`ReadDevicesXMLFile()` ->
`DYNAMIC_NODE_PORT`), NOT from tile count. P3 `devices_ariane.xml` has 9 devices
(chip/mem/iob/sd/uart/ariane_debug/bootrom/clint/plic -> portnum 0..8), so a
correct `chipset_impl.tmp.v` instantiates ports `_0.._8`. But the io_xbar
`.tmp.v` files on disk were stale from the 09:50 64-core **Verilator** build,
which uses a different (8-port) devices table -> `io_xbar_top_wrap` only had
ports `_0.._7`. `chipset_impl` (regenerated fresh at 20:19 with 9 ports) then
referenced a nonexistent `dataIn_8`.

Deleting only the 5 tile-dependent tmp (define/chip/chipset_impl/flat_id/xy) that
CLAUDE.md lists is NOT enough for a cross-flow switch (Verilator sim <-> P3
FPGA). The io_xbar set (`io_xbar_top_wrap`, `io_xbar_top`, all
`io_xbar/dynamic/rtl/*`, `io_xbar/components/rtl/*`, `packet_filter`,
`storage_addr_trans*`) also depends on the devices table and must be purged so
it regenerates with the P3 (9-port) devices, consistent with `chipset_impl`.

Fix: removed all stale chipset/io_xbar `.tmp.v` + the 5 tile tmp, cleaned the
`p3b72_2x1` workdir, relaunched. Lesson: when reusing a repo that last ran a
different flow (sim vs FPGA, or a different board), purge the full chipset/io_xbar
tmp set, not just the tile tmp, or synth fails on device-port mismatch.


## 2026-07-09 -- Static deep-dive: L1.5 WMT write-guard removed "for timing" is a concrete suspect for the intermittent stale-L1 hang

Since sim exhausts the RTL-functional coherence hypotheses (all coh_* PASS) but
the board hangs intermittently under load, I traced the ONE piece of L1.5
coherence state that (a) gates whether an invalidation reaches the L1 D-cache
at all, and (b) has an explicit "timing fix" that dropped a hazard guard: the
**Way-Map Table (WMT)**.

### Why the WMT is the linchpin of L1 invalidation

The L1.5 does not track which L1-D way holds a line directly; it keeps a WMT
(`rf_l15_wmt.v.pyv`, one entry per L1D set) mapping L1.5 line -> L1D way. When a
remote store forces an invalidation, the pipeline forwards it to the core ONLY
if the WMT lookup matches (`l15_pipeline.v.pyv:3904`):
```
`L15_CPX_GEN_INVALIDATION_IF_TAGCHECK_MES_AND_WAYMAP_VALID:
    cpx_req_val_s3    = tagcheck_state_mes_s3 && wmt_compare_match_s3;  // WMT-gated
    cpx_inval_way_s3  = wmt_compare_match_way_s3;                       // way from WMT
```
So a stale/incorrect WMT read => the invalidation is silently dropped or sent to
the wrong way => the L1 keeps a stale line => a spinning reader reads the old
value forever. That is exactly the stop_machine / XSBench symptom.

### The WMT RAM has a 2-cycle write latency and NO write-to-read bypass

`rf_l15_wmt.v.pyv`: read is `regfile[read_index]` registered (issue s2, data
s3). Write is registered TWICE: `write_valid`->`write_valid_f` (1 cyc), then the
actual `regfile[write_index_f] <= ...` on the NEXT cycle. So a WMT write decided
in s3 lands in the array **2 cycles after s3**, and the read port does not
forward the pending `write_data_f`.

### The removed guard (the concrete suspect)

`l15_pipeline.v.pyv:3776-3778`:
```
// trin: timing fix: removing guard for wmt writes; should still be correct
// l15_wmt_write_val_s3 = wmt_write_val_s3 && !stall_s3 && val_s3;
l15_wmt_write_val_s3 = wmt_write_val_s3 && val_s3;   // <-- !stall_s3 dropped
```
The WMT write now fires even while s3 is stalled. `stall_s3 = stall_for_cpx_s3
|| stall_for_noc3_s3` -- i.e. it stalls whenever the CPX (response-to-core) or
NoC3 (writeback-to-L2) channel is congested. **Under 64-core load those channels
are frequently congested, so s3 stalls are common** -- load-dependent, matching
the board behaviour and the sim's inability to reproduce (the sim's
zero/fixed-latency `fake_mem_ctrl` almost never congests CPX/NoC3).

### The hazard window (hypothesis, not yet proven)

The index-conflict guard that protects back-to-back same-set accesses only
covers the s2/s3 slots:
```
stall_index_bypass_match_s1 = (val_s2 && idx_s1==idx_s2) || (val_s3 && idx_s1==idx_s3);
```
But the WMT write completes 2 cycles AFTER s3. When s3 is stalled for multiple
cycles with the write-guard removed, the timing relationship between "when the
WMT write actually lands" and "when a following same-index request's s2 read
samples the array" shifts by the stall length. The guard reasons about pipeline
slots (s2/s3), not about the RAM's internal 2-cycle write pipe, so a stalled-s3
WMT write racing a trailing WMT read of the same index is not obviously covered.
If that read samples the array in the 1-cycle gap before the delayed write
lands, it gets stale way-map data -> wrong/dropped invalidation.

This is a **specific, code-anchored hypothesis**, NOT yet proven. It is exactly
the class the fixed-latency sim can't hit and STA can't see (functional protocol
race, not setup/hold), and it is load-gated (needs CPX/NoC3 congestion to stall
s3), which fits "passes light diags, hangs under heavy 64-core traffic".

### Next steps to prove/disprove
1. Build a focused sim assertion: in `cmp_l15_messages_mon`, flag any cycle
   where `l15_wmt_write_val_s3` is high while `stall_s3` is high AND a WMT read
   to the same index is in s2 within the next 2 cycles. Run the existing 8x8
   model under coh_stopmachine + an added CPX/NoC3 backpressure injector so s3
   actually stalls (the missing ingredient the fake mem never provides).
2. If confirmed, the fix is to restore the guard (or add a WMT write-to-read
   bypass / extend the index-conflict stall to cover the RAM's 2-cycle write
   pipe). Cheap to try: re-add `&& !stall_s3` and re-run the board XSBench/boot.
3. Board-side cross-check: the earlier proposal (a kernel module hammering
   stop_machine, dumping the stuck hart's PC/satp) still distinguishes "stale L1
   data" (this hypothesis) from an MMU/TLB or IPI-delivery cause.

## 2026-07-08 -- coh_stopmachine PASSES too: sim exhausts the shared-memory/coherence hypotheses; failure is NOT in a layer the sim models

`coh_stopmachine.c` (the diag purpose-built to mirror `multi_cpu_stop`: leader
plain-stores a shared `state` through 8 phases, 63 followers tight-spin reading
it) ran on the instrumented 8x8 model: **Hit Good trap at main_time 44M, zero
COH_IPI64_MONFAIL, zero stuck_hart** -- all 8 rounds of cross-hart shared-state
visibility converged cleanly at 64 cores (43 min wall).

### The full simulation ledger now

| diag | pattern under test | result |
|------|--------------------|--------|
| coh_2core | 2-core L1 invalidate (store->load) | PASS |
| coh_64core | 64-core broadcast invalidate | PASS |
| coh_ipi64 x4 | MSIP send + poll pending bit + slots[] | PASS x4 |
| **coh_stopmachine** | **multi_cpu_stop tight shared-state spin** | **PASS** |

I deliberately built the last diag to be the closest possible match to the
board's failing pattern (concurrent tight shared-writer visibility, the exact
thing hypothesis #1 blamed). It still passes. Combined with the instrumented
monitor never tripping, this is a decisive **negative result**: in a
cycle-accurate 64-tile RTL simulation, the L1/L2/NoC coherence + shared-memory
ordering path is functionally correct even for the multi_cpu_stop access
pattern. Hypothesis #1 ("cross-hart shared-memory ordering race in the RTL") is
now itself largely falsified at the RTL-functional level.

### What this redirects to: the failure lives in a layer the sim does NOT model

The sim is RTL-cycle-accurate but omits exactly the things that differ between
"diag passes" and "board hangs":

1. **Real DDR3 / MIG + asynchronous clock domains.** Sim uses a behavioral,
   near-deterministic memory model. The board has real DDR3 refresh, MIG
   calibration latency, and NoC<->chipset<->DDR clock-domain crossings
   (`noc_mig_bridge.v`, async FIFOs). A coherence/ordering hazard that only
   manifests under real memory latency or a marginal CDC would pass in sim and
   hang on the board.
2. **MMU / TLB / S-mode.** The board runs full Linux: `multi_cpu_stop` executes
   with the MMU on, page-table walks, and TLB state; stop_machine is often used
   precisely for TLB shootdown-adjacent work. Our diags are bare-metal M-mode,
   physical addresses, no MMU/TLB. A TLB or page-table-walk coherence issue is
   invisible to these diags.
3. **True concurrency of timer + IPI + workload.** The board has 250 Hz timer
   IRQs on all 64 harts plus IPIs plus real memory traffic, all at once. The
   diags exercise one pattern with no concurrent timer/IRQ.

### Revised conclusion + next step

The bug is very unlikely to be a pure RTL coherence-logic defect (4 diags + an
instrumented protocol monitor say the logic is correct). The strongest
remaining suspects are the ones the sim cannot see:
- **(a) real-hardware timing / CDC / DDR-latency-dependent ordering hazard**, or
- **(b) an MMU/TLB-level coherence or shootdown issue under Linux**, or
- **(c) the dbg21 MMIO-read barrier being insufficient at 64 cores** (it papers
  over ordering only on non-IRQ trap entry; heavy concurrent compute + timer
  IRQs have windows it doesn't cover -- consistent with XSBench hanging in
  heavy-compute and boot hanging at the stop_machine after clocksource init).

Because sim has been exhausted for this class, the decisive next step is
**board-side, not more sim**:
- Add a tiny **Linux kernel module** that runs a `multi_cpu_stop`-style
  all-hart tight shared-state spin (or just calls `stop_machine()` in a loop)
  and, on timeout, dumps **which hart** failed to observe the shared update and
  its `satp`/`sstatus`/PC -- distinguishing (b) MMU/TLB from (a) plain memory.
- OR the ILA on CLINT `msip_q`/`ipi_o` + the NoC-to-DDR bridge handshake, to
  catch (a) a timing/CDC stall that sim's behavioral memory can't reproduce.
- Also worth a cheap test: re-enable the dbg22 all-traps barrier BUT only after
  the IRQ ack (fixing the dbg22 livelock) -- if that makes the hang go away,
  it's (c) barrier-coverage.

Net: the "coherence race" has been chased into a corner where sim says the RTL
logic is correct; the remaining truth is at the hardware-timing or MMU layer,
which only board instrumentation can see.

## 2026-07-08 -- Instrumented the L1.5 monitor fail branches; coh_ipi64 passes again; new coh_stopmachine diag targets the real race

Acting on the 2026-07-07 re-analysis (the bug is most likely a cross-hart
shared-memory ordering race in `multi_cpu_stop`, not a lost MSIP). Did the
sim-side work that needs no board churn.

### Instrumented every monitor fail branch (the real fix to "guessing from the last print")

`cmp_l15_messages_mon.v.pyv` has 28 `MONITOR_PATH.fail(...)` branches; the
central `fail` task (`monitor.v.pyv:72`) only prints the comment string, no
context -- which is why prior debugging guessed the failing tile from the last
high-volume monitor line. Fixed at the source: in the per-tile PyHP loop, every
`` `MONITOR_PATH.fail( `` is now prefixed with a compact self-report
`COH_IPI64_MONFAIL t=<time> TILE<i> write_val_s2=.. ghid_val_s2=.. read_val_s2=..
val_s1=.. stall_s1=.. noc2_reqtype=.. mshrid=.. hmc_fill=..`. The literal
`TILE0`/`L15_TOP0`/`L15_PIPE0` are substituted to the correct tile index by the
existing loop, so any tile's fail now names itself + its L1.5 CSM/pipeline state.
All signal names verified against `l15_csm.v.pyv` (write/ghid/read_val_s2) and
`l15_pipeline.v.pyv` (noc2decoder_l15_*). Rebuilt the 8x8 model
(`--hierarchical -O0`, ~13 min, rc=0, `Vcmp_top` 285 MB).

### coh_ipi64 passes AGAIN (4th pass) -- it does not reproduce the real failure

One instrumented `coh_ipi64` run (monitor enabled, 54 min wall, reached
`main_time=53.7M`, past both old ~23.5M and ~40M "fail" windows): **Hit Good
trap, no COH_IPI64_MONFAIL**. Combined with the 3 prior passes, `coh_ipi64` is
not a reliable reproducer. The reason is structural: `coh_ipi64` = hart0
*sequentially* writes MSIP[1..63] + readers *poll the local mip pending bit*;
its only shared-memory step is a one-shot `slots[]` write/read. That is neither
the concurrent IPI storm nor the tight shared-state spin that `multi_cpu_stop`
actually does. Looping 54-min runs to chase a ~33% (and now apparently lower)
hit rate on the *wrong* access pattern is a poor use of sim time.

### New diag: coh_stopmachine.c -- models the actual multi_cpu_stop spin

Wrote `piton/verif/diag/c/riscv/ariane/coh_stopmachine.c` to match the failing
pattern directly: a leader advances a SHARED `state` word through ROUNDS=8
phases with a PLAIN store; all 63 followers TIGHTLY SPIN reading that same
shared word until they observe each update (AMO only for the arrive/ack
counters, so the sync is coherent regardless of the plain `state` path under
test). A follower that spins past SPIN_LIMIT without seeing the update records
`stuck_hart`/`stuck_round` and fails -- that stale shared read IS hypothesis #1.
This exercises cross-hart shared-writer visibility under concurrency, which
`coh_ipi64`/`coh_64core` do not. If the L1.5 coherence anomaly is real on this
path, the instrumented monitor will now self-report which tile and what CSM
state. Run launched (finish_mask=0x1, hart0 = leader/verifier). Result pending.

### Files
- `piton/verif/env/manycore/cmp_l15_messages_mon.v.pyv` -- fail-branch instrument (per-tile self-report).
- `piton/verif/diag/c/riscv/ariane/coh_stopmachine.c` -- new multi_cpu_stop-pattern probe.

## 2026-07-07 -- Re-analysis: "MSIP delivery to 58 harts broken" is FALSIFIED; real suspect narrows to a concurrent-IPI ordering/coherence race

Went back over the wiki logs + static evidence instead of pushing the blocked
OpenSBI probe. The prior "stop_machine IPI deadlock => 58/64 harts never get
their MSIP" root-cause statement is **over-read and internally inconsistent**.
Correcting it.

### Falsification 1: SMP bringup already proves MSIP delivery works for all 64 harts

Static: OpenSBI `sbi_hsm.c:168-186` -- a secondary hart parks in
`sbi_hsm_hart_wait` with `csr_set(CSR_MIE, MIP_MSIP|MIP_MEIP)` then `wfi()`.
It is woken **only** by an MSIP IPI (`hart_start` -> `sbi_ipi_raw_send`). So
every secondary that comes online was woken from `wfi()` by an MSIP.

Board log (dbg27, `b0369yu8y.output`): `smp: Brought up 1 node, 64 CPUs`,
`online_cpus=64`. **All 64 harts were successfully woken by MSIP.** Therefore
the CLINT MSIP -> per-hart interrupt-line delivery path is **functional for all
64 harts** -- a permanently broken wire/route to 58 harts is impossible, or
those harts would never have come online. The earlier "58 harts never respond
to the IPI" is falsified.

### Falsification 2: soft-lockup absence != "hart didn't get the IPI"

The 6 CPUs that report soft lockup are `{0,1,3,4,11,12}`. I previously read the
other 58 as "did not respond to the stop IPI". That is a misread of the
soft-lockup mechanism:

- soft lockup is a **per-CPU** watchdog: a CPU prints **only if its own timer
  IRQ is still alive** AND it has been stuck >20 s in a non-scheduling loop.
- So the 6 reporters = harts that (a) got the stop IPI, (b) entered
  `multi_cpu_stop`, and (c) still have a live timer IRQ to run the watchdog.
- The silent 58 are **not** necessarily "IPI lost". They split into: harts
  parked idle (nothing to report), harts stuck with a **dead** timer IRQ
  (can't print), or harts stuck elsewhere. Absence of a soft-lockup line
  carries almost no information about IPI receipt.

So the headline number "6/64 responded" does **not** mean "58 IPIs lost".

### What actually differs: bringup (works) vs stop_machine (hangs)

Both use the same CLINT MSIP hardware, so the bug is not the wire. The
differences that matter:

1. **Concurrency/contention.** Bringup is sequential/batched -- harts woken
   with low simultaneous NoC load. stop_machine fires IPIs to all 63 harts
   near-simultaneously **and** each acks back, a burst of concurrent MSIP MMIO
   writes + returns through the NoC/chipset CLINT bridge under contention.
2. **Privilege/MMU state.** At bringup the target sits in M-mode `wfi`. At
   stop_machine the target runs S-mode Linux with the MMU on; the IPI must be
   taken as M-mode MSIP, reflected to S-mode SSIP (`sbi_ipi_process` ->
   `csr_set(MIP, MIP_SSIP)`), and delivered through Linux's irq path. More
   stages, more shared state.
3. **Shared-memory sync, not just the interrupt.** `multi_cpu_stop` is a
   spin-loop state machine over a **shared `multi_stop_data` struct** in DRAM:
   every hart must SEE the state advance (MULTI_STOP_PREPARE -> DISABLE_IRQ ->
   RUN -> EXIT) written by the leader. This is exactly the cross-hart
   shared-writer visibility that the silent MMIO-read coherency barrier (dbg21)
   was patching. If a hart's view of `msdata->state` is stale, it spins forever
   even though its IPI arrived fine.

### The strongest under-weighted lead: the sim DID reproduce an L1.5 anomaly on the IPI/sync path

`coh_ipi64.c` on the 8x8 Verilator model produced an intermittent
`cmp_l15_messages_mon` failure -- an L1.5 DTAG/protocol anomaly around a TILE0
store at `0x8020e9c`, on the IPI/sync path. It was later demoted to a "stale
lead" only because a low-overhead *progress-only* sweep didn't re-trip the
monitor in 3 runs -- that is weak evidence of absence (the monitor prints were
suppressed for speed, and the fail is intermittent). Crucially, `coh_64core`
(AMO/store broadcast) did **not** trip it, but `coh_ipi64` (MSIP + tight
cross-hart sync) did -- the anomaly is specific to the interrupt/sync pattern,
which is exactly `multi_cpu_stop`.

### Revised root-cause hypothesis (ranked)

1. **Cross-hart shared-memory ordering/visibility race in `multi_cpu_stop`'s
   `msdata->state`** (a coherence/ordering problem on the sync variable, NOT a
   lost interrupt). Fits: bringup works (little shared spinning) but
   stop_machine hangs (tight shared spin); dbg21 needed an MMIO-read barrier
   for exactly this class; the `coh_ipi64` L1.5 anomaly is on this path;
   intermittent + timing-dependent; XSBench unionized-grid (heavy shared
   sync) hangs the same way.
2. **Concurrent-IPI ordering bug** in the CLINT bridge / `sbi_ipi` fast-path
   under a simultaneous 63-way storm (e.g. the `__atomic_fetch_or(ipi_type)`
   "only send if was 0" optimization dropping a needed re-send when an ack and
   a new send race). Lost/merged IPI, but only under the storm.
3. **Timer IRQ death on the stuck harts** (the silent 58 include harts whose
   timer stopped), making it look like more harts are stuck than the 6 that can
   still print.

### Why this matters / next step

The blocked OpenSBI MSIP write+readback probe was testing hypothesis "MSIP
doesn't reach CLINT" -- which Falsification 1 already makes unlikely. Higher
value now:

- **Re-run `coh_ipi64` with the messages-monitor fail branch actually
  instrumented** (compact context print on the real `MONITOR_PATH.fail`), loop
  until it trips, and capture a narrow VCD of the TILE0 L1.5 `0x8020e9c`
  DTAG/MESI transition + the CLINT MSIP return flit. This directly tests
  hypothesis #1/#2 in sim, no board churn, no OpenSBI rebuild.
- Alternatively, a board-side software test that is decisive without ILA:
  a tiny kernel module / bare-metal loop that hammers `multi_cpu_stop`-style
  all-to-all IPI + shared-flag spin and counts which harts fail to observe the
  flag (distinguishes "IPI lost" from "shared read stale").
- ILA on CLINT `msip_q[63:0]`/`ipi_o[63:0]` remains the hardware-truth
  fallback, but it tests the *least* likely hypothesis first, so it is not the
  cheapest next step.

Bottom line: the failure is most likely a **cross-hart shared-memory
ordering/coherence race on `multi_cpu_stop`'s sync state under concurrency**,
not a broken per-hart MSIP wire. This also re-opens the question of whether the
dbg21 MMIO-read barrier is truly sufficient at 64 cores or just shifts the race
window.

## 2026-07-06 -- SBI-IPI->CLINT MSIP link full static audit: all code correct, bug is runtime

Per the user's link diagram and probability ranking, statically audited every
stage of Linux stop_machine -> SBI ecall -> OpenSBI -> CLINT MSIP -> interrupt
distribution -> tile. Conclusion: **the static code is correct at every stage;
there is no code bug**. The 58/64 non-respond is a runtime hardware behavior
(MSIP written but interrupt not delivered).

Per-stage audit (all PASS):
- DTB `cpu@N reg=<N>` (`p3_generate_opensbi_dts.py:11`) -- hartid 0..63 contiguous.
- mhartid==flat_id==cpu index -- proven by successful 64-CPU bringup.
- Linux `__sbi_send_ipi_v02` (`sbi.c:238`): `sbi_ecall(IPI, SEND_IPI, hmask=1, hbase=cpuid_to_hartid_map(cpu))` -- per-CPU ecall, hmask=1 correct.
- OpenSBI `sbi_ipi_send_many` (`sbi_ipi.c:118-172`): `weight==count` validation passes (else `SBI_EINVAL` + pr_err).
- OpenSBI `mswi_ipi_send` (`aclint_mswi.c:44`): `writel_relaxed(1, msip[hartid-first_hartid])` -- offset correct, matches `clint.sv` decode.
- OpenSBI `sbi_ipi_process` (`sbi_ipi.c:259-271`): `raw_clear -> xchg(ipi_type,0) -> loop` -- standard read-clear-process, xchg cannot lose a bit. `raw_send`/`raw_clear` wmb pairing correct.
- chip-level `ipi_i[63:0]` distribution (`chip.tmp.v`): all 64 tiles wired to `ipi_i[flat_id]` (count=64, identical to `timer_irq_i`).

Discriminating evidence:
- UART log has **zero** SBI IPI errors (grep `__sbi_send_ipi_v02`/`hbase failed` empty) -> all 64 ecalls succeed, OpenSBI really wrote 64 MSIPs (weight checks all pass).
- Yet only 6/64 harts `{0,1,3,4,11,12}` entered `multi_cpu_stop` (reported migration soft lockup); 58 did not respond.

Statically exonerated: #6 timer (soft lockup proves timer alive); #2 cpuid<->hartid mapping (DTB reg contiguous + bringup OK + no ecall errors); #3 OpenSBI hmask/hartid handling (`sbi_ipi_send_many` + mswi offset correct); code-level #1 (MSIP offset decode correct).

Converges to runtime suspects (invisible to static reading):
- MSIP MMIO write actually failed to set `msip_q[h]` in CLINT despite OpenSBI getting the B response (#1 runtime, needs ILA on `msip_q`).
- `ipi_o[h]` -> tile h interrupt line / 2-flop synchronizer intermittently fails for 58 harts.
- `ipi_type` shared cache-line coherence (#5, hart0 atomic write to remote scratch not visible to remote).
- 58 harts in idle, idle-state MSIP handling issue.

The 6/64 structural split `{0,1,3,4,11,12}` cannot be explained statically; needs runtime to see if the responder set is fixed across boots (structural) or random (NoC loss).

Next: build a runtime probe. dbg28 = `nowatchdog` to skip the boot-time stop_machine -> reach shell -> IPI ack-count probe (each CPU increments a shared counter on IPI; tally which CPUs respond); if shell unreachable, escalate to ILA on CLINT `msip_q[63:0]`/`ipi_o[63:0]` for deterministic hardware truth.

## 2026-07-06 -- dbg27 ROOT CAUSE: stop_machine IPI deadlock (timer exonerated; __do_softirq EPC mystery solved)

dbg27 (bootargs add watchdog/panic_on_warn; zero-synth, zero-sim) captured the
decisive root-cause evidence: the 64-core boot hangs at ~32s right after
`clocksource_done_booting`, and it is a **stop_machine IPI deadlock**, NOT timer
data corruption. The timer is fully exonerated.

Decisive stack (from the soft-lockup dump):
```
Stopper: multi_cpu_stop+0x0/0x14a <- stop_machine_cpuslocked+0xcc/0xfc
epc : __do_softirq+0x9e/0x25a
ra  : __irq_exit_rcu+0x8e/0xb4
```

Two iron-clad facts:
1. **`Stopper: multi_cpu_stop <- stop_machine_cpuslocked`** -- stuck in
   **stop_machine**. The migration kthread is busy-waiting in `multi_cpu_stop`
   for all harts to reach the stopmachine state.
2. **`epc: __do_softirq+0x9e`** -- **this is exactly the EPC that was misread
   for weeks as "the timer crash site"**. Real context: while the migration
   thread busy-waits in `multi_cpu_stop`, the timer interrupt fires normally
   (timer is innocent), and on IRQ exit the path is `__irq_exit_rcu` ->
   `__do_softirq`, so EPC lands at `__do_softirq+0x9e` -- the softirq entry
   boundary (the `csrsi sstatus,2` re-enable-interrupt spot identified in the
   2026-07-04 static check). It is an IRQ return address, NOT a fault site.
   The "timer data structure corruption" hypothesis is definitively dead.

CPUs that responded to stop_machine and are busy-waiting in `multi_cpu_stop`
(reported migration soft lockup):
- CPU#0 (migration/0, 218s) -- boot CPU, the stop_machine initiator, also waiting
- CPU#3 (34s), CPU#4 (50s), CPU#11 (154s), CPU#12 (170s)
(list still growing; per-CPU independent detection at different thresholds)

All of these have **live timer interrupts** (soft-lockup detection runs off the
timer-driven watchdog hrtimer), so the timer is completely innocent. They are
stuck in `multi_cpu_stop` because **some hart(s) never responded to the
stop_machine IPI**.

Root-cause direction: **stop_machine IPI delivery intermittently fails**.
stop_machine sends IPIs via SBI ecall -> OpenSBI -> CLINT MSIP MMIO write
(NoC -> chipset CLINT) to all 64 harts. Some hart never sees its MSIP -> its
migration thread never runs `multi_cpu_stop` -> every hart that did respond
busy-waits forever in `multi_cpu_stop` with no request-timeout (the 2026-07-04
no-timeout finding is exactly why this is a silent permanent hang). This narrows
the failure class from "memory/IO response loss" to specifically **IPI (CLINT
MSIP) delivery**, consistent with the 2026-07-04 convergence.

The demoted `coh_ipi64.c` 3-pass sweep was correctly demoted for an unloaded
sequential MSIP test; boot-time concurrent + real-SBI-ecall MSIP delivery is a
different regime. A new concurrent IPI stress probe is needed to reproduce.

bootargs lesson: `softlockup_panic=1` / `rcu_cpu_stall_timeout=10` /
`nmi_watchdog=1` were rejected as "unknown kernel command line parameters" (the
6.6 RISC-V kernel does not register them as `__setup` bootargs). Correct form is
`rcupdate.rcu_cpu_stall_timeout=10`. But `watchdog_thresh=10` + `panic_on_warn=1`
+ `CONFIG_SOFTLOCKUP_DETECTOR=y` (default on) were enough to fire this detection
(soft-lockup BUG + full stack). Next dbg28 should use `rcupdate.rcu_cpu_stall_timeout=10`
to also get the RCU stall (21s default), which names the non-responding hart.

Next steps:
1. Wait for / force an RCU stall to dump all CPU stacks -> identify the exact
   non-responding hart.
2. Write a concurrent IPI stress diag (64-hart simultaneous stop_machine-style
   MSIP storm) to reproduce MSIP loss.
3. Statically trace the OpenSBI SBI-IPI ecall -> CLINT MSIP MMIO write path
   (NoC -> chipset) for the drop point.

Artifacts: SD image `build/huaprop3/opensbi64/p3_opensbi_linux_64hart_dbg27.img`
(sha `211fb01f`); UART log in task `b0369yu8y` output (~103 KB, growing).

## 2026-07-04 -- dbg27: bootargs make the hang audible (softlockup/RCU-stall/panic_on_warn)

Non-sim route ① (make the hang audible; zero-synth, zero-sim). Built on the
static conclusion "no request timeout -> dropped response = silent permanent
hang": add kernel stall detectors to bootargs so a hang prints a stack /
identifies the stuck hart instead of going silent.

New bootargs appended (dbg27 DTB, all other payloads reused from dbg26):
`softlockup_panic=1 rcu_cpu_stall_timeout=10 panic_on_warn=1 nmi_watchdog=1 watchdog_thresh=10`

Config supports it: `SOFTLOCKUP_DETECTOR=y`, `LOCKUP_DETECTOR=y`,
`RCU_CPU_STALL_TIMEOUT=21` (overridable), `KALLSYMS_ALL=y` (stack traces carry
symbols), `PRINTK_TIME=y`. `DEBUG_INFO_NONE=y` (no DWARF, but kallsyms suffices).

Key engineering note: the bundle packer (`p3_make_opensbi_bundle_image.py`)
does NOT write `linux,initrd-start/end` into the DTB -- those were the
dbg26-corrected values. So the DTB cannot be regenerated from the generator
(that would drop initrd-end -> revive the dbg25 "No working init"). Instead:
decompile the dbg26 DTB -> patch the bootargs string only -> recompile,
preserving `initrd-end=0x90107d9c`. Then repack with the same payloads
(fw_jump_dbg21 / Image / initrd_v2) + the new DTB.

Artifacts:
- `build/huaprop3/opensbi64/p3_opensbi_64hart_dbg27.dtb` (23644 B; +96 B = new bootargs string)
- `build/huaprop3/opensbi64/p3_opensbi_linux_64hart_dbg27.img` (sha `211fb01f...`, 256 MiB)
- payload identical to dbg26 (fw 270048 / image 19884032 / initrd 1080732); only the DTB changed.
PDI (Build 68 hardware) unchanged -- bootrom reads the bundle from SD, so a
re-card + reset is enough; no FPGA reprogram.

Expected readout:
- "RCU stall" + CPU N stack on hang -> identifies the stuck hart and function.
- softirq/softlockup panic -> timer-path, stack visible.
- Still completely silent (no RCU stall either) -> hart fully dead, timer/IPI
  both severed -> escalate to ④ OpenSBI M-mode heartbeat watchdog or ILA.

Next: write card + reset + capture UART, run single-thread XSBench stress, wait
for a hang and see whether a stack/stall is printed.

## 2026-07-04 -- Interrupt/timer RTL path verified clean; no-request-timeout explains silent hang

Continued the static root-cause hunt through the interrupt/timer hardware
path. **No concrete bug found** in the interrupt path itself, but the sweep
produced two findings that redirect the investigation.

**Verified clean (do not chase bugs here):**
- `chipset_impl.v.pyv:1213` instantiates `riscv_peripherals` ONCE in the
  chipset -> there is a single shared CLINT (not 64 per-tile copies). `NumHarts`
  = `PITON_NUM_TILES`.
- `clint.sv` (ETH Zurich design) is standard: shared `mtime`, per-hart
  `msip`/`mtimecmp`, `MSIP_BASE`/`MTIMECMP_BASE` decode indices
  (`address[7:2]` / `address[8:3]`) correct for `NR_CORES=64`. `ipi_o=msip_q`
  (registered level), `timer_irq_o` combinational `mtime>=mtimecmp` (level,
  re-sampled by the core synchronizer).
- Interrupt distribution is **direct wiring, not a network**: chipset
  `timer_irq_o[63:0]`/`ipi_o[63:0]` -> `chip.v.pyv:152` -> each tile consumes
  its own bit (`chip.tmp.v:3689 .timer_irq_i(timer_irq_i[0])`). No NoC, no
  arbitration, no serialization -> if CLINT asserts `ipi_o[h]`, tile h WILL see
  it (after the 2-flop synchronizer in `ariane_verilog_wrap.sv:156-181`).
  Core-side wake-up interrupt workaround is commented out; the active gate is
  just a 32k-cycle SRAM-init reset delay.
- `noc_axilite_bridge.v` (every CLINT/PLIC/UART/bootrom MMIO write): correct
  single-outstanding AXI-lite FSM; AW/W/B status tracked separately and
  combined; load/store ACK arbitration is fair.
- `rv_plic_gateway.sv` (lowRISC): standard edge/level `ip`/`ia` logic.
- `rtc`/`mtime`: `system.v:601 assign rtc = rtc_div[6]` -- synchronous 7-bit
  divide-by-128 of `core_ref_clk`, registered (no glitch); CLINT resynchronizes
  via 2-stage `clint_sync_wedge`. timebase matches DTB.

**Decisive architectural finding -- no request timeout:**
`grep -riE "timeout|watchdog"` over `tile/l15/rtl`, `tile/l2/rtl`,
`tile/dynamic_node/rtl`, and `wt_l15_adapter.sv` returns **zero** matches in
the synthesized logic. The only hit, `corev_apu/tb/.../sim_timeout.sv`, is a
**testbench** `$fatal` watchdog -- which exists precisely because the design
can hang forever. L2 (`l2.v`, `l2_pipe1.v`, `l2_mshr.v.pyv`) has 0 timeouts.
**Consequence:** an L1.5/L2 MSHR that allocates for a miss and never receives
its fill/response stays PENDING forever -> **silent permanent hang, no trap,
no log, no error**. The architecture assumes the NoC is perfectly reliable.

**Why this redirects the root cause:**
The observed symptom -- intermittent, SAME image sometimes boots fully,
sometimes silently stalls at `[0.000000]`; single-thread XSBench also hangs;
stop_machine also deadlocks; all with zero UART growth and no panic -- is
exactly the signature of **an intermittently dropped/misrouted NoC response
(memory fill or MMIO ack) becoming a permanent silent hang because there is no
timeout**. The timer EPC captured earlier is incidental (timer interrupts keep
firing; the trap state happens to land at the softirq IRQ-enable boundary).
This is NOT 64-core-specific -- a single core doing heavy memory traffic can
hit it. The failure mode is the memory/IO response path, not the timer logic.

**Highest-value next probe (cheap, deterministic in sim):** add an
MSHR-stall watchdog to the Verilator harness -- sample the L1.5
outstanding-request signal and dump VCD + the stuck address the first time a
request stays outstanding > N (e.g. 10000) cycles. Run a single-hart memory-
stress diag that mimics XSBench's pattern (large working set, pointer chase,
cache thrashing) rather than the simple store-load of `coh_*.c`. The watchdog
fires deterministically the moment a response is dropped, localizing the drop
to a specific address / NoC route. This beats blind 8x8 VCD sweeps.

## 2026-07-04 -- Static check refutes kernel timer data-corruption hypothesis

Static review of the kernel-side timer/softirq path refutes the working
hypothesis that 64-core instability comes from corrupted Linux timer data
structures (hrtimer / `timer_list` linked lists).

- **Kernel is 6.6.0**, not 5.1.0-rc7. (5.1.0-rc7 is the Build 66 BBL baseline;
  the 64-core OpenSBI/Linux path uses 6.6.) Config: `NR_CPUS=64`, `HZ=250`,
  `NO_HZ_IDLE=y`, `HIGH_RES_TIMERS=y`, `PREEMPT_NONE=y`, `DEBUG_INFO_NONE=y`,
  and **`DEBUG_LIST/DEBUG_SG/DEBUG_PLIST=y`** (linked-list integrity self-checks
  enabled).
- **Crash EPC reinterpretation.** `__do_softirq` is at `0xffffffff807fa0b6`;
  crash EPC `0xffffffff807fa154` = `__do_softirq+0x9e`. Disassembly shows the
  instruction there is `sext.w a5,s11`, immediately preceded by
  `csrsi sstatus,2` (re-enable S-mode interrupts). `__do_softirq` explicitly
  `local_irq_enable()` before draining the softirq backlog; a pending timer
  interrupt is sampled at that boundary with EPC pointing to the next
  (non-memory, non-faulting) instruction. So this EPC is the **normal S-mode
  timer-interrupt entry boundary**, not a fault inside list traversal.
  `cause=0x8000000000000005` is "S-mode timer interrupt" (bit 63 set),
  consistent with this. The earlier "traversing a corrupted list -> crash"
  reading is wrong; `sext.w` cannot fault.
- **`DEBUG_LIST/SG/PLIST=y`** means software list corruption would WARN/BUG
  with a stack trace before any silent hang. No such warning appears in any
  captured log -> no evidence of software-corrupted timer lists.
- **`__hrtimer_run_queues`** (`kernel/time/hrtimer.c`) and the RISC-V timer
  driver (`drivers/clocksource/timer-riscv.c`) are standard upstream code with
  no 64-core-specific anomaly visible statically. No SSTC on this CVA6, so the
  kernel programs the timer via `sbi_set_timer()` ecall -> OpenSBI -> CLINT
  mtimer (M-mode side already verified correct in the 2026-07 deep-dive).
- **Decisive argument against a software timer bug:** the failure is
  **intermittent with the SAME PDI/SD image** -- sometimes boots fully to a
  64-core shell, sometimes stalls at `[0.000000]`. A deterministic kernel
  data-structure corruption would reproduce every boot. Intermittency =>
  timing/race in a path exercised by SMP bring-up and heavy parallel compute,
  not a software bug in timer data.
- **Evidence converges on the interrupt/timer-IPI delivery round-trip:** CPU
  MMIO write (CLINT MSIP for IPI, CLINT mtimer for timer) -> NoC -> chipset ->
  CLINT register, with the resulting interrupt routed back to a target hart.
  Both `stop_machine` (`IPI_CPU_STOP` to all 64 harts) and the early boot stall
  require reliable per-hart interrupt delivery. Hypothesis #3 (NoC/memory
  subsystem intermittent) is the best fit, **specifically the per-hart CLINT
  interrupt delivery path**, not random memory data corruption.
- **Next target:** statically inspect the OpenPiton chipset CLINT MSIP/mtimer
  interrupt-injection path, then add a stress probe (64-hart MSIP storm that
  mimics `stop_machine` timing under NoC load) to reproduce the intermittent
  loss. The demoted `coh_ipi64.c` used a 4M-cycle spin limit and only tested
  MSIP visibility under no load; it was not representative of the kernel's
  all-hart-simultaneous IPI under NoC contention.

## 2026-07-02 -- Evidence audit demotes stale coh_ipi64 root-cause claim

- Audited the current 64-core bring-up guidance after the 2026-07-01
  progress-only `coh_ipi64.c` sweep. The old L1.5 monitor failure is now
  classified as an unconfirmed stale lead, not an active reproduced root cause.
- Updated current guidance to stop spending time on blind 8x8 VCD windows from
  the old `~40M` log. If simulation is used again, it should first instrument
  concrete `MONITOR_PATH.fail(...)` branches; otherwise, prioritize board-level
  layer evidence for programming DONE, UART TX, DDR, SPI-SD/GPT/`P3OS`, OpenSBI
  entry, and Linux SMP progress.
- Historical June devlog entries are kept as original records. Current status
  pages and agent guidance now distinguish verified facts, stale leads, and
  unproven hypotheses.
- Added an explicit board-evidence hierarchy: B69-style diagnostics can prove
  UART/DDR/SPI-SD/GPT/`P3OS` layers, later logs verify OpenSBI and Linux
  64-CPU SMP bring-up, but current retained evidence still does not prove an
  interactive 64-core shell, `nproc=64`, or XSBench completion.

## 2026-07-01 -- coh_ipi64 progress-only sweep does not reproduce old fail

- Ran the rebuilt progress-only `coh_ipi64.c` 8x8 Verilator sweep with no
  small-VCD hierarchy dereferences. The first three runs all passed:
  run 1 `12:09:46..13:23:51`, run 2 `13:23:51..14:38:34`, and run 3
  `14:38:34..15:36:47`.
- Run 3 crossed the older `main_time ~= 40M` suspected failure window and still
  reached the good trap. Run 4 started automatically and was stopped manually
  because three consecutive passes are enough to show the old `~33%` failure
  rate is not reproducing in the current model.
- Treat the older `TILE36 L15_CSM REQ MON` / `SIGDIE` log as a stale lead, not
  as a current root-cause reproducer. The next step is to instrument the actual
  `cmp_l15_messages_mon.v.pyv` `MONITOR_PATH.fail(...)` branches with compact
  context prints, then rerun until a real fail branch self-identifies.

## 2026-07-01 -- coh_ipi64 progress-only long-run mode

- Added `COH_IPI64_PROGRESS_ONLY` to the Verilator harness so long
  `coh_ipi64.c` sweeps can print `COH_IPI64_PROGRESS main_time=<n>` without
  enabling the small VCD signal sampler.
- This keeps the next root-cause step ordered: first reproduce the real
  messages-monitor fail string and approximate `main_time` with a low-overhead
  run, then rebuild the harness with a narrow `COH_IPI64_SMALL_VCD_START/STOP`
  window around that time.
- The earlier full small-VCD and short-window runs remain useful for exclusion,
  but they are too slow for probability sweeps because even dormant hierarchical
  signal access adds measurable overhead on the 8x8 `-O0` model.

## 2026-07-01 -- coh_ipi64 short-window VCD excludes TILE36 BootROM/CSM hypothesis

- Ran the short `main_time=22000000..24000000` `coh_ipi64` VCD window for
  seed 0 and decoded the older `23535xxx` TILE36 event. The access is an
  IFILL response for BootROM address `0xfff101000c`, not a CLINT MSIP/IPI
  response (`0xfff1020000` range).
- The TILE36 CSM state at `23535250` is a read-only path:
  `l15_csm_req_val_s2=1`, `read_val_s2=1`, `write_val_s2=0`, `ghid_val_s2=0`,
  and `wr_en_s2=0`. The NoC2 message is `MSG_TYPE_DATA_ACK` with `mshrid=1`
  and `noc2decoder_l15_hmc_fill=0`, so it is not an HMC/CSM refill write.
- This rules out the concrete hypothesis that the old
  `TILE36 L15_CSM REQ MON` / `SIGDIE` line was caused by a BootROM or CLINT
  device response being misclassified as a CSM/HMC fill. The next root-cause
  capture must trigger on the actual messages-monitor fail condition rather
  than the last high-volume monitor line printed before abort.

## 2026-07-01 -- coh_ipi64 small-VCD compile fix

- Fixed the `coh_ipi64` small-VCD harness compile path for the local Verilator 5
  generated headers. `VlWide<N>` uses an unsigned size template parameter, so
  the helper that extracts 64-bit chunks from raw NoC2/CSM wide words now uses
  `std::size_t N`.
- This is still a diagnostic harness change, not an RTL behavior change. The
  immediate test remains the short `main_time=22000000..24000000` capture of
  TILE36 raw NoC2 flits, CSM `write_val_s2 && !ghid_val_s2`, and BootROM/CLINT
  return flits.

## 2026-07-03 -- FIRST RELIABLE 64-core interactive shell evidence (nproc=64, P3_64CORE_SHELL_READY)

### Board-level layered evidence chain COMPLETE

Programmed Build 68 PDI (DONE bit: HIGH, 4 ILAs) + dbg26 SD image (dbg21 fw + corrected DTB initrd-end 0x90107d9c + XSBench rootfs v2). UART capture (72113 bytes, ev_retry.log, saved at /tmp/uart_64core_shell_evidence.log). The full layer chain:

1. **DONE bit: HIGH** -- programming confirmed.
2. **bootrom** -- `B68 OpenSBI bundle bootrom` + `B68 jump fw=0000000080000000 dtb=0000000088000000`.
3. **OpenSBI** -- banner + SBI TIME/IPI/RFENCE extensions detected.
4. **Linux kernel** -- `Linux version 6.6.0 ... #5 SMP Fri Jun 12 14:31:03 CST 2026`.
5. **64-CPU SMP bringup** -- all 64 harts brought up (calibrated).
6. **Interactive shell** -- init script output: `nproc=64`, `cpuinfo_processors=64`, `=== P3_64CORE_SHELL_READY ===`, `~ #` prompt.

This is the **first one-hand UART log** proving an interactive 64-core shell with `nproc=64`. Prior "64-core shell achieved" claims in older devlog entries were based on expired/truncated captures and are now superseded by this evidence.

### Intermittent early-Linux stall confirmed

Run 1 (same PDI/SD): stalled at `[0.000000] OF: reserved mem: mmode_resv0@80000000` (39517 bytes, zero growth). Run 2 (reprogram): reached the full shell (72113 bytes). The forward-progress stop at Linux `[0.000000]` is **intermittent** -- the same combination sometimes stalls before SMP bringup, sometimes succeeds all the way to the shell.

### Shell interactivity verified

Sent commands via slow per-character UART write; shell echoed them back (garbled due to keep_bootcon double-printing + interleaving). The init script output (`nproc=64` + `cpuinfo_processors=64`) is authoritative and does not depend on interactive UART reliability.

## 2026-07-03 -- 64-thread XSBench ACTUALLY RUNS but HANGS during unionized grid init

### First real XSBench execution on 64-core Linux (not a misattribution)

On the confirmed 64-core shell (this session's P3_64CORE_SHELL_READY), ran `/root/XSBench -s small -l 100000` with 64 threads (OMP_NUM_THREADS=1 did not take effect; XSBench reported `Threads: 64`).

XSBench output summary (from UART log):
- H-M Benchmark Size: small
- Total Nuclides: 68
- Unionized Energy Gridpoints: 768,604
- Particle Histories: 500,000
- Total XS Lookups: 100,000
- Threads: 64
- Est. Memory Usage: 241 MB

### Freeze point: unionized grid initialization -> complete system hang

XSBench progressed through "Intializing nuclide grids..." then started "Intializing unionized grid..." and FROZE. Zero UART growth after that. Shell unresponsive (sent `echo P3_CHECK`; UART echoed the command but no output returned -- the entire Linux system hung, not just XSBench).

The unionized grid init is heavy parallel compute (sorted merge of 768K energy gridpoints across 64 threads). This is consistent with the intermittent Linux forward-progress stop seen during boot (stop_machine deadlock at ~32s, and `[0.000000]` stall) -- heavy SMP compute triggers a system-level hang.

### Next: single-thread XSBench to isolate

Need to reprogram + retry with `OMP_NUM_THREADS=1` actually enforced. If single-thread XSBench completes, the hang is specific to multi-threaded parallel compute (the 64-thread shared-write/sync path).

## 2026-07-03 -- 64-core manual screen capture: boot → shell → XSBench → softirq crash (timer Load Access Fault)

### User manually captured via screen on /dev/ttyUSB0

Reprogrammed Build 68 PDI (DONE bit: HIGH). User watched the full boot on screen. Three outcomes observed across reprogram cycles:

### Outcome 1: intermittent `[0.000000]` stall (reproducible)
First reprogram: boot reached `[0.000000] OF: reserved mem` and stalled (zero UART growth, ~39517 bytes). Consistent with prior runs.

### Outcome 2: full boot to 64-core shell (confirmed again)
Second reprogram: complete boot to `P3_64CORE_SHELL_READY`, `nproc=64`, `~ #` interactive shell (72113+ bytes). Shell responded to commands.

### Outcome 3: XSBench run → system hang
On the confirmed shell, ran `/root/XSBench -s small -l 100000` (Threads: 64, 768K gridpoints, 241 MB). XSBench progressed through nuclide grids init, then froze at "Intializing unionized grid..." — zero UART growth. Shell became unresponsive (`echo P3_CHECK` echoed but no output). Complete system hang during heavy 64-thread parallel compute.

### Outcome 4: long run → softirq Load Access Fault crash (~2772s)
Third reprogram: boot reached shell, ran for ~46 minutes (kernel time ~2772s) before crashing. User captured the crash dump on screen:

```
epc : ffffffff807fa154 ra : ffffffff80014b50 sp : ffffffc800003f60
a7 : 0000000054494d45    (ASCII "TIME")
status: 0000000200000120
badaddr: 00000000000d879b    (corrupted pointer)
cause: 8000000000000005     (Load Access Fault)
[<ffffffff807fa154>] __do_softirq+0x9e/0x25a
[<ffffffff80014b4c>] __irq_exit_rcu+0x8a/0xb4
```

**Analysis**: The crash is in `__do_softirq` called from `__irq_exit_rcu` — a timer/software interrupt handler. `a7=0x54494d45` (ASCII "TIME") suggests the timer SBI ecall path. `badaddr=0xd879b` is a corrupted pointer (not a valid kernel address). `cause=5` (Load Access Fault) means a load from an invalid address during softirq processing.

This is consistent with the stop_machine / forward-progress hang pattern: the system runs for a while, then a timer/IPI interrupt handler hits a corrupted data structure and crashes. The ~2772s survival time (vs ~426s in prior dbg26) confirms the issue is intermittent and timing-dependent.

### Summary of failure modes (all same root cause)

| Failure mode | When | Evidence |
|---|---|---|
| `[0.000000]` stall | boot, before SMP | UART stops at OF: reserved mem |
| stop_machine deadlock | boot, ~32s kernel time | soft lockup CPU#0 stuck 347s |
| XSBench unionized grid hang | heavy 64-thread compute | shell unresponsive, zero growth |
| softirq Load Access Fault | ~2772s after boot | `__do_softirq` crash, `a7=TIME`, `badaddr=0xd879b` |

All point to an intermittent timer/IPI/coherency issue in the 64-core SMP interrupt handling path. The corruption (`badaddr`) suggests a data race or coherency violation in shared kernel data structures accessed during interrupt context.

## 2026-07-03 -- SINGLE-THREAD XSBench also hangs — NOT a parallelism issue

### Critical finding: OMP_NUM_THREADS=1 XSBench still hangs

User manually ran `OMP_NUM_THREADS=1 /root/XSBench -s small -l 100000` on the 64-core shell via picocom. XSBench still hung (same as the 64-thread run). This rules out OpenMP parallelism / multi-thread contention as the trigger.

### Root cause narrowed: timer interrupt crash during heavy compute

The crash data from the manual screen capture (this session):
- `epc: __do_softirq+0x9e` — softirq handler
- `ra: __irq_exit_rcu+0x8e` — called from IRQ exit
- `a7: 0x54494d45` (ASCII "TIME") — SBI timer ecall
- `badaddr: 0xd879b` — corrupted pointer (not a valid kernel address)
- `cause: 5` — Load Access Fault

Even single-threaded, the Linux timer tick (~100 Hz) fires during XSBench's heavy compute. The timer interrupt handler accesses a corrupted data structure → Load Access Fault → system hang/crash. XSBench triggers it because it runs long enough + accesses enough memory for the timer interrupt corruption to manifest. Lightweight commands (echo, ls) don't trigger it because they complete before the timer corruption occurs.

### Implications

- NOT an OpenMP / multi-thread parallelism issue (single-thread also crashes).
- NOT the L1 coherency path (coh_64core PASS; the corruption is in kernel interrupt data, not user-space shared memory).
- IS a timer/IPI interrupt handling corruption in the 64-core SMP kernel. The `a7=TIME` + `badaddr` points to the SBI timer ecall path or the kernel's timer softirq accessing corrupted per-CPU or shared timer data.
- The corruption is intermittent (sometimes the system runs for ~2772s before crashing, sometimes XSBench triggers it sooner).

### Next investigation target

The crash is in `__do_softirq` during timer interrupt processing. Key questions:
1. What data structure at `badaddr=0xd879b` is being accessed? (Likely a corrupted hrtimer/timer_list linked list node.)
2. Is the corruption from a coherency issue on per-CPU timer data (one CPU's timer update not visible to another)?
3. Is it an SBI/OpenSBI timer delivery bug (wrong hart gets the timer interrupt, or timer data races)?
4. Would disabling the timer tick (nohz) or reducing its frequency avoid the crash?

## 2026-07-03 -- OpenSBI timer path deep-dive: code is CORRECT — corruption is in kernel timer DATA

### Crash disassembly analysis

The crash EPC `0xffffffff807fa154` (`__do_softirq+0x9e`) disassembles to:
```asm
ffffffff807fa154:  000d879b    sext.w  a5, s11   # sign-extend register, NOT a load
```

**Key corrections from earlier analysis:**
- `cause = 0x8000000000000005`: bit 63 SET → this is a **S-mode timer INTERRUPT**, not a Load Access Fault (which would be cause=5 without bit 63).
- `badaddr = 0xd879b`: for interrupts, sbadaddr is not updated. This value (= the instruction encoding `0x000d879b`) is **stale/garbage** — the trap frame itself may be corrupted.
- The EPC instruction (`sext.w`) cannot fault — confirming the crash state is corrupted.

### OpenSBI timer code review: all correct

Reviewed `sbi_timer.c`, `aclint_mtimer.c`, `fdt_timer_mtimer.c`:

1. **mtimecmp write**: 64-bit atomic MMIO (`writeq_relaxed`, single 64-bit write). OpenPiton CLINT has `has_64bit_mmio = true`. No split-write race.
2. **Per-hart mtimecmp**: `time_cmp[target_hart - mt->first_hartid]` with `target_hart = current_hartid()` (reads `mhartid` CSR). Correct hart indexing.
3. **Per-hart timer state**: `timer_state` in per-hart scratch, spinlock-protected. No cross-hart data race in OpenSBI's timer management.
4. **STIP forwarding**: `csr_set(CSR_MIP, MIP_STIP)` in the callback. Standard M→S timer delegation.
5. **dbg21 barrier**: runs only on non-IRQ traps (ecalls). Timer interrupts are IRQs → barrier does NOT interfere with timer path.

**Conclusion**: OpenSBI's timer code is standard, well-tested, and correct for 64 harts. No bug found.

### Where the corruption actually is

The crash is in **Linux's kernel timer softirq data structures** (hrtimer bases, timer lists, per-CPU timer data). The corruption propagates from an unknown source into the kernel's linked list / per-CPU data, and the timer interrupt handler crashes when it traverses the corrupted structures.

Possible corruption sources (not yet confirmed):
1. **Subtle coherency corner case**: coh_64core proved basic store→load visibility works, but kernel timer linked lists involve complex pointer-chasing patterns (READ_ONCE/WRITE_ONCE across cache lines, per-CPU locks, cross-CPU timer delegation). A subtle invalidation miss on a specific access pattern could corrupt list nodes.
2. **Kernel SMP bug at 64 cores**: Linux's per-CPU timer management might have a scaling issue (e.g., per-CPU data alignment, TLB pressure, or a race in the timer wheel at high core counts).
3. **NoC/memory subsystem**: intermittent data corruption during heavy 64-core memory traffic (unlikely but not ruled out).

### Next investigation targets (not yet started)
1. Test with fewer cores (2x1, 4x4) — if crash doesn't reproduce, it's 64-core-specific.
2. Add kernel-side instrumentation to the timer softirq (check list integrity before traversal).
3. Try `nohz_full` (disable timer ticks on non-boot cores) to see if the crash goes away.
4. Kernel config: `CONFIG_HZ=100` → `CONFIG_HZ=10` (reduce timer tick frequency) to see if crash frequency changes.

## 2026-07-13 -- BOARD EVIDENCE via ILA: CPU hung internally after console_dev access (no UART = no banner)

Obtained board evidence remotely via ILA capture (no SD swap needed) — the board
was still programmed with Build 73 PDI (debug hub 0x3ffc0000000, 4 ILAs live).
Captured all 4 ILAs to CSV (`/tmp/b73_ila_*.csv`) at 12:46-12:52.

Decoded the p3_tile_debug_bus packing (tile.tmp.v:1022): the 64-bit core_bus64
carries p3_last_l15_address_r[39:24,23:8,7:0] + rqtype + size + L15 handshake
bits + rst/clk. core_bus64 = 0x008004208003f407 decodes to:

- Last L15 (memory) address = 0x80042080 = console_dev + 0x80
  (nm confirms `console_dev` at 0x80042000, a bss symbol). So the CPU's last
  DDR access was inside the OpenSBI console device struct.
- L15 handshake bits (low byte 0x07): transducer_l15_val=0, l15_transducer_val=0,
  l15_transducer_ack=0 => the L1.5<->transducer interface is IDLE. The CPU is
  NOT stalled waiting for a memory/NoC response.

ILA1 counters over 1024-sample window: heartbeat toggles (design clock running),
but core_seen16/uart_seen16/ddr_seen16 are FROZEN (7fff/feff/ef7f constant) =>
no new core/uart/ddr activity. uart_seen frozen => no UART writes after bootrom
=> **no OpenSBI banner printed (board-confirmed)**.

Interpretation (board-evidence root-cause pointer): OpenSBI cold-boot reaches the
console path (accessing console_dev at 0x80042080), then the CPU enters an
INTERNAL stall — not a memory/NoC stall (L15 idle), not a UART LSR poll (last
address is DDR console_dev, not 0xfff0c2c0xx). This is an internal Ariane stall
or a wfi-for-untriggered-interrupt after the console-region access.

This is the first BOARD evidence in the transcript for the 2-core OpenSBI
no-banner hang, and it points the next investigation at the OpenSBI console-init
path (platform_early_init -> generic_early_init -> fdt_serial_init ->
uart8250_device_init) rather than at a memory/NoC fault.

Caveat: the debug bus has no PC, so this localizes the hang to the console PATH
(last mem op) but not to an exact instruction. The exact step still needs the
OpenSBI probe fw (I[SHDMWRTEPB markers), which requires the physical SD card
swap. dbg21 fw also runs console init, so the dbg21-fix hypothesis is now
TESTABLE-PREDICTED-AT-RISK: if the hang is a 2-core-specific console-path
internal stall, dbg21 fw may also hang at the same point.
