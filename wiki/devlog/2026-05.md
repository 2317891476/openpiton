# Dev Log -- 2026-05

Entries are append-only, newest first.

---

## 2026-05-26 -- P3 Build 24 debug XDC flow avoids open_run hang

- Build 24 clean synthesis completed with 0 errors and 0 critical warnings; final `Report BlackBoxes` no longer listed `unread`.
- After synthesis, Vivado hung while `open_run synth_1` tried to reopen the generated VP1902 checkpoint for ILA insertion; a direct `open_checkpoint` attempt showed the same idle behavior after loading the part.
- Updated `scripts/p3_build24_rtl_debug.tcl` to generate the RTL debug ILA XDC directly from explicit top-level debug nets rather than opening the synthesized design and calling `save_constraints`.
- Added `scripts/p3_build24_impl_resume.tcl` so a completed `synth_1` can proceed directly to implementation/PDI generation with the generated debug XDC.
- Next action: run the resume script, then program `p3_top_rtl_debug.pdi` and capture `ila_capture_rtl_debug.csv`.

## 2026-05-26 -- P3 Build 24 unread empty module blackbox fixed

- A clean non-incremental Build 24 synthesis did read `common_cells/src/unread.sv`, but final `Report BlackBoxes` still listed seven `unread` instances.
- Root cause refinement: the upstream Ariane/common_cells `unread` module is intentionally input-only and empty, so Vivado preserves it as an implementation black box even when the file is present.
- Added `piton/design/xilinx/huaprop3/unread_vivado_impl.sv`, a Vivado-specific LUT sink implementation of module `unread`.
- Updated `scripts/p3_build24_rtl_debug.tcl` to remove the empty common_cells `unread.sv` from the project and add the shim before synthesis.
- Next action: rerun Build 24 and confirm final synthesis `Report BlackBoxes` no longer lists `unread`.

## 2026-05-26 -- P3 Build 24 incremental property value fixed

- The first attempt to disable stale `synth_1` incremental synthesis failed before synthesis launch.
- Vivado rejected `set_property AUTO_INCREMENTAL_CHECKPOINT false [get_runs synth_1]` with `bad lexical cast`; this run property expects numeric `0`/`1` rather than Tcl boolean text.
- Updated `scripts/p3_build24_rtl_debug.tcl` to set `AUTO_INCREMENTAL_CHECKPOINT` to `0` and to fail loudly if any required run-property write is rejected.
- Next action: rerun Build 24 and inspect the generated synthesis Tcl for absence of `read_checkpoint -auto_incremental`.

## 2026-05-26 -- P3 Build 24 stale incremental synthesis disabled

- Re-ran Build 24 after adding Ariane `common_cells/src/unread.sv`.
- Synthesis read `unread.sv`, but Vivado auto-incremental synthesis reused the stale imported checkpoint `utils_1/imports/synth_1/p3_top.dcp` at 100% reuse / 0% resynthesis.
- Result: `synth_1/runme.log` still reported seven `unread` black boxes, so the implementation DRC `INBB-3` would not be fixed by source-list changes alone.
- Updated `scripts/p3_build24_rtl_debug.tcl` to disable `synth_1` incremental checkpoint properties and remove the imported checkpoint from the project before launching synthesis.
- Next action: rerun Build 24 and confirm the generated `synth_1/p3_top.tcl` no longer contains `read_checkpoint -auto_incremental`.

## 2026-05-25 -- P3 Build 24 Ariane unread source added

- Build 24 reached implementation but failed in `opt_design` DRC `INBB-3` because seven Ariane frontend instances of module `unread` were left as black boxes.
- Root cause: the current P3 Vivado project did not include Ariane/CVA6 `common_cells/src/unread.sv`, even though `Flist.ariane` lists it and the module exists in the source tree.
- Updated `scripts/p3_build24_rtl_debug.tcl` to explicitly add `piton/design/chip/tile/ariane/common/submodules/common_cells/src/unread.sv` as SystemVerilog before `update_compile_order`.
- Next action: rerun Build 24 and confirm implementation reaches PDI/LTX generation instead of stopping at `opt_design`.

## 2026-05-25 -- P3 Build 24 ILA clock net selection fixed

- Build 24 synthesis completed successfully with the RTL debug buses and Ariane/RV64 macro set enabled.
- The first post-synthesis ILA insertion attempt failed at `connect_debug_port u_ila_0/clk` because `get_nets -hier chipset_clk` matched 242 same-named hierarchical nets and Vivado could not connect them to a single ILA clock port.
- Updated `scripts/p3_build24_rtl_debug.tcl` with a single-net resolver for the ILA clock and probe buses; ambiguous matches now fail with candidate names instead of creating an invalid debug-port connection.
- Next action: rerun Build 24 from the completed synthesis checkpoint path and continue to implementation/PDI generation.

## 2026-05-25 -- P3 Build 24 RTL debug bus implemented

- Added a `P3_RTL_DEBUG` build mode that exports stable RTL debug buses from Ariane/tile/chip/chipset/uart up to `p3_top`, avoiding the unreliable post-synthesis `connect_probe` path used in Build 23.
- New top-level ILA-visible probes cover:
  - chip/core reset and Ariane wake status
  - L15 request/response activity
  - off-chip NoC request/response activity
  - bootrom request/response visibility
  - UART NoC and AXI-lite write visibility
  - DDR AXI address valid/address snapshots
- Added scripts:
  - `scripts/p3_build24_rtl_debug.tcl`
  - `scripts/p3_ila_capture_rtl_debug.tcl`
- Build output target is `huaprop3_openpiton/debug_build/p3_top_rtl_debug.pdi` plus matching `.ltx`.
- Build 24 also forces the Ariane/RV64 Vivado macros (`PITON_ARIANE`, `PITON_RV64_PLATFORM`, `PITON_RV64_DEBUGUNIT`, `PITON_RV64_CLINT`, `PITON_RV64_PLIC`, `WT_DCACHE`) because the current P3 `.xpr` does not visibly carry them even though the generated PyHP RTL is Ariane-oriented.
- Added HUAPROP3 tie-offs for the RISC-V debug-unit JTAG signals inside `system.v`, matching the A7203 approach when `PITON_RV64_DEBUGUNIT` is enabled without external JTAG pins.
- Next action: regenerate PyHP `.tmp.v` files with the HUAPROP3/P3 Ariane environment, run Build 24, program the PDI, and capture `ila_capture_rtl_debug.csv`.

## 2026-05-25 -- P3 UART BD smoke programmed and serial verified

- Programmed BD-path smoke PDI:
  - `huaprop3_uart_bd/p3_uart_bd.runs/impl_1/p3_uart_bd_wrapper.pdi`
  - Target: `xcvp1902_1` over `hw_server 100.93.77.36:3121` and XVC `202.197.4.99:2540`
  - DONE bit: HIGH
- Captured UART for 8 seconds with `scripts/p3_serial.py --capture 8`.
- Result: repeated `BDPATH` lines were observed on `/dev/ttyUSB0` at 115200 8N1.
- Conclusion: the reference-style BD external-port route `uart_txd/uart_rxd` on CW58/CW59 is also functional.
- Combined conclusion from direct + BD smoke tests: both physical UART routing styles are good; the OpenPiton/Ariane silence should stay focused on reset/clock/fetch/bootrom/core activity, not board-level UART wiring.

## 2026-05-25 -- P3 UART direct smoke programmed and serial verified

- Programmed direct-path smoke PDI:
  - `huaprop3_uart_direct/p3_uart_direct.runs/impl_1/p3_uart_direct_top.pdi`
  - Target: `xcvp1902_1` over `hw_server 100.93.77.36:3121` and XVC `202.197.4.99:2540`
  - DONE bit: HIGH
- Captured UART for 8 seconds with `scripts/p3_serial.py --capture 8`.
- Result: repeated `DIRECT` lines were observed on `/dev/ttyUSB0` at 115200 8N1.
- Conclusion: the current-style top-level direct `uart_tx/uart_rx` physical route on CW58/CW59 is functional.

## 2026-05-25 -- P3 UART BD smoke PDI generated successfully

- `scripts/p3_uart_bd_build.tcl` completed after setting the BD file `synth_checkpoint_mode` to `None`.
- Generated and copied PDI:
  - `huaprop3_uart_bd/p3_uart_bd.runs/impl_1/p3_uart_bd_wrapper.pdi`
  - Size: 6.7 MB
- Implementation status:
  - `write_device_image Complete!`
  - Route status: 92/92 routable nets fully routed, 0 routing errors
  - Timing summary: WNS 7.603 ns, TNS 0.000 ns, WHS 0.100 ns, THS 0.000 ns
  - Utilization: 52 CLB LUTs, 58 CLB registers, 1 `IBUFDS`, 1 `BUFGCE`, 3 `OBUF`
- Key flow lesson: the BD smoke test must use inline BD/module-ref synthesis on this Windows/WSL Vivado setup; the default module-ref OOC DCP path can trip an internal `ps_wizard` generation failure.

## 2026-05-25 -- P3 UART BD smoke flow switched to inline BD synthesis

- Updated `scripts/p3_uart_bd_build.tcl` after the module-ref/OOC BD run failed in `ps_wizard` generation.
- The BD file now sets `synth_checkpoint_mode None` before target generation and wrapper creation.
- Purpose: keep the BD external ports `uart_txd/uart_rxd`, but avoid generating a separate module-ref OOC DCP/run for `p3_uart_bd_printer`; this should make the implementation path closer to the direct smoke test that already generated a PDI.

## 2026-05-25 -- P3 UART BD smoke implementation fails in ps_wizard generation

- Ran `scripts/p3_uart_bd_build.tcl` with the minimal BD/module-ref UART path and Windows `%TEMP%` project/run directory.
- BD creation, validation, wrapper generation, module-ref OOC synthesis, and top-level synthesis all completed.
- `impl_1` failed during `opt_design` while generating the Versal `ps_wizard` support cell:
  - `Could not create slave interpreter 'rodin:slave23'`
  - `Failed to generate IP 'ps_wizard/inst/pmcps_0'`
  - `Failed to add ps_wizard instance in design`
- This is distinct from the earlier BD Tcl API failures: the BD itself is buildable, but the module-ref/OOC BD flow adds enough Vivado IP-generation complexity to trip an internal Versal `ps_wizard` failure. Next attempt should avoid OOC/module-ref synthesis for the BD smoke test if possible.

## 2026-05-25 -- P3 UART direct smoke PDI generated successfully

- `scripts/p3_uart_direct_build.tcl` completed after moving Vivado project/run directories to Windows `%TEMP%`.
- Generated and copied PDI:
  - `huaprop3_uart_direct/p3_uart_direct.runs/impl_1/p3_uart_direct_top.pdi`
  - Size: 6.7 MB
- Implementation status:
  - `write_device_image Complete!`
  - Route status: 94/94 routable nets fully routed, 0 routing errors
  - Timing summary: WNS 7.242 ns, TNS 0.000 ns, WHS 0.106 ns, THS 0.000 ns
  - Utilization: 51 CLB LUTs, 58 CLB registers, 1 `IBUFDS`, 1 `BUFGCE`, 3 `OBUF`
- Next action: build the BD-externalized `uart_txd/uart_rxd` smoke PDI and then program/capture both variants.

## 2026-05-25 -- P3 UART smoke builds moved Vivado run dirs to Windows local temp

- Updated the UART smoke-test build scripts again after the single-process flow failed in Versal `ps_wizard` IP generation.
- New approach: keep the standard Vivado project-mode `launch_runs ... -to_step write_device_image` flow, but create the Vivado project and `.runs` directories under Windows `%TEMP%` instead of the WSL-mounted repository path.
- Each script copies the generated PDI back to the repository output directory after a successful build:
  - Direct: `huaprop3_uart_direct/p3_uart_direct.runs/impl_1/p3_uart_direct_top.pdi`
  - BD: `huaprop3_uart_bd/p3_uart_bd.runs/impl_1/p3_uart_bd_wrapper.pdi`
- Purpose: preserve the proven Versal project-mode device-image path while avoiding the WSL network-path `rundef.js` failure.

## 2026-05-25 -- P3 UART direct single-process flow fails in Versal ps_wizard generation

- Retried `scripts/p3_uart_direct_build.tcl` with the single-process Vivado flow.
- Result: RTL synthesis again completed successfully; the UART smoke RTL still has no synthesis/elaboration issue.
- New failure: `opt_design` failed while Vivado attempted to auto-add/generate the Versal `ps_wizard` support cell for device-image generation:
  - `Could not create slave interpreter 'rodin:slave27'`
  - `Failed to generate IP 'ps_wizard/inst/pmcps_1'`
  - `Failed to add ps_wizard instance in design`
- This shows the single-process workaround avoids `rundef.js`, but it exposes a different Vivado internal IP-generation failure. Next build-flow attempt should align more closely with the previously successful minimal UART project-mode run rather than keeping all implementation steps in the top-level Tcl interpreter.

## 2026-05-25 -- P3 UART smoke builds switched to single-process Vivado flow

- Updated `scripts/p3_uart_direct_build.tcl` and `scripts/p3_uart_bd_build.tcl` to avoid `launch_runs` for the small UART smoke tests.
- The scripts now run `synth_design`, `opt_design`, `place_design`, `phys_opt_design`, `route_design`, and `write_device_image` in the same Vivado batch process, while preserving the expected PDI output paths under `.runs/impl_1/`.
- Purpose: bypass the Windows/WSL `rundef.js` launcher failure seen in the direct smoke test's `impl_1` run, without changing the UART test RTL or pin mapping.

## 2026-05-25 -- P3 UART direct smoke build hit Vivado rundef.js path error

- Ran `scripts/p3_uart_direct_build.tcl` after simplifying the direct smoke test to pure RTL plus `IBUFDS`.
- Result: `synth_1` completed successfully with 0 errors, 0 critical warnings, and 1 warning; the synthesized design included one `IBUFDS`, one `BUFG`, three output buffers, and the small UART printer logic.
- `impl_1` did not reach place/route. The Vivado generated run launcher failed during `init_design` with `rundef.js(196, 3) JavaScript runtime error: An unexpected network error occurred.` and left `.init_design.error.rst`.
- This is a Windows/WSL Vivado run-launcher path issue, not an RTL elaboration failure. Next fix is to avoid `launch_runs` for the smoke tests and use a single-process Tcl flow (`synth_design`, `opt_design`, `place_design`, `route_design`, `write_device_image`) inside the already-running Vivado session.

## 2026-05-25 -- P3 UART smoke tests simplified to pure-RTL clocking

- Updated the direct UART smoke-test flow to remove the temporary Clock Wizard/proc_sys_reset BD; the direct test now instantiates only top-level RTL, buffers the P3 100 MHz differential system clock with `IBUFDS`, and drives `uart_tx/uart_rx` directly.
- Updated the BD UART smoke-test flow to keep the reference-style BD external ports `uart_txd/uart_rxd`, but the BD now contains only a simple module reference and no OOC-heavy clock/reset IP.
- Added a shared `p3_uart_clock_in` helper so both smoke tests use the same CE6/CF6 100 MHz clock path and 115200 baud divisor.
- Rationale: the previous Clock Wizard-based direct build stalled during Vivado OOC IP synthesis, so these smoke tests should isolate UART routing without introducing unrelated IP generation risk.

## 2026-05-25 -- P3 UART direct-vs-BD smoke tests added

- Added two isolated UART smoke-test build flows to distinguish top-level direct UART routing from BD-externalized UART routing:
  - Direct path: top-level `uart_tx/uart_rx`, expected serial text `DIRECT`.
  - BD path: BD external ports `uart_txd/uart_rxd`, expected serial text `BDPATH`.
- Added shared small RTL under `piton/design/xilinx/huaprop3/` plus minimal XDC files using the known P3 pins:
  - TX = CW58, RX = CW59, clock = CE6/CF6, reset = A6, LEDs = CF60/CF59.
- Added `scripts/p3_program_pdi.tcl` so a specific smoke-test PDI can be programmed without editing the OpenPiton programming script.
- Purpose: verify whether the remaining OpenPiton no-UART symptom is related to `p3_top` direct UART wiring versus the reference project's BD external-port style.

## 2026-05-25 -- P3 migration: UART path cleared, Ariane startup still silent

- Current reliable hardware status:
  - Remote Ubuntu / Vivado Lab access works through Tailscale; `hw_server` is reachable on port 3121 and XVC is `202.197.4.99:2540`.
  - PDI programming path remains usable; `huaprop3_openpiton/debug_build/p3_top_debug.pdi` and `.ltx` were regenerated on 2026-05-25.
  - ILA snapshot `huaprop3_openpiton/debug_build/ila_capture.csv` shows `leds[1:0]=3`, `uart_tx=1`, `uart_rx=1`, `reset_IBUF=0`, and no AXI valid activity across the 4096-sample window.
- UART hardware is no longer the lead suspect:
  - A minimal pure-Verilog UART HELLO image was built under `huaprop3_minimal/` and proved the CW58/CW59 pinout, FTDI path, ttyUSB path, and basic 30 MHz clock/reset path.
  - The OpenPiton image still has no UART output, so the next debug target is Ariane reset/clock/fetch visibility, not board-level UART wiring.
- Debug-flow lessons:
  - Test A/Test B BD comparison is not closed; Vivado BD Tcl API failures make those tests non-authoritative.
  - Build 23's post-synthesis `connect_probe` approach for `chip_rst_n` was unreliable even with `mark_debug`; future ILAs should expose stable debug ports at RTL/top level or probe already-known OBUF/IBUF/AXI nets.
- Next action:
  - Build a deterministic Ariane bring-up probe image with RTL-level exported debug signals for `chip_rst_n`, core clock/reset state, and first NoC/bootrom request activity, then capture after programming.

## 2026-05-23 -- P3 migration: Serial debug setup, devlog rule strengthened

- Set up serial debugging toolchain for P3 board via remote machine (100.93.77.36):
  - Installed `pexpect` Python library for SSH password automation (sshpass unavailable)
  - Remote machine has `picocom` at /usr/bin/picocom, FTDI dual RS232 (ttyUSB0/ttyUSB1)
  - ttyUSB0 = P3 UART (115200 8N1), ttyUSB1 = XVC JTAG (for hw_server)
  - picocom connected successfully but NO serial output detected on ttyUSB0
- Strengthened R1 wiki sync rule in CLAUDE.md:
  - Added R1-A: Devlog DAILY Rule — mandatory devlog entry every calendar day with FPGA work
  - Added R1-C anti-patterns: "I'll write the devlog after this build finishes", "crossing day boundary without entry"
  - Added R1-D: entries should include build numbers, error codes, root cause, PDI paths
  - Trigger: 15+ builds across 3 days with zero devlog entries
- Started writing comprehensive devlog entries for May 21-23 (backfill)

## 2026-05-23 -- P3 migration Build 19: Correct reset fix — chipset alive!

- **Chipset reset fix VERIFIED**: Build 19 programmed, ILA capture shows:
  - `leds[1:0]=3` (both ON) → `init_calib_complete=1`, `chipset_rst_n_ff=1` ✅
  - Previously (Build 15-18): `leds=0` (chipset stuck in reset)
  - `m_axi_bvalid` changed from stuck-1 (Build 18) to 0 (properly reset) ✅
- Fix summary: peripheral_aresetn passed directly (no inverter) + PITON_FPGA_RST_ACT_HIGH removed for HUAPROP3
  - Both system.v and chipset.v no longer invert the reset
  - WSL cache: p3_top.v and piton_system.vh swapped from /tmp copies
- **New issue**: No UART output, no AXI DDR activity despite chipset being alive
  - Chipset reset released but Ariane core may still be waiting for something
  - Possible: core reset separate from chipset reset, clock issue, or DDR dependency
- Next: diagnose Ariane core startup, add probe for core reset/clock
- PDI: `debug_build/p3_top_debug.pdi` (8.9 MB), LTX: `debug_build/p3_top_debug.ltx`

- **Root cause identified (3rd iteration)**: The reset chain has TWO `PITON_FPGA_RST_ACT_HIGH` inversions:
  1. `system.v`: `sys_rst_n_rect = ~sys_rst_n`
  2. `chipset.v`: `rst_n_rect = ~rst_n` (where rst_n = chipset_rst_n from system)
  - `peripheral_aresetn` from BD is active-LOW (1 = running)
  - Previous fix (Build 17-18): added `~` inverter at p3_top got canceled by system.v, but chipset.v inverted again → `rst_n_rect = 0` → chipset stuck in reset
  - **Correct fix**: Remove `PITON_FPGA_RST_ACT_HIGH` for HUAPROP3 (so NEITHER module inverts) AND pass `peripheral_aresetn` directly (no inverter at p3_top)
- Build 19 script: `scripts/p3_build19_rst_fix2.tcl`
  - WSL cache workaround: both `p3_top.v` and `piton_system.vh` swapped from /tmp copies
  - 9 ILA probes (chipset_clk, uart_tx/rx, reset_IBUF, leds[1:0], AXIx4)
  - Expected: leds[0]=1, bootrom prints UART banner, AXI DDR activity

## 2026-05-23 -- P3 migration Build 18: Reset fix + ILA (v4, passed)

- Build 18 v4: `scripts/p3_build18_ila_rst.tcl`
  - p3_top.v with `~peripheral_aresetn` inverter (intended to cancel PITON_FPGA_RST_ACT_HIGH)
  - 9 ILA probes all connected cleanly (no driverless nets)
  - route_design passed (0 DRC errors) but "Failed Timing" (non-fatal, chipset_clk only 30 MHz)
  - PDI: `debug_build/p3_top_debug.pdi` (8.9 MB), LTX: `debug_build/p3_top_debug.ltx`
  - Programmed: DONE bit HIGH
  - **ILA capture result**: leds[1:0]=0, uart_tx=1 (idle), no AXI activity, chipset_clk toggling
  - **Still no serial output** — the inverter approach was wrong (see Build 19 analysis)

## 2026-05-23 -- P3 migration Build 18 (v1-v3): ILA probe connection struggles

- Build 18 v1: `peripheral_aresetn` net not found (optimized away due to `~` inverter)
- Build 18 v2: added hierarchical net search, but `create_debug_port` called for probe0 created extra probe11 → driverless net errors
- Build 18 v3: fixed probe0 handling but probe0 and probe6 still driverless (hierarchical search matched wrong/internal nets)
- Key lesson: `mark_debug` before synthesis is more reliable than post-synthesis net name search; preferred approach is probing known OBUF/IBUF nets

## 2026-05-22 -- P3 migration Build 17: Reset inversion at p3_top level (attempt 1)

- Alternative fix attempt: `~peripheral_aresetn` passed to sys_rst_n at p3_top level
- Stale ILA debug XDC constraints from previous build caused route_design driverless net errors
- Removed old `p3_top_debug.xdc` from constrs_1 and rebuilt
- **Serial test: no output on ttyUSB0** — fix didn't work
- Root cause (later analysis): WSL caching prevented Vivado from reading the updated `/tmp/p3_top.v`

## 2026-05-22 -- P3 migration Build 16: piton_system.vh WSL cache workaround

- Copied updated `piton_system.vh` (HUAPROP3 removed from PITON_FPGA_RST_ACT_HIGH) to /tmp
- Swapped the file in project as global include
- PDI generated and programmed
- **ILA capture: identical values to Build 15** — fix not picked up
- Root cause: `piton_system.vh` is `include`d within RTL source files, not resolved from project fileset — file swap approach doesn't help for include files

## 2026-05-22 -- P3 migration Build 15: piton_system.vh reset fix (failed via WSL cache)

- **Root cause discovered via ILA**: `PITON_FPGA_RST_ACT_HIGH` defined for HUAPROP3 in `piton_system.vh:142`
  - P3 BD provides `peripheral_aresetn` (active-LOW, high=running)
  - With `PITON_FPGA_RST_ACT_HIGH`: `rst_n_rect = ~rst_n = ~1 = 0` → chipset permanently in reset
  - `init_calib_complete_out = 0` because `ui_clk_sync_rst = ~chipset_rst_n = ~0 = 1`
  - This explains ALL symptoms: no UART, no AXI, LEDs=0
- Fix: remove `elsif HUAPROP3_BOARD` from `PITON_FPGA_RST_ACT_HIGH` ifdef chain in piton_system.vh
- Added safety net: `assign init_calib_complete = 1'b1` when `ifndef PITON_FPGA_MC_DDR3`
- Build 15 ran but ILA showed no change — WSL filesystem caching prevented Vivado from reading updated include file

## 2026-05-22 -- P3 ILA debug build: project-mode flow, capture reveals reset bug

- Created `scripts/p3_add_ila.tcl` v3 (project mode):
  - `open_project` → `open_run synth_1` → `create_debug_core` → save → `launch_runs impl_1` → PDI
  - 12 ILA probes: peripheral_aresetn, reset_IBUF, uart_tx/rx, sd_clk/cmd/cd, AXIx4, leds
  - Fixed `write_debug_probes` pre-implementation error with `catch {}`
- ILA immediate capture (4096 samples at 30 MHz):
  - `peripheral_aresetn=1` (BD reset released)
  - `reset_IBUF=0` (button not pressed)
  - `uart_tx=1` (idle — **no bootrom activity**)
  - `sd_clk_out=0` (not running)
  - `m_axi_awvalid/arvalid/rvalid=0` — no DDR access at all
  - `leds=0` — `init_calib_complete` stuck at 0
- Created `scripts/p3_ila_capture.tcl` for automated ILA snapshot capture

## 2026-05-21 -- P3 Build 14 successful, first remote programming via XVC

- Build 14: fixed LED port width (4→2 bits, P3 only has 2 user LEDs)
  - Changed `system.v`, `chipset.v`, `p3_top.v`: `output [1:0] leds`
  - Removed unused `leds[2]`/`leds[3]` assigns in chipset.v
  - DRC passed, PDI generated successfully
- Remote programming setup:
  - hw_server at 100.93.77.36:3121, XVC target at 202.197.4.99:2540
  - Devices found: arm_dap_0, xcvp1902_1
  - Programmed successfully: DONE bit HIGH
  - PDI: `huaprop3_openpiton.runs/impl_1/p3_top.pdi` (~8.5 MB)
- **No serial output from P3** — first indication of the reset bug
- User requested ILA debug method to diagnose lack of serial output

## 2026-05-21 -- P3 Build 13b: DRC failures from LED port mismatch

- DRC errors: NSTD-2 (missing IOSTANDARD), UCIO-1 (missing LOC) for leds[3:2]
- P3 board only has 2 LEDs (CF60, CF59) but RTL had 4-bit port
- Fixed by reducing LED port width to [1:0] across all layers

## 2026-05-20 -- P3 migration Phase 6: BD creation validated in Vivado

- Successfully ran `scripts/p3_create_bd.tcl` in Vivado 2024.2 batch mode
  - Block Design `openpiton_top` created and validated without errors
  - HDL wrapper generated: `openpiton_top_wrapper.v`
  - Project: `huaprop3_openpiton/huaprop3_openpiton.xpr`
- Fixed 7 issues during iterative BD debugging:
  1. `clk_wizard_0/locked` pin not found → Versal clk_wizard has no locked output; used xlconstant VCC for proc_sys_reset dcm_locked
  2. `BOARD` variable missing → Set before sourcing rtl_setup.tcl
  3. `verilog_include_dirs` invalid property → Vivado 2024.2 uses `include_dirs`
  4. `module_ref` invalid type → Vivado 2024.2 uses `-type MODULE`
  5. `mc_define.h` not found by MODULE scanner → Added all headers as `Verilog Header` file_type with `is_global_include=1` (matching protosyn gen_project.tcl pattern)
  6. Address map error (0x80000000 out of DDR_LOW0 aperture) → Fixed to offset 0x0 (storage_addr_trans_unified subtracts 0x80000000 before AXI output)
  7. Reset polarity mismatch + AXI clock not associated → Used `peripheral_aresetn` (active-low) and set `CONFIG.ASSOCIATED_BUSIF`/`CONFIG.FREQ_HZ` on chipset_clk
- Non-fatal warnings (expected, safe to ignore for synthesis):
  - `cross_module.tmp.h` not found (SPARC-only monitoring, guarded by `PITON_CORE0_INST_DONE` ifdef)
  - 5 missing Ariane dead-reference files (axi_master_connect*.sv, reg_intf_pkg.sv — unused)
- Key insight: Vivado MODULE reference scanner traces full hierarchy and requires ALL headers in the project with correct file_type, not just include_dirs
- Remaining: synthesis test, debug any elaboration errors, implementation, program FPGA

---

## 2026-05-20 -- P3 migration Phase 5: BD wrapper, pyhp, AXI connection

- Created `openpiton_wrapper.v` (`piton/design/xilinx/huaprop3/`):
  - Thin wrapper around `system.v` exposing only HUAPROP3-active ports
  - AXI4 master (512-bit data, 64-bit addr, 6-bit ID, 11-bit user)
  - Clocks: chipset_clk + sd_sys_clk from BD Clock Wizard
  - mc_clk tied to chipset_clk (same 30 MHz)
  - Reset, UART, SD card (with P3 vsd_en/sd_sel/sd_resetn), LEDs[3:0]
- Fixed system.v switch/LED ifdef chains:
  - Added `HUAPROP3_BOARD` to switch exclusion (no switches on P3 daughter card)
  - Added `HUAPROP3_BOARD` to LED width chain (4-bit, matching chipset.v)
  - Fixed chipset instantiation `.sw()` exclusion (added `ifndef HUAPROP3_BOARD`)
- Integrated `pyhp_preprocess.tcl` into BD script:
  - Sources protosyn's `pyhp_preprocess` proc to resolve `.pyv` → `.tmp.v`
  - Uses pre-existing `.tmp.v` files (92 files from previous protosyn run)
- Updated `p3_create_bd.tcl` to full 16-section automated BD creation:
  - Section 13: Adds openpiton_wrapper.v, creates `module_ref` cell in BD
  - Section 14: Connects wrapper to Clock Wizard, proc_sys_reset, AXI NoC, external IO
  - Section 15: Assigns DDR address map (0x80000000, 2 GB → DDR_LOW0)
  - Section 16: Validates BD, generates HDL wrapper, updates compile order
  - ddr_ready tied to VCC (DDRMC calibration internal to AXI NoC)
  - AXI interface connected via `connect_bd_intf_net` (auto-inferred from signal naming)
- Remaining: run BD creation in Vivado, synthesis test, debug any width mismatches

---

## 2026-05-20 -- P3 migration Phase 4: BBL, DDR4 constraints, BD RTL integration

- Generated `embedded_dtb.h` from P3 DTB (`build/huaprop3/huaprop3.dtb`, 1667 bytes)
- Built BBL for P3: configured riscv-pk with `--host=riscv64-linux-gnu`, `--with-payload=payload.bin`
  - Required `-fno-stack-protector -U_FORTIFY_SOURCE -D_FORTIFY_SOURCE=0` in Makefile CFLAGS (same as AX7203)
  - BBL ELF: 14.2 MB, embedded_dtb at 0x80006630, payload at 0x80200000
  - Binary: `build/huaprop3/bbl.bin` (14.2 MB, ready for SD card at sector 2048)
- Added DDR4 SODIMM pin constraints to `constraints.xdc`:
  - Memory ref clock: CP83/CP82 (LVDS15, separate from system clock)
  - DDR4 address ADR[16:0], BG[1:0], BA[1:0]
  - DDR4 control: RESET_N, ACT_N, CK[1:0], CKE[1:0], CS_N[1:0], ODT[1:0], PAR
  - DDR4 data: DQ[71:0] (72-bit with ECC), DQS[8:0], DM_N[8:0], ALERT_N
  - All pins from reference project shell.xdc (proven working)
  - No IOSTANDARD on DDR4 pins (DDRMC IP handles I/O standards internally)
- Updated `p3_create_bd.tcl` with RTL source integration:
  - Sources `rtl_setup.tcl` (canonical protosyn file list) for all Verilog files
  - Sources `board.tcl` for HUAPROP3_BOARD macro
  - Sets verilog_include_dirs and verilog_define from protosyn infrastructure
  - Iterates SYSTEM/CHIP/CHIPSET_RTL_IMPL_FILES, adds existing files, warns on missing pyv-generated files
- Remaining: pyhp preprocessing, BD wrapper module, AXI4 port connection, synthesis test

---

## 2026-05-20 -- P3 migration Phase 3: system.v, BD script, bootrom

- Fixed `system.v` (FPGA top-level): replaced all `ifndef F1_BOARD` DDR guards with `ifndef PITONSYS_AXI4_MEM` (port declarations + chipset instantiation)
- Added HUAPROP3 to system.v SPI section (sd_reset exclusion, sd_vsd_en/sd_sel/sd_resetn ports)
- Added HUAPROP3 to JTAG debug unit exclusion chain (no JTAG pins on P3 daughter card)
- All three RTL layers now use consistent `PITONSYS_AXI4_MEM` ifdef: chipset.v, chipset_impl.v.pyv, system.v
- Created `scripts/p3_create_bd.tcl`: Vivado Block Design automation script
  - Clock Wizard: 100 MHz diff → 30 MHz chipset_clk + 30 MHz sd_sys_clk
  - AXI NoC: DDR_LOW0 region (covers OpenPiton's 0x80000000 DDR base)
  - DDRMC: DDR4-2933Y, 72-bit (ECC), dual-rank SODIMM
  - ps_wizard, proc_sys_reset (from reference project config)
- AXI width note: OpenPiton noc_axi4_bridge = 512-bit data / 64-bit addr / 6-bit ID; reference Chipyard = 64/36/4. AXI NoC handles width conversion internally.
- Compiled `build/huaprop3/huaprop3.dtb` from DTS (2 GB DDR4, all addresses verified)
- Rebuilt bootrom (`bootrom_linux.sv`) with P3 DTB using riscv64-unknown-elf-gcc 7.2.0
- Remaining: BBL rebuild, run BD creation in Vivado, synthesis test

---

## 2026-05-20 -- P3 migration RTL implementation (Phase 2: ifdef cleanup + DTS)

- Fixed ifdef nesting bugs in chipset.v and chipset_impl.v.pyv:
  - Replaced stacked `ifndef F1_BOARD / ifndef HUAPROP3_BOARD` with single `ifndef PITONSYS_AXI4_MEM` for DDR port declarations and chipset_impl instantiation
  - This is a semantic improvement: AXI4 vs MIG is a memory interface choice, not a board identity check
- `piton_system.vh`: Added `undef PITON_CHIPSET_CLKS_GEN` for HUAPROP3
  - Block Design flow provides clocks externally (Clock Wizard IP in BD, not MMCM in RTL)
  - chipset_clk, mc_clk, sd_sys_clk become input ports (correct for BD wrapper)
  - clk_locked defaults to 1'b1 (Clock Wizard locked is handled in BD reset logic)
- Removed broken `ifndef HUAPROP3_BOARD` wrapper around MMCM instantiation in chipset.v
- Added HUAPROP3 to switch/LED port width ifdefs (no switches, 4-bit leds)
- LED[0]=init_calib_complete, LED[1]=chipset_rst_n_ff (most useful debug signals for bring-up)
- Created `build/huaprop3/huaprop3.dts`: device tree for P3, 2 GB DDR4, all addresses match devices_ariane.xml
- Remaining: Vivado Block Design creation, bootrom/BBL rebuild with new DTB, synthesis test

---

## 2026-05-20 -- P3 migration RTL implementation (Phase 1)

- Registered `huaprop3` as new OpenPiton board (Versal VP1902, xcvp1902-vsva6865-1MP-e-S)
- `piton_system.vh`: Added HUAPROP3_BOARD to 3 ifdef chains (diff clock, active-HIGH reset, DDR4)
- `chipset.v`: Added 6 HUAPROP3_BOARD ifdef blocks:
  - SD port declarations with VSD_EN/SD_SEL/SD_RESET# outputs
  - uart_boot_en=0 for SD boot
  - Behavioral ODDR replacement (Versal has no ODDR primitive)
  - LED assignments, noc_power_test_hop_count
- `mc_define.h`: DDR4 parameters matching VCU118 (2 GB, 64-bit)
- New board files: `piton/design/xilinx/huaprop3/` (devices_ariane.xml, devices.xml, constraints.xdc)
- Pin assignments from reference project shell.xdc (PHC3/Bank 705, all LVCMOS15)
- Board registration: board.list, block.list (30 MHz, 2048 MB), board.tcl, fpga_lib.py
- Deferred to next phase: Vivado Block Design, Clock Wizard IP, DTS/Bootrom/BBL

---

## 2026-05-20 -- P3 FPGA scripts created (build/program/debug)

- Created `scripts/p3_build_bitstream.tcl`: synthesis → implementation → PDI generation
- Created `scripts/p3_program.tcl`: program VP1902 via remote hw_server at 100.93.77.36:3121
- Created `scripts/p3_debug.tcl`: ILA debug session with probe loading, interactive TCL console
- Updated CLAUDE.md with P3 programming section and remote hw_server details
- Updated wiki/concepts/vivado-tooling.md with P3 build flow and script table

---

## 2026-05-20 -- P3 migration guide created

- Created `wiki/concepts/p3-migration-guide.md`: comprehensive 10-section migration document
- Covers 7-layer analysis, critical failure points, RTL change list, bring-up sequence
- Key decisions: PHC3 (Bank 705) for daughter card, Block Design flow first
- Critical issues identified: ODDR replacement, DDRMC integration, SD voltage control signals
- Updated architecture-evolution.md and resource-estimation.md with VP1902 platform info

---

## 2026-05-20 -- Vivado upgraded to 2024.2 on D:\Xilinx

- WSL wrapper (`/home/illya/bin/vivado`) updated: `E:\Xilinx\Vivado\2023.2` → `D:\Xilinx\Vivado\2024.2`
- CLAUDE.md and wiki/concepts/vivado-tooling.md updated to reflect new path and version

---

## 2026-05-20 -- Wiki initialized, project goal formalized

- Created wiki structure for the Quicksilver-on-1000-core project
- Deployment path: single-core → incremental scale-up → kilo-core
- R1 rule (wiki sync) added to CLAUDE.md and AGENTS.md
- Current status: single-core AX7203 boots Linux, SD + UART working
