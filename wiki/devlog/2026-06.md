# Dev Log -- 2026-06

Entries are append-only, newest first.

---

## 2026-06-13 -- Build 69 route_design completes

- Remote Build 69 `route_design` completed its routing task successfully with 0 failed nets, 0 unrouted nets, 0 partially routed nets, and 0 node overlaps.
- Final routing utilization was 21.1712% vertical and 19.8515% horizontal. The route task took about 2:14:13 elapsed and peaked around 111.4 GiB.
- The last intermediate setup result was `WNS=8.850 ns`, while hold repair was still reported as `WHS=-0.207 ns`, `THS=-0.374 ns` before final timing analysis. Routed checkpoint/report generation, final timing signoff, PDI/LTX generation, retrieval, and board UART testing remain pending.

## 2026-06-13 -- Build 69 phys_opt completes and routing starts

- Remote Build 69 `phys_opt_design` completed successfully with 0 errors. Estimated setup timing was `WNS=9.262 ns`, `TNS=0`, so Vivado skipped setup optimizations and left the placed netlist unchanged.
- `route_design` then started with the Advanced Flow feature set and a maximum of 32 CPUs. At the 09:33 CST checkpoint it had completed initial routing and entered `Phase 5.1 Global Iteration 0`.
- Global routing reported localized SLL pressure rather than a hard failure: the largest listed bidirectional region was `1.326` across the SLR0/SLR1 cut, with other over-capacity regions up to about `1.073`. Final route status, timing, PDI/LTX generation, retrieval, and board UART testing remain pending.

## 2026-06-13 -- Build 69 place_design completes

- Remote Build 69 `place_design` completed successfully at 07:37 CST with 0 errors. The place step reported 241 infos, 170 warnings, 2 critical warnings, and took 1:39:46 elapsed.
- Peak memory during placement was about 90.6 GiB. The final post-placement congestion table still showed route pressure, with the highest observed per-SLR estimate around `1.06` in SLR3 east short congestion and `1.04` in SLR1 west long congestion.
- This clears the first major 64-core physical-implementation gate for Build 69, but it is not a routed/PDI result. Route, timing reports, PDI/LTX generation, artifact retrieval, and board UART testing remain pending.

## 2026-06-13 -- Build 69 global placement reaches detailed placement

- Remote Build 69 `place_design` passed floorplanning and global placement by 06:54 CST and entered detailed placement. No placement error had been reported at the checkpoint.
- The global placement summary reported total estimated congestion peaks around 1.02 east long congestion and 1.01 east short congestion, with per-SLR peaks up to about 1.03. This is still a pressure point for route, but it is not an immediate global-placement failure.
- Memory peaked around 88.8 GiB during global placement. Detailed placement, route, PDI/LTX generation, artifact retrieval, and board UART testing remain pending.

## 2026-06-13 -- Build 69 opt_design completes

- Remote Build 69 `impl_1` completed `opt_design` at 05:41 CST with 0 errors. The step reported 107 infos, 170 warnings, 2 critical warnings, and took 36:04 elapsed with a peak around 36.1 GiB during `opt_design`.
- The NoC compiler inside `opt_design` finished successfully, and the follow-up `report_drc` completed with the DRC output written under the Build 69 `impl_1` run directory.
- Vivado was writing post-opt timing/physical databases at this checkpoint. Place, route, PDI/LTX generation, artifact retrieval, and board UART testing remain pending.

## 2026-06-13 -- Build 69 implementation reaches opt_design

- Remote Build 69 `impl_1` passed `link_design` at 05:11 CST with 0 errors. The link step reported 39 infos, 32 warnings, 1 critical warning, and completed after 24:53 elapsed.
- The implementation run then entered `opt_design`, acquired the VP1902 implementation license, and completed the pre-optimization DRC with 0 errors.
- At this checkpoint `opt_design` was still active in the MIG/Advanced IO and debug-core generation phases. Place, route, PDI/LTX generation, artifact retrieval, and board UART testing remain pending.

## 2026-06-13 -- Build 69 64-core diagnostic passes synth_design

- Remote Build 69 on the offline Ubuntu Vivado 2024.2.2 host reached the main 8x8 `synth_design` gate. `synth_1/runme.log` reported `Synthesis finished with 0 errors, 0 critical warnings and 37740 warnings`.
- The synthesized design report listed design size `8009722`, raw LUT primitives `4,820,410` (`LUT1` through both `LUT6` rows), FD-style registers `2,549,221`, `RAMB36E5_INT=4805`, `RAMB18E5_INT=64`, `URAM288E5=128`, and DSP primitive groups at `1153`.
- The full `synth_1` run then completed `write_checkpoint` and generated `p3_top_utilization_synth.rpt` at 04:32:09 CST. Formal utilization is `CLB LUTs=4,424,784`, `Registers=2,553,637`, `Block RAM Tile=4,837`, `URAM=128`, and `DSP Slices=1,153`.
- `impl_1` started at 04:32:19 CST and reached `link_design`; route, `write_device_image`, PDI/LTX retrieval, and board UART testing remain pending.

## 2026-06-13 -- 64-core no-UART debug escalation rule fixed

- Set the next Build 68/69 board-test rule: UART silence is a boot finding only after `program_hw_devices` completes and Vivado prints `DONE bit: HIGH`; otherwise it remains a programming/XVC/session gate.
- If a completed Build 69 programming run still produces 0 UART bytes, the next action is not another identical rerun. Capture Build 69 ILAs if possible, then generate a narrower remote diagnostic PDI.
- The diagnostic order is fixed as UART, DDR, SPI-SD/GPT/`P3OS` bundle, then core release/OpenSBI/Linux. This matches the earlier P3 build history where UART, DDR, SD, and core were isolated by separate PDI generations before returning to the full boot path.

## 2026-06-13 -- Build 69 64-core diagnostic PDI path added

- The 2026-06-13 Build 68 reprogramming attempt reached `program_hw_devices` against `xcvp1902_1` but did not return `DONE bit: HIGH`; the local Vivado log stopped at `program_hw_devices [current_hw_device]` while the process continued for about 25 minutes. Remote UART capture was repaired and held open on `/dev/ttyUSB0` and `/dev/ttyUSB1`, but both logs stayed at 0 bytes during that incomplete programming session.
- Treat that result as a JTAG/XVC/Vivado programming-session stall, not as evidence that the programmed 64-core design booted silently. A UART-no-output conclusion requires a completed programming run with `DONE bit: HIGH`.
- Added Build 69 as the next 8x8 / 64-core diagnostic PDI path. It keeps the Build 68 tile config, self-contained Vivado flow, OpenSBI bundle addresses, AXI16550 UART, translated DDR, SPI-mode SD path, and Build 52/65 four-ILA debug shape.
- Build 69 uses `BOOTROM_MODE=opensbi_bundle_diag`: startup emits a no-stack `B69 ASM` UART marker before touching DDR-backed C state, then C bootrom prints `B69 OpenSBI bundle diag bootrom`, probes DDR at `0x84001000`, initializes SD, reads GPT, reads the `P3OS`/`BI64` bundle header, copies each component to DDR, prints the first 64-bit word at each destination, and only then releases harts and jumps to OpenSBI.
- New scripts: `scripts/p3_build69_8x8_opensbi_diag.tcl`, `scripts/p3_rebuild_build69_8x8_opensbi_diag.sh`, `scripts/p3_ila_capture_build69_8x8_opensbi_diag.tcl`, and `scripts/p3_decode_build69_ila_csv.py`. Local validation passed `bash -n`, Python compile, Tcl completeness checks, and a full Build 69 bootrom rebuild that generated `bootrom_linux.sv` with the expected B69 diagnostics.

## 2026-06-12 -- Build 68 final 64-core board image generated

- Completed the local source rebuild with `riscv64-linux-gnu-gcc` 11.4 and binutils 2.38. OpenSBI is an RV64 static PIE with the custom batch extension, and Linux is 6.6.0 with `CONFIG_NR_CPUS=64`, `CONFIG_SMP=y`, and `CONFIG_RISCV_SBI=y`.
- Generated `build/huaprop3/opensbi64/p3_opensbi_linux_64hart.img`, size 268,435,456 bytes, SHA256 `2518b7c8b4585c5895816c49304c1d9cc70ce4b85c8179e8ffefe224356a43ae`.
- Component hashes: OpenSBI binary `1b18d5847b248124c9e36f391e152607ebaa65f549a73f7f72d34e8ac62fb5c5`; Linux Image `c5fdf852dcf69b05b30788edf8e1439be1f39dbeee21501c39273db480794a5a`; DTB `0aecb2af343f39689e73f3e30a268eeceb189df2469e3a02cd8e41b01af9c5f6`; P3 initramfs `fcfb4a2812e8d4d3a9bd2f810f7d84f78a44b9f8cf10843144588444f54a83fe`.
- Verified the GPT partition starts at LBA 2048 and contains a `P3OS`/`BI64` version-1 header with exactly four components at `0x80000000`, `0x80200000`, `0x88000000`, and `0x90000000`. Reading each component back from the image produced the same SHA256 as its source file.
- Verified the DTB contains 64 CPU nodes, 64 CLINT hart contexts, and 64 PLIC hart contexts. The initramfs contains a static RV64 BusyBox, prints both CPU counts, emits `P3_64CORE_SHELL_READY`, and executes `/bin/sh`.
- This is not yet a 64-core board-boot result. The image must be written to the removable SD card with complete readback verification, then tested with the existing Build 68 PDI/LTX and pre-program UART capture.

## 2026-06-12 -- Build 68 board initramfs keeps an interactive shell

- The first successful source rebuild produced valid OpenSBI, Linux 6.6, 64-hart DTB, and a four-component `P3OS`/`BI64` bundle, but inspection found that the package's `rootfs_exp_min.cpio.gz` runs `poweroff -f` immediately after printing the online CPU count.
- Updated the Build 68 image script to generate a P3-specific initramfs from the same static RISC-V BusyBox rootfs. Its `/init` mounts `proc`, `sysfs`, and `devtmpfs`, prints `nproc` plus the `/proc/cpuinfo` processor count, emits `P3_64CORE_SHELL_READY`, and enters `/bin/sh`.
- This is required for the board acceptance gate: UART must show 64 processors online and leave a shell available for an independent `nproc` or `/proc/cpuinfo` check. The script still accepts `P3_64CORE_INITRD` for intentional initramfs overrides.
- The final SD bundle hash remains pending until the image is repacked with this board initramfs.

## 2026-06-12 -- Build 68 Linux source repair uses full v6.6 baseline

- The first local retry restored the missing RISC-V `vmlinux.lds.S`, then failed on another omitted core build file: `include/asm-generic/vmlinux.lds.h`.
- Comparing the packaged tree against the official Linux 6.6 archive found a systematic omission pattern, including at least 30 `vmlinux.lds*` build files. A single-file fallback is therefore insufficient and has been retired.
- The image script now verifies official `linux-6.6.tar.xz` SHA256 `d926a06c63dd8ac7df3f86ee1ffc2ce2a3b81a2d168484e76b5b389aba8e56d0`, then uses `tar --skip-old-files` to restore only missing files. The package's custom SBI batch files and all other existing files remain unchanged.
- The official baseline is a local build input under `build/p3_64core/`, not a committed 134 MiB artifact. The formal source rebuild remains pending after this correction.

## 2026-06-12 -- Build 68 software image rebuild moved local

- Corrected the execution boundary: only VP1902 Vivado work requires the offline Ubuntu host. Build 68 OpenSBI/Linux/DTB/initramfs/SD-bundle generation now runs locally with the validated `riscv64-linux-gnu-gcc` 11.4 and binutils 2.38 toolchain.
- The remote attempt failed before producing an image because its Jammy `riscv64-unknown-elf-ld` 2.35.1 cannot create the PIE required by OpenSBI 1.8.1. The Vivado 2024.2 RISC-V wrapper is actually a `riscv32-xilinx-elf` toolchain and also reports `-pie not supported`, so neither remote toolchain is a valid 64-bit OpenSBI builder.
- The first local rebuild exposed two source-flow defects. The supplied 64-core source archive omits Linux 6.6 `arch/riscv/kernel/vmlinux.lds.S`, and the image script invoked Linux `scripts/config` without `--file`, causing it to write a repository-root `.config`.
- Updated `scripts/p3_prepare_64core_opensbi_image.sh` to validate extracted package inputs, restore the exact upstream Linux v6.6 RISC-V linker script from a repository-local fallback, and apply all config edits to `linux/.config`. The fallback file SHA256 is `c49cb1ad6e4e026f55e4b27300ff025f33d5b21f75d056970eb0768462bb4a2a`.
- The formal local rebuild remains pending after this flow correction. Do not treat the older `P3_64CORE_USE_PREBUILT=1` image as the board candidate.

## 2026-06-12 -- Build 68 programs P3 Pro and reaches bundle bootrom

- Pulled the remote Build 68 artifacts into `huaprop3_build68_8x8_opensbi_linux/debug_build/` and verified the local hashes: PDI SHA256 `c93d558c24080631611ce211fcb6df391730d8d04d943a1d35bc7f960612a379`; LTX SHA256 `5c945b15727491d5cd6a8cc7391cd383abc1d4e1336a298f847bf628b8a1eac2`.
- Programmed `xcvp1902_1` twice through `hw_server 100.93.77.36:3121` and XVC `202.197.4.99:2540`. The verified capture run reported `Successfully programmed PDI`, `DONE bit: HIGH`, debug hub `0x3ffc0000000`, four ILAs (`hw_ila_1` through `hw_ila_4`), and Vivado exit code 0.
- The second programming run used a UART capture that started before `program_hw_devices`. `/dev/ttyUSB0` produced 82 bytes: `B68 OpenSBI bundle bootrom`, `initializing SD...`, and `copying block 0 of 1 blocks (0 %)`. `/dev/ttyUSB1` produced no bytes.
- This proves the Build 68 PDI runs on P3 Pro through bootrom execution, AXI16550 UART, SPI-SD initialization, and initial bundle-copy parsing. It does not reach OpenSBI yet.
- The SD software image is now the active gate. The local `build/huaprop3/opensbi64/p3_opensbi_linux_64hart.img` is a 256 MiB prebuilt-artifact smoke image whose manifest references `p3_64core_prebuilt_test2`; repository rules prohibit treating it as the final board candidate. Rebuild OpenSBI/Linux on offline Ubuntu with the P3 jump/FDT addresses, then write and read back that image when the SD card is returned to the reader.

## 2026-06-12 -- Build 68 PDI/LTX generated successfully

- The active remote Build 68 run finished `impl_1` at 2026-06-12 12:12 CST. Vivado reported `write_device_image completed successfully`, `Implementation status for impl_1: write_device_image Complete!`, and no strict non-comment `ERROR:` lines in the scanned logs.
- Remote published artifacts:
  - `/home/cs/openpiton/huaprop3_build68_8x8_opensbi_linux/debug_build/p3_top_build68_8x8_opensbi_linux.pdi`, 237,439,824 bytes
  - `/home/cs/openpiton/huaprop3_build68_8x8_opensbi_linux/debug_build/p3_top_build68_8x8_opensbi_linux.ltx`, 199,395 bytes
- Run-directory artifact hashes: `p3_top.pdi` SHA256 `c93d558c24080631611ce211fcb6df391730d8d04d943a1d35bc7f960612a379`; `p3_top.ltx` and `debug_nets.ltx` SHA256 `5c945b15727491d5cd6a8cc7391cd383abc1d4e1336a298f847bf628b8a1eac2`.
- This is the first complete 8x8 / 64-core VP1902 Vivado run through PDI/LTX generation. It is still not a board-boot result; the next gate is programming the P3 Pro board, capturing UART from reset, and proving OpenSBI/Linux reaches 64 harts online.

## 2026-06-12 -- Build 68 remote default raised to 32 jobs

- Updated the Build 68 remote Vivado policy per the 384-thread host capacity check: future project runs default to `JOBS=32`.
- `scripts/p3_remote_vivado_64core.sh` now defaults to `JOBS=32`, and `scripts/p3_build68_8x8_opensbi_linux.tcl` sets the shared Build 52 wrapper default to `-jobs 32` unless explicitly overridden.
- The current active run remains the old `-jobs 8` process and was not modified in place. Continue monitoring it on the requested 15-minute cadence until it exits or publishes PDI/LTX artifacts.
- `64+` jobs remain non-default until there is evidence that the Vivado flow benefits and the remote shell/file-descriptor limits are adjusted if needed.

## 2026-06-12 -- Build 68 remote Vivado capacity check

- Rechecked the offline Ubuntu host while the active Build 68 run was still alive. The machine is a dual-socket AMD EPYC 9654 host with 384 logical CPUs, 192 physical cores, 2 NUMA nodes, and 1.5 TiB RAM.
- Current host state at 2026-06-12 11:31 CST: load average about 35, 131 GiB RAM used, about 1.3 TiB RAM available, no swap use. The active implementation child Vivado process had 1,169 OS threads, about 99 GiB RSS, and about 395% CPU at that checkpoint.
- The active run remains a `-jobs 8` run. Logs prove Vivado used up to 7 synthesis processes and up to 8 CPUs for place/route, DRC, methodology, and timing update. The run has completed route and post-route reports but still has no published PDI/LTX artifact at this checkpoint.
- Practical guidance at this checkpoint was to raise the next run to `JOBS=32` because CPU and memory have ample headroom. Do not jump directly to 64+ for the production path until Vivado scaling evidence is collected; Vivado's route/place scaling is not linear, and the shell still reports `ulimit -n=1024`.

## 2026-06-12 -- Build 68 remote Vivado default parallelism raised

- Checked the active offline-Ubuntu Build 68 Vivado run. It was launched with `-jobs 8`; Vivado reported main `synth_design` using up to 7 processes, and `place_design`, `route_design`, DRC, and timing update using up to 8 CPUs.
- The offline Ubuntu host reports 384 logical CPUs and 1.5 TiB RAM, with roughly 1.3 TiB available during the run. Peak memory observed so far was about 42 GiB for synthesis, 90 GiB for placement, and 96 GiB for routing, so the host has headroom for more parallelism.
- Updated the Build 68 flow so future remote runs initially defaulted to `JOBS=16`, and so `scripts/p3_build68_8x8_opensbi_linux.tcl` set the shared Build 52 wrapper default to `-jobs 16`. The shared wrapper now also sets `general.maxThreads` and `synth.maxThreads` to the chosen job count before top synthesis and place/route.
- The existing Build 52 implementation child-IP cache guard remains serialized through `synth.maxThreads=1` and `synth.maxClusterJobsRunCount=1` after synthesis. That preserves the historical workaround for generated debug/IP child runs while allowing the large top-level Build 68 synthesis/place/route stages to use more CPU.
- This change affects future runs only; the currently active PID 335626 run was not modified in place.

## 2026-06-12 -- Build 68 64-core route completes with timing met

- The remote Build 68 `impl_1` run reached and completed `route_design` on the offline Ubuntu Vivado 2024.2.2 host. Vivado reported `Router Completed Successfully`, `route_design completed successfully`, and 0 errors in the routing task.
- `route_report_route_status_0.rpt` reports 6,943,224 routable nets, all 6,943,224 fully routed, with 0 routing errors. The route log also reported 0 failed nets, 0 unrouted nets, 0 partially routed nets, and 0 node overlaps.
- `route_report_timing_summary_0.rpt` reports all user timing constraints met: `WNS=8.834 ns`, `TNS=0`, `WHS=0.010 ns`, `THS=0`, `WPWS=0.063 ns`, and `TPWS=0`.
- Post-place utilization from `place_report_utilization_0.rpt` was 4,394,422 CLB LUTs total by logic/memory components, 2,557,136 registers, 4,844 BRAM tiles, 128 URAMs, and 1,153 DSP slices. The BRAM pressure is high but did not prevent route closure in this run.
- This is the first 8x8 / 64-core VP1902 place-route-timing feasibility result. It is still not a board-boot result: PDI/LTX generation and hardware validation remain pending.

## 2026-06-12 -- Build 68 64-core synthesis passes and implementation starts

- The remote offline-Ubuntu Vivado 2024.2.2 run for Build 68 completed all source/elaboration gates and finished main `synth_1` at 2026-06-12 04:29 CST. `impl_1` started immediately afterward.
- `p3_top_utilization_synth.rpt` for `xcvp1902-vsva6865-1MP-e-S` reports 4,424,744 CLB LUTs (52.30%), 2,553,630 registers (15.09%), 4,836 BRAM tiles (71.03%), 128 URAMs (5.82%), and 1,153 DSP slices (16.80%).
- This proves the 8x8 OpenPiton/Ariane top is synthesizable in the self-contained Build 68 OpenSBI/Linux project after fixing the Ariane debug-port archive issue and the missing companion `bootrom` generation path.
- This is not yet a placement, routing, timing, PDI, or board-boot result. The active risks now move from source completeness to BRAM placement density, route congestion, and cross-SLR timing during `impl_1`.

## 2026-06-12 -- Build 68 companion ROM uses inline minimal DTS

- The next remote retry exposed a second clean-archive issue in the companion baremetal ROM path: `bootrom/baremetal/rv64_platform.dts` is a tracked symlink to `../rv64_platform.dts`, but the symlink target is a generated file ignored by `bootrom/.gitignore`.
- Local workspaces had that generated target from earlier bootrom runs, while the remote committed source archive did not. `dtc` therefore failed with `Couldn't open "rv64_platform.dts"` before Vivado project creation.
- Updated `scripts/p3_rebuild_build68_8x8_opensbi_linux.sh` to create a temporary minimal DTS inline for the companion `bootrom` module. The temporary DTB is only embedded in the unused baremetal ROM instance so `riscv_peripherals.sv` can elaborate both ROM modules; the actual Build 68 OpenSBI/Linux boot DTB remains the 64-hart DTB generated and packaged by the OpenSBI image flow.
- This keeps the remote clean archive independent of both ignored generated DTS files and git metadata.

## 2026-06-12 -- Build 68 companion ROM avoids riscvlib git dependency

- The remote Build 68 rerun started from committed `HEAD` and verified the uploaded script/software hashes, but stopped in the companion baremetal ROM rebuild before Vivado project creation.
- The failure was in `bootrom/baremetal/Makefile`: it invokes `riscvlib.py`, which calls `git log | ... awk -e ...` while generating `rv64_platform.dts`. The remote source tree is a clean archive without `.git`, and the Vivado-provided environment reported `awk: not an option: -e`, so the DTS generation failed before `bootrom.sv` could be produced.
- Updated `scripts/p3_rebuild_build68_8x8_opensbi_linux.sh` to generate the companion `bootrom` module directly from the tracked `bootrom/baremetal/rv64_platform.dts`: run `dtc`, compile `bootrom.S`, convert it to `bootrom.img`, and run `gen_rom.py`. This keeps the clean-archive path independent of git metadata and the riscvlib `awk -e` pipeline.
- This companion baremetal ROM remains a synthesis dependency of `riscv_peripherals.sv`; it is not the selected Build 68 OpenSBI/Linux boot path.

## 2026-06-12 -- Build 68 bootrom rebuild validates locally

- Reran `scripts/p3_rebuild_build68_8x8_opensbi_linux.sh` locally after replacing the module-name guard with `awk`.
- The script generated `bootrom/baremetal/bootrom.sv` with `module bootrom`, then generated `bootrom/linux/bootrom_linux.sv` with `module bootrom_linux` under `BOOTROM_MODE=opensbi_bundle`, `MAX_HARTS=64`, `P3_OPENSBI_FW_ADDR=0x80000000`, and `P3_OPENSBI_DTB_ADDR=0x88000000`.
- The local run completed with exit code 0. It did not leave tracked bootrom DTS or script diffs; generated ROM/dump outputs remain uncommitted build products.
- Next action is a remote offline-Ubuntu Vivado rerun from committed `HEAD` to verify that the previous `Synth 8-439 module 'bootrom' not found` gate is cleared in a clean self-contained source snapshot.

## 2026-06-12 -- Build 68 bootrom module check made portable

- Local execution of `scripts/p3_rebuild_build68_8x8_opensbi_linux.sh` successfully generated `bootrom/baremetal/bootrom.sv`, then exposed a bug in the new guard: the `grep -E` module-name regular expression itself failed with `Unmatched (` before reaching the Linux ROM rebuild.
- Replaced the fragile extended-regex check with an `awk` check for `module bootrom` and `module bootrom_linux` as the first two tokens on a source line. This preserves the intended clean-archive guard without depending on grep parenthesis escaping.
- The failed local validation did not dirty tracked bootrom DTS files; generated ROM outputs remain build products and are not committed.

## 2026-06-12 -- Build 68 rebuild now generates both bootrom modules

- Traced the `Synth 8-439 module 'bootrom' not found` failure through the real RTL path. `riscv_peripherals.sv` always instantiates both `bootrom` and `bootrom_linux`, then selects between them with `ariane_boot_sel_i`; therefore the baremetal `bootrom` module is still required for synthesis even when Build 68 boots through the OpenSBI/Linux ROM.
- The remote clean source archive only regenerated `bootrom/linux/bootrom_linux.sv`. The local tree had an old untracked `bootrom/baremetal/bootrom.sv`, which masked the missing-generation bug locally but not on offline Ubuntu.
- Updated `scripts/p3_rebuild_build68_8x8_opensbi_linux.sh` so Build 68 first regenerates `piton/design/chipset/rv64_platform/bootrom/baremetal/bootrom.sv`, checks that it declares `module bootrom`, then regenerates the OpenSBI bundle `bootrom_linux.sv` and checks `module bootrom_linux`.
- This is still before any 64-core resource, congestion, timing, route, PDI, or board-boot conclusion. The next remote Vivado run should verify that `synth_1` passes this bootrom elaboration gate before treating later failures as real 8x8 hardware feasibility data.

## 2026-06-12 -- Build 68 reaches next synth_1 gate: bootrom module missing

- Reran Build 68 after applying the Ariane debug-port fix. The `p3_ariane_debug_bus` error did not recur; Vivado advanced through the Ariane wrapper/tile elaboration path and reached `riscv_peripherals`.
- The new first hard error is `Synth 8-439 module 'bootrom' not found` at `source_snapshot/piton/design/chip/tile/ariane/corev_apu/openpiton/riscv_peripherals.sv:381`.
- The downstream `riscv_peripherals`, `chipset_impl`, `chipset`, `system`, `openpiton_wrapper`, and `p3_top` failures are fallout from that missing module. This remains an RTL/filelist/source-snapshot issue before resource reporting, not a VP1902 capacity, routing, congestion, or timing result.
- Next action is to trace the Build 68 bootrom rebuild output, module name, and Vivado source-file inclusion path. The likely class is that the generated bootrom source exists under the bootrom tree but is not added to the self-contained Vivado fileset under the module name expected by `riscv_peripherals`.

## 2026-06-12 -- Build 68 Ariane debug-port fix committed

- Committed the missing `P3_RTL_DEBUG` Ariane wrapper port in the CVA6/Ariane submodule as `bf55dcba Expose P3 Ariane debug bus port`, then updated the OpenPiton superproject gitlink to point at that submodule commit.
- The root cause of the 00:34 CST `synth_1` failure was not Vivado, VP1902 capacity, or PyHP generation. `tile.v.pyv` already generated the `p3_ariane_debug_bus` connection, but the remote source archive packed the Ariane submodule from its committed `HEAD`; the matching local `ariane_verilog_wrap.sv` port was still a dirty submodule change and was therefore absent on offline Ubuntu.
- Updated `scripts/p3_remote_vivado_64core.sh` to fail before packing when any recursive submodule has staged or unstaged tracked changes. Remote Build 68 source archives now require committed and pushed submodule RTL fixes plus a committed superproject gitlink update.

## 2026-06-12 -- Build 68 main synthesis exposes Ariane debug-port mismatch

- The recursive-archive Build 68 run failed during main `synth_1` RTL elaboration at 2026-06-12 00:34 CST, after all BD/IP OOC runs had completed.
- Vivado's first hard error was `Synth 8-11365`: the generated `tile.tmp.v` connected named port `p3_ariane_debug_bus` on instance `core` of `ariane_verilog_wrap`, but `ariane_verilog_wrap.sv` did not declare that port.
- The downstream `clk_gating_latch`, `tile`, `chip`, `system`, `openpiton_wrapper`, and `p3_top` synthesis errors are fallout from that first port mismatch. This is an RTL interface/define consistency issue, not a VP1902 resource, BRAM, placement, routing, congestion, or timing result.
- Next action is to inspect the real P3 RTL generation path and align the `P3_RTL_DEBUG` Ariane wrapper/tile interface before rerunning Build 68.

## 2026-06-12 -- Build 68 recursive snapshot reaches main 64-core synthesis

- The rerun using the recursive source archive passed the self-contained path checks: the generated XPR had zero `$PPRDIR/../piton/...` live include paths at the 00:33 CST checkpoint.
- All required BD/IP OOC synthesis runs before the OpenPiton top completed successfully on the offline Ubuntu Vivado 2024.2.2 host. The four BD-owned `axis_ila_*` runs, `uart_16550_synth_1`, `openpiton_top_clk_wizard_0_0_synth_1`, `openpiton_top_proc_sys_reset_0_0_synth_1`, and `openpiton_top_axi_noc_0_0_synth_1` all produced `.vivado.end.rst` markers with no error marker.
- The main 64-core top synthesis run started at 2026-06-12 00:33:07 CST as `synth_1`, with Vivado invoking `synth_design -top p3_top -part xcvp1902-vsva6865-1MP-e-S` and checking out the `Synthesis`/`xcvp1902` license.
- This is the first remote Build 68 run to move beyond environment, license, recursive submodule, BD generation, and OOC-IP gates into the real 8x8 OpenPiton top synthesis gate. It is not yet a resource, timing, placement, routing, congestion, PDI, or board-boot result; those remain pending until `synth_1`, `impl_1`, and hardware validation complete.

## 2026-06-11 -- Build 68 self-contained archive now requires recursive submodules

- The license-enabled remote Build 68 run completed all four BD-owned ILA OOC synthesis runs on VP1902 with 0 errors and 0 critical warnings. It then stopped before the main 64-core `synth_1` run because the self-contained validation found live include paths in the XPR: `$PPRDIR/../piton/design/chip/tile/ariane/common/submodules/common_cells/include` and `$PPRDIR/../piton/design/chip/tile/ariane/corev_apu/register_interface/include`.
- Root cause was the remote source archive: `scripts/p3_remote_vivado_64core.sh` packed only first-level `.gitmodules` entries, so Ariane nested submodules such as `common_cells` and `register_interface` were missing on the offline Ubuntu host. `p3_create_bd.tcl` then could not snapshot those include directories and Vivado preserved the original live-repo `VerilogDir` values.
- Updated `scripts/p3_remote_vivado_64core.sh` to package `git submodule status --recursive` contents and fail if any required submodule is not initialized. Updated `scripts/p3_create_bd.tcl` so self-contained mode fails immediately when a repo-local include directory is missing, instead of carrying a live path into the Vivado project.
- This preserves the AGENTS.md rule that Build 66+ scaling projects must be self-contained: RTL sources, headers, constraints, top files, wrappers, shims, and include directories must live under `<workdir>/source_snapshot/`; generated BD/IP products may remain under the Vivado project `.gen/.srcs/.cache` directories.

## 2026-06-11 -- Build 68 remote Vivado license checkout works

- Found the Vivado/FlexLM license payload in `D:\Download\9d11b-main\xilinx_ise_vivado.zip`, extracted `xilinx_ise_vivado.lic`, and installed it on the offline Ubuntu build host as `/home/cs/.Xilinx/xilinx_ise_vivado.lic` with mode `600`. The local extracted file and remote installed file both hashed to `63e08704a941138b97803b74d4c94c70f5a3abde78f7d423400ea97fa1b1cacb`.
- The active Build 68 run uses `XILINXD_LICENSE_FILE=/home/cs/.Xilinx/xilinx_ise_vivado.lic` and `LM_LICENSE_FILE=/home/cs/.Xilinx/xilinx_ise_vivado.lic`. Vivado 2024.2.2 now reports `Got license for feature 'Synthesis' and/or device 'xcvp1902'`, so the previous offline-host license blocker is cleared.
- Build 68 has advanced past bootrom rebuild, 8x8 PyHP generation, self-contained project creation, BD generation/output products, and the first BD ILA OOC synthesis runs. `openpiton_top_axis_ila_0_0_synth_1`, `_1_0_synth_1`, and `_2_0_synth_1` completed with 0 errors and 0 critical warnings; `_3_0_synth_1` was in progress at the 23:33 CST checkpoint.
- This is still not a 64-core OpenPiton top synthesis/resource result. The next gate remains the main `synth_1` run, followed by implementation, placement, routing, congestion, and timing.

## 2026-06-11 -- Build 68 license path check finds no Vivado license file

- Checked `/media/d1/Xilinx/Vivado/2024.2/bin` on the offline Ubuntu host after the synthesis license failure. The directory contains Vivado executables and license tools such as `vlm` and `xlicdiag`, but no `.lic`, `*license*`, `.dat`, `.env`, or settings file that configures `XILINXD_LICENSE_FILE` or `LM_LICENSE_FILE`.
- Recursive text checks under that `bin` directory found no FlexLM license-file markers such as `SERVER`, `VENDOR`, `FEATURE`, or `INCREMENT`. Wider `/home` and `/media/d1` path scans found documentation and product data files, but no Xilinx synthesis/device license file for VP1902.
- Current blocker remains unresolved: the offline Ubuntu shell needs an actual Vivado license source, either a valid `.lic` file or a reachable FlexLM server exported through `XILINXD_LICENSE_FILE`/`LM_LICENSE_FILE`.

## 2026-06-11 -- Build 68 remote Vivado reaches synthesis license gate

- Reran full Build 68 after the PyHP PATH fix. The remote Vivado flow passed bootrom rebuild, regenerated the 8x8 PyHP files, created the self-contained `p3b68_8x8` project, added 523 RTL source files, validated the BD, completed NoC placement/routing for the block design, generated BD output products, and produced `synth_1` scripts.
- The run stopped at the first out-of-context ILA synthesis run, `openpiton_top_axis_ila_0_0_synth_1`, before any OpenPiton 8x8 synthesis result was available. Vivado reported: `A valid license was not found for feature 'Synthesis' and/or device 'xcvp1902'`.
- This is now a Vivado licensing/environment blocker on the offline Ubuntu host, not a 64-core RTL/resource/timing failure. No utilization, congestion, route, or timing datapoint should be inferred from this run.

## 2026-06-11 -- Build 68 remote Vivado PATH now exposes PyHP

- After the remote bootrom compile passed, the full Build 68 Vivado run advanced into project/BD creation and 8x8 PyHP regeneration, then failed in `scripts/p3_create_bd.tcl` while sourcing the common `pyhp_preprocess.tcl`: `exec pyhp.py` could not find `pyhp.py` in the remote Vivado `PATH`.
- Added a P3-local PATH setup in `scripts/p3_create_bd.tcl` so `${repo}/piton/tools/bin` is prepended to Tcl `env(PATH)` before the common PyHP preprocessing fallback runs.
- This keeps the fix scoped to P3 Vivado project creation while preserving the existing common `pyhp_preprocess.tcl` behavior.

## 2026-06-11 -- Build 68 remote bootrom compile verified

- Copied the committed `e8893c4` Build 68 picolibc-header fix into `/home/cs/openpiton` on the offline Ubuntu host and reran `scripts/p3_rebuild_build68_8x8_opensbi_linux.sh` without starting full Vivado.
- The remote build selected the system `riscv64-unknown-elf-gcc` path with `P3_BOOTROM_EXTRA_CFLAGS=-isystem /usr/lib/picolibc/riscv64-unknown-elf/include`, compiled `main_opensbi_bundle.c`, `uart.c`, `spi.c`, `sd.c`, and `startup_opensbi_bundle.S`, linked with `-nostdlib -nodefaultlibs -nostartfiles`, and generated `bootrom_linux.sv`.
- The previous bootrom blockers are cleared. The next Build 68 run should reach Vivado project creation and then the first real 8x8 synthesis/resource gate.

## 2026-06-11 -- Build 68 bootrom rebuild supports Jammy picolibc headers

- The second remote Build 68 attempt got past the missing compiler but stopped in `src/main_opensbi_bundle.c` because Ubuntu's `gcc-riscv64-unknown-elf` package does not search `/usr/lib/picolibc/riscv64-unknown-elf/include` by default; GCC's wrapper `stdint.h` then failed at `include_next <stdint.h>`.
- Added `P3_BOOTROM_EXTRA_CFLAGS` to the bootrom Makefile and taught `scripts/p3_rebuild_build68_8x8_opensbi_linux.sh` to pass `-isystem /usr/lib/picolibc/riscv64-unknown-elf/include` when `$HOME/scratch/riscv_install/bin/riscv64-unknown-elf-gcc` is absent but Jammy picolibc headers are installed.
- The fix keeps the existing bootrom link model intact: `-nostdlib`, `-nodefaultlibs`, and `-nostartfiles` still apply, and picolibc is used only as a header provider for the system-packaged RISC-V GCC path.

## 2026-06-11 -- Build 68 remote bootrom toolchain installed

- Built a small offline Jammy package bundle for the offline Ubuntu build host instead of transferring the 12 GiB local `$HOME/scratch/riscv_install` tree. The uploaded tarball SHA256 was `cf1a589a854804e0a8cd6ee198c30a9ead07ec9123451519936d494a44887f19`.
- Installed `libfdt1`, `binutils-riscv64-unknown-elf`, `gcc-riscv64-unknown-elf`, and `device-tree-compiler` on `cs@202.197.4.150` with `dpkg -i`.
- Verified the remote tools after installation: `/usr/bin/riscv64-unknown-elf-gcc` reports version 10.2.0, `/usr/bin/riscv64-unknown-elf-objdump` reports binutils 2.35.1, and `/usr/bin/dtc` reports DTC 1.6.1. Build 68 can now be rerun past the previous bootrom compile blocker.

## 2026-06-11 -- Build 68 remote Vivado run blocked by bootrom toolchain

- Started the Build 68 remote flow with `JOBS=8 scripts/p3_remote_vivado_64core.sh` through the Windows jump host to the offline Ubuntu machine. The run uploaded the self-contained repository archive plus `riscv64-linux-64core-src-20260610.tar.gz`, then launched Vivado 2024.2.2 from `/media/d1/Xilinx/Vivado/2024.2/bin/vivado`.
- The first remote attempt stopped before project creation, synthesis, placement, or routing. Vivado invoked `scripts/p3_rebuild_build68_8x8_opensbi_linux.sh`, which entered `piton/design/chipset/rv64_platform/bootrom/linux/Makefile` with `BOOTROM_MODE=opensbi_bundle` and `MAX_HARTS=64`, then failed while compiling `src/main_opensbi_bundle.c`: `make: riscv64-unknown-elf-gcc: No such file or directory`.
- This is a remote environment/toolchain blocker, not a 64-core hardware resource, congestion, or timing result. The next required step is to expose the OpenPiton bare-metal RISC-V toolchain on the offline Ubuntu host, preferably at `$HOME/scratch/riscv_install/bin/riscv64-unknown-elf-gcc` or through an equivalent `RISCV`/`PATH` setting, then rerun Build 68.

## 2026-06-11 -- Build 68 64-core OpenSBI/Linux path implemented

- Added the Build 68 direct 8x8 P3 Pro target with `PITON_X_TILES=8`, `PITON_Y_TILES=8`, and `PITON_NUM_TILES=64`, reusing the self-contained Build 66/67 Vivado flow through `scripts/p3_build68_8x8_opensbi_linux.tcl`.
- Added `BOOTROM_MODE=opensbi_bundle`. Hart 0 now reads a `P3OS`/`BI64` SD bundle header, copies OpenSBI, Linux `Image`, DTB, and initramfs into DDR, then releases all harts into OpenSBI at `0x80000000`.
- Added the 64core software packaging flow from `riscv64-linux-64core-src-20260610.tar.gz`: P3 DTB generation, SD bundle image packing, local prebuilt-artifact smoke mode, and the remote Ubuntu Vivado runner, including submodule content packaging for the offline host.
- Local validation passed Python/shell syntax checks, 64-hart DTB generation with `cpu@63` and `riscv,ndev=<2>`, dummy bundle header verification, prebuilt-artifact SD bundle packaging, and Build 68 bootrom compilation. Full 8x8 Vivado synthesis/place/route remains to be run on the offline Ubuntu host.

## 2026-06-11 -- P3 offline Vivado build host flow documented

- Documented the validated connection chain for future P3 Pro / VP1902 64-core builds: local machine to `23178@100.70.176.125`, then `ssh -J` / `ProxyJump` to `cs@202.197.4.150`.
- Recorded the offline Ubuntu build environment as host/user `ubun` / `cs`, remote workspace `/home/cs/openpiton`, and Vivado `/media/d1/Xilinx/Vivado/2024.2/bin/vivado`, validated as 2024.2.2.
- Standardized the rule that the remote Windows machine is only a TCP SSH jump host. Build scripts, long shell logic, and Vivado Tcl should run on offline Ubuntu through direct `ssh -J` commands, with artifacts returned via `scp -o ProxyJump=...`.

## 2026-06-11 -- Build 67 SD-image debug campaign paused for direction change

- Paused the current Build 67 SD-image write/test loop before another card write. The remote Ubuntu host currently shows only the Kingston multi-reader's empty 0-byte slots, so no new `dd` write was performed for the regenerated fixed-format safe MTIP image.
- The recent image sequence is now recorded as a single campaign: keep-divisor proved stable Linux ASCII output and 2-hart bring-up; SBI trace proved the old RPC line was not the final stop and showed `/bin/sh` launch; clean-delay proved the nonzero-hart delay alone is not sufficient; low-noise console proved the real post-shell failure is CPU1 RCU/forward-progress loss; IRQ-chain trace narrowed the first hard break to hart1 timer/SBI/STIP behavior; MTIP-state trace proved the sampled hart1 interrupt was real machine timer cause 7 and that the normal `csrc mie, MTIE` path can work; safe MTIP then exposed a CPU1 illegal-instruction panic at a valid kernel address plus a firmware `%u` trace-format bug.
- Current status: Build 67 is past bootrom, SPI-SD, DDR copy, BBL `mret`, Linux banner, and 2-hart SMP bring-up. It can reach `/bin/sh` under low-noise diagnostic bootargs, but it is not an interactable shell or XSBench milestone because CPU1 later stalls and UART input does not complete.
- Current root-cause framing: the failure is not RPC/NFS, not stale SD image contents, not BBL-to-Linux entry, and not the physical UART path. The unresolved fault is CPU1 forward progress under SMP, most likely at the boundary between timer/IPI delivery, BBL's M-mode STIP/SSIP redirect, Linux scheduling/RCU, or instruction/cache coherency after executable-page remote fences. The fixed-format safe MTIP image SHA256 `c908aa0ff09800349981125d1a8979eca498ed9f756265e7139f680b0daae21d` remains generated but untested on hardware.

## 2026-06-11 -- Build 67 fixed-format safe MTIP image regenerated

- Regenerated `build/huaprop3/sd_images/huaprop3_linux_xsbench_2x1_mtip_state_safe.img` after fixing the Build 67 firmware trace format to avoid unsupported `%u` in riscv-pk's tiny `snprintf`.
- The regenerated SD image is `201326592` bytes with SHA256 `c908aa0ff09800349981125d1a8979eca498ed9f756265e7139f680b0daae21d`.
- Embedded DTB SHA256 is `8d0ad434739cc3b9dbc4fce1931f6981eb960a90132c24768ee2923f876847dc`; it keeps the two-hart Build 67 DTB, UART interrupt source 1, `riscv,ndev = <2>`, keep-divisor earlycon, `keep_bootcon`, and `initcall_debug`.
- Instrumented BBL SHA256 is `e4dc6f2b4b98a89687768197ee01317d16f0f9d3196a0101da6820fb5bc17184`. This image should replace the earlier safe MTIP SD contents before retesting, because the previous run's `B67I` numeric fields were affected by the unsupported format specifier.

## 2026-06-11 -- Build 67 safe MTIP run exposes printk-format issue

- Programmed the unchanged Build 67 PDI/LTX after writing `huaprop3_linux_xsbench_2x1_mtip_state_safe.img`; Vivado reported `DONE bit: HIGH`, debug hub `0x3ffc0000000`, and four ILAs.
- UART capture `~/p3_uart_logs/ttyUSB0_20260611_190041_b67_mtip_state_safe.log` reached Linux 5.1.0-rc7, `smp: Brought up 1 node, 2 CPUs`, passed `init_sunrpc`, and entered `populate_rootfs`.
- The run then reported `Oops - illegal instruction [#1]` on CPU1 with `sepc=0xffffffe0006e4004`, `ra=0xffffffe0006e43e6`, `scause=2`, and then panicked with `Fatal exception in interrupt`. Local raw-kernel disassembly shows `0xffffffe0006e4004` contains a valid compressed jump/call sequence in the wait/queue path, so the symptom is consistent with wrong or stale instruction fetch, not a deliberately invalid instruction in the binary.
- The same log showed corrupted `B67I` lines (`set0=...et1`) and embedded instruction bytes. Code inspection found the immediate diagnostic bug: riscv-pk's tiny `snprintf` supports `%p`, `%x`, `%d`, `%s`, and `%c`, but not `%u`; the Build 67 trace format used `%u`, causing varargs to go out of sync.
- Updated the generator to replace all firmware `%u` diagnostics with supported `%ld` plus explicit casts. The current safe MTIP run is valid for the Linux `sepc/scause` observation, but its `B67I` numeric fields are not trusted until the fixed-format image is regenerated and retested.

## 2026-06-11 -- Build 67 safe MTIP image written to SD

- Reidentified the inserted card as `/dev/sdc`, model `Multi-Reader -1`, size `31914983424`, `TYPE=disk`, `TRAN=usb`, and `RM=1`; the other Kingston reader slots remained 0-byte devices.
- Transferred the zstd-compressed image to the remote Ubuntu host. Compressed SHA256 `6487eb62a1f0184e7dc717a949f9f1a33b135e8edaf5dd56a740a83cbdefe3ba` and decompressed image SHA256 `6fd1729e2cd3374c4ef37d546b464b73645b7ec1b0f78f1230d125278ba5578c` matched the local files.
- Unmounted the card partitions and wrote all `201326592` bytes of `huaprop3_linux_xsbench_2x1_mtip_state_safe.img` to the whole disk.
- Read back exactly `201326592` bytes from `/dev/sdc`; SHA256 was `6fd1729e2cd3374c4ef37d546b464b73645b7ec1b0f78f1230d125278ba5578c`, matching the generated image.
- The next P3 run should use the unchanged Build 67 PDI/LTX and capture `B67C mt1z/mt1cf` through shell launch and the first CPU1 RCU stall.

## 2026-06-11 -- Build 67 non-perturbing MTIP image generated

- Generated `build/huaprop3/sd_images/huaprop3_linux_xsbench_2x1_mtip_state_safe.img`, size `201326592` bytes, SHA256 `6fd1729e2cd3374c4ef37d546b464b73645b7ec1b0f78f1230d125278ba5578c`.
- Embedded DTB SHA256 is `8d0ad434739cc3b9dbc4fce1931f6981eb960a90132c24768ee2923f876847dc`; it retains two harts, UART interrupt source 1, `riscv,ndev = <2>`, keep-divisor earlycon, `keep_bootcon`, and `initcall_debug`.
- Instrumented BBL SHA256 is `7d44dc22eb13f39838811c311f9193f5e7dccafee2beab7c56c92d0221a875e8`; binary inspection confirmed the new `B67C mt1z/mt1cf/mie1/mip1/cause1/epc1/status1` format.
- `mtrap.o` disassembly places the `mtimecmp` store, STIP clear, and MTIE set before either diagnostic `printm()` call. `mentry.o` disassembly shows the normal MTIP path clears MTIE and jumps directly to STIP redirect; full CSR stores are reachable only from the MTIE-zero or MTIE-clear-failure branches.
- This image is ready for SD writing and reuses the unchanged Build 67 PDI/LTX.

## 2026-06-11 -- Build 67 MTIP trace made non-perturbing

- The first MTIP-state hardware run programmed the unchanged Build 67 PDI successfully and reached Linux 5.1.0-rc7 with two CPUs. Its first valid hart1 sample was `cause=0x8000000000000007`, incoming `mie=0x2aa`, post-clear `mie=0x22a`, and `mt1z=0`; this proves the sampled trap was a real machine timer interrupt and that `csrc mie, MTIE` worked on that normal entry.
- Raw UART bytes showed the run stopped during `populate_rootfs` exactly when both harts reached timer trace count `0x401`. The old diagnostic called multiple polling `printm()` operations before programming `mtimecmp`, clearing STIP, and re-enabling MTIE. Both harts also wrote the same AXI16550 without a lock; missing/interleaved fields in `B67I` are direct evidence of that collision.
- Changed `mcall_set_timer()` so timer rearm completes before diagnostic output, made the shared timer counters volatile, and restricted post-Linux firmware timer/IPI output to hart0.
- Reduced the MTIP fast path to its counter and MTIE clear on normal entries. Full `mie`, `mip`, `mcause`, `mepc`, and `mstatus` snapshots are now written only if MTIE was already clear at entry or remained set after `csrc`; `B67C` reports both `mt1z` and the new `mt1cf` clear-failure counter.
- Added `scripts/p3_make_build67_mtip_state_safe_image.sh` for the next software-only SD image. The previous MTIP-state run cannot be used to claim a kernel stop at 0.7 seconds because the raw log continued to `populate_rootfs`; its diagnostic UART placement was itself a runtime perturbation.

## 2026-06-11 -- Build 67 MTIP-state trace image written to SD

- Reidentified the card immediately before writing as `/dev/sdc`, model `Multi-Reader -1`, size `31914983424`, `TYPE=disk`, `TRAN=usb`, and `RM=1`; the protection script also required the exact model and size before allowing `dd`.
- Transferred a zstd-compressed image to the remote Ubuntu host. Compressed SHA256 `cbcb9bd60accab807669d13bf0f575f42703d45d0a5b8065dcbcb934d2a893c8` and decompressed image SHA256 `5e0333d9aa315f3127116bc99a356922772bae8c2e995c092584fd6280900995` both matched the local files.
- Unmounted the card partitions and wrote all `201326592` bytes of `huaprop3_linux_xsbench_2x1_mtip_state_trace.img` to the whole disk.
- Read back exactly `201326592` bytes from `/dev/sdc`; SHA256 was `5e0333d9aa315f3127116bc99a356922772bae8c2e995c092584fd6280900995`, matching the generated image.
- The next P3 run should use the unchanged Build 67 PDI/LTX and capture the first `B67C` snapshots through the CPU1 RCU stall.

## 2026-06-11 -- Build 67 MTIP-state trace image generated

- Generated `build/huaprop3/sd_images/huaprop3_linux_xsbench_2x1_mtip_state_trace.img`, size `201326592` bytes, SHA256 `5e0333d9aa315f3127116bc99a356922772bae8c2e995c092584fd6280900995`.
- Embedded DTB SHA256 is `8d0ad434739cc3b9dbc4fce1931f6981eb960a90132c24768ee2923f876847dc`; validation confirmed `cpu@1`, UART interrupt source 1, `riscv,ndev = <2>`, keep-divisor earlycon, `keep_bootcon`, and `initcall_debug`.
- Instrumented BBL SHA256 is `73ba71b3049492b0ed35e42ea9a53d4e2a343c46d818e874d4d3792c54a3cef8`; binary inspection confirmed the `B67C mt1z/mie1b/mie1a/mip1/cause1/epc1/status1` snapshot format is present.
- The board discriminator is now concrete: increasing `mt1z` means hart1 entered the machine-timer trap with `mie.MTIE` already clear; nonzero `mie1a & 0x80` means the MTIE clear did not take effect or persist; a cause other than interrupt 7 means the trap decode or routing assumption is wrong.
- This is a software-only SD image. It must be written and fully read back before reusing the unchanged Build 67 PDI/LTX.

## 2026-06-11 -- Build 67 MTIP-state discriminator added

- Extended the Build 67 BBL diagnostic generator with a second low-frequency `B67C` snapshot for hart1 machine-timer entries.
- The MTIP fast path now records incoming `mie`, `mip`, `mcause`, `mepc`, and `mstatus`, records `mie` immediately after `csrc mie, MIP_MTIP`, and counts entries where incoming `mie.MTIE` was already zero.
- Kept every new per-hart field in its own 64-byte row and used only the already saved `a0` and `a1` registers in `mentry.S`, preserving the original fast-trap register contract.
- Throttled all SBI IPI trace events per hart to the first eight calls plus every 1024th call. This preserves remote-fence progress markers without reproducing the UART flood seen while `flush_icache_pte()` mapped executable pages.
- Added `scripts/p3_make_build67_mtip_state_trace_image.sh`; it generates a software-only SD image and does not require a new Vivado build.

## 2026-06-11 -- Build 67 IRQ-chain run isolates hart1 MTIP re-entry

- Programmed the unchanged Build 67 PDI/LTX and booted the verified IRQ-chain SD image. UART capture `~/p3_uart_logs/ttyUSB0_20260611_153409_b67_irq_chain_retry.log` reached Linux 5.1.0-rc7, `smp: Brought up 1 node, 2 CPUs`, `Run /bin/sh as init process`, and the shell banner before the first CPU1 RCU stall at 77.58 seconds.
- Raw-kernel disassembly mapped the repeated `B67S ipi_enter hart=1 event=2 ... mepc=0xffffffe000695c5a` to the instruction after the `SBI_REMOTE_FENCE_I` ecall in Linux `flush_icache_pte()`. The call is the SMP implementation of `flush_icache_all()` used when an executable page first clears `PG_dcache_clean`; it is not evidence of a random or permanently stuck software IPI.
- The remote-fence workload stopped with hart1 `SBI_SET_TIMER` count `set1=5266`, machine-software entry count `ms0=5265`, and `send10=718`. The later RCU stalls therefore occur after the executable-page fence burst, not inside a persistent `send_ipi_many()` wait.
- The decisive counter split is `set1=5266` remaining fixed while the hart1 machine-timer entry counter `mt1` advances from 6144 through at least 38912. The inserted `mentry.S` counter is reached only after `mcause` decodes as `IRQ_M_TIMER`; that same path immediately executes `csrc mie, MIP_MTIP` before raising `MIP_STIP`.
- Under the normal BBL timer contract, one MTIP entry disables MTIE and Linux must call `SBI_SET_TIMER` to clear STIP, program `mtimecmp`, and re-enable MTIE. Repeated `mt1` growth without `set1` growth is therefore inconsistent with a normal timer-rearm sequence.
- The next discriminator must store hart1 `mie`, `mip`, `mcause`, `mepc`, and `mstatus` at MTIP entry plus `mie` after `csrc`, and count entries whose incoming `mie.MTIE` was already zero. This separates an RTL/CSR interrupt-mask failure from an interrupt-cause encoding/routing failure before any scheduler or UART change is attempted.

## 2026-06-11 -- Build 67 IRQ-chain trace image written to SD

- Reidentified the inserted card as `/dev/sdc`, model `Multi-Reader -1`, size `31914983424`, `TYPE=disk`, `TRAN=usb`, and `RM=1`; no stale device name was assumed.
- Transferred the zstd-compressed diagnostic image to the remote Ubuntu host, verified compressed SHA256 `aebe2423acb080de6554b1e47ffafff17db0a2b3dd3faa2a9c598dab6c6089ec`, decompressed it, and verified image SHA256 `d32474b42de396a6a3a89ce476b0f8358b6f03058e7c43764b877f2cc15fe168`.
- Unmounted the card partitions and wrote all `201326592` bytes of `huaprop3_linux_xsbench_2x1_irq_chain_trace.img` to the whole disk.
- Read back exactly `201326592` bytes from `/dev/sdc`; SHA256 was `d32474b42de396a6a3a89ce476b0f8358b6f03058e7c43764b877f2cc15fe168`, matching the generated image.
- The next P3 boot should use the unchanged Build 67 PDI/LTX and capture `B67I irq_snapshot` through the first CPU1 RCU stall.

## 2026-06-11 -- Build 67 IRQ-chain trace image generated

- Generated `build/huaprop3/sd_images/huaprop3_linux_xsbench_2x1_irq_chain_trace.img`, size `201326592` bytes, SHA256 `d32474b42de396a6a3a89ce476b0f8358b6f03058e7c43764b877f2cc15fe168`.
- Embedded DTB SHA256 is `8d0ad434739cc3b9dbc4fce1931f6981eb960a90132c24768ee2923f876847dc`; it retains `cpu@1`, UART interrupt source 1, `riscv,ndev = <2>`, keep-divisor earlycon, `keep_bootcon`, and `initcall_debug`.
- Instrumented BBL binary SHA256 is `294ae21b6e76527b72e0674f5a1568f04c4779ace67b3e1b20afeaa39cbc7fb6`.
- Disassembly verified that the MTIP/MSIP fast paths use only the already saved `a0` and `a1`, index their per-hart 64-byte counter slots with `mhartid << 6`, restore both registers, and then execute the original STIP/SSIP redirect path.
- The next board run requires writing this image to the SD card and capturing `B67I irq_snapshot` through the first CPU1 RCU stall. No new PDI is required.

## 2026-06-11 -- Build 67 IRQ-chain trace generator added

- Extended the Build 67 SBI trace generator with low-frequency per-hart interrupt-chain counters instead of adding more high-volume printk traffic.
- The wrapper invokes the shared SBI trace generator explicitly through `bash`, because the existing shared script is tracked without an executable bit; image generation must not depend on that file mode.
- BBL's fast machine trap path now counts MTIP entries before raising STIP and MSIP entries before clearing CLINT MSIP and raising SSIP. The SBI path separately counts `SBI_CLEAR_IPI`, per-direction software IPI sends, and timer rearms.
- Per-hart hot counters use separate 64-byte slots. Hart0 and hart1 therefore do not write the same cache line merely to produce diagnostics, which avoids adding false sharing to an investigation that may already involve scheduler atomics or cache coherence.
- Hart0 prints a `B67I irq_snapshot` every 1024 timer rearms. Each snapshot includes both harts' timer SBI counts, MTIP entries, MSIP entries, IPI clears, and hart0-to-hart1/hart1-to-hart0 software IPI sends, so hart1's final state remains observable even if hart1 stops making SBI calls.
- Added `scripts/p3_make_build67_irq_chain_trace_image.sh`; it generates `huaprop3_linux_xsbench_2x1_irq_chain_trace.img` while preserving the keep-divisor early console, `keep_bootcon`, and `initcall_debug`.
- Interpretation is explicit: `send01` without `ms1` indicates CLINT MSIP delivery failure; `ms1` without `clear1` indicates BBL-to-Linux SSIP handling failure; advancing `mt1` without advancing `set1` indicates STIP/Linux timer-rearm failure; advancing interrupt counters with persistent `_TIF_NEED_RESCHED` moves the primary suspect to scheduler atomics/cache coherence.

## 2026-06-11 -- Build 67 CPU1 stall diagnosis refined

- Rechecked the exact Linux `ariane-v0.7`, BBL, generated Build 67 RTL, and UART trace paths instead of treating the RCU task dump as a live program-counter sample.
- The RCU dump reports CPU1's current `sh` task with thread flags `0x00000008`; this is `_TIF_NEED_RESCHED` in `arch/riscv/include/asm/thread_info.h`. CPU1 still shows the same pending reschedule 63 seconds later, so the hard evidence is failed CPU1 forward progress after a reschedule request.
- The saved stack entry `__schedule+0x1b4` is not sufficient to prove that CPU1 is spinning inside `__schedule`; `dump_cpu_task()` unwinds the saved stack of the remote CPU's current task and does not capture live CPU registers on this RISC-V target.
- The sparse SBI trace also needs a narrower interpretation. Hart1's last printed `SBI_SET_TIMER` count is `0x0c01`, but the trace prints only the first eight calls and every 1024th call. A tickless idle CPU can legitimately stop making timer SBI calls, so this marker alone does not prove a lost CLINT interrupt.
- The same trace shows hart1 executing an `SBI_SEND_IPI` at approximately 35.9 seconds during `pty_init`, long after its last sparse timer marker near 3 seconds. Hart1 therefore did not permanently stop at the last printed timer count.
- Static wiring remains consistent: two CLINT compare registers are present, hart1 uses `mtimecmp` at `CLINT_BASE + 0x4008`, and separate timer/IPI outputs reach tile1. The next discriminator must count dynamic MTIP entry, MSIP entry, supervisor IPI clear, and timer rearm per hart. This will separate CLINT delivery, BBL M-mode redirect, Linux SSIP/STIP handling, and a possible shared-memory/atomic scheduler failure.

## 2026-06-11 -- Build 67 low-noise run exposes CPU1 timer/RCU stall

- Programmed the unchanged Build 67 PDI/LTX after installing the low-noise console SD image. Vivado reported `DONE bit: HIGH`, refreshed debug hub `0x3ffc0000000`, and enumerated `hw_ila_1` through `hw_ila_4`.
- UART logs `~/p3_uart_logs/ttyUSB0_20260611_1440_b67_low_noise_retry.log` and `~/p3_uart_logs/ttyUSB0_20260611_1445_b67_low_noise_continue.log` show the full 65,536-block SD-to-DDR copy, BBL entry, Linux 5.1.0-rc7, and `smp: Brought up 1 node, 2 CPUs`.
- The old RPC line is conclusively not a kernel stop. `init_sunrpc` returned after 57 ms, `populate_rootfs` returned after about 30 seconds, `serial8250_init` returned successfully, the OF probe registered `ttyS0` at `0xfff0c2c000` with IRQ 1, and Linux reached `Run /bin/sh as init process`.
- `keep_bootcon` kept the early UART console active after the normal `ttyS0` console enabled, so kernel lines appear twice. This duplicate output is expected for this diagnostic image and proves both console paths are writing the same AXI16550.
- The first real failure appears after shell launch: at 69.9 seconds Linux reports `rcu_sched detected stalls on CPUs/tasks`, identifies CPU1, and dumps `sh` as a running task in `sys_read -> __schedule`. The same CPU1 stall repeats at 132.9 and 259.0 seconds.
- A correctly throttled UART command, `echo P3_SLOW_OK`, sent one character every 250 ms produced no command echo or result. The shell is therefore launched but not interactable once CPU1 enters the repeated RCU stall. One earlier unthrottled probe caused a single `ttyS0 input overrun` and is not used as evidence.
- Static Build 67 source inspection confirmed `riscv_peripherals.NumHarts=2`, two CLINT `mtimecmp` registers, `timer_irq_o[0]`/`timer_irq_o[1]`, and separate connections to tile0/tile1. The 2-hart DTB also maps machine timer interrupt 7 to both CPU interrupt controllers.
- Comparison with the earlier SBI trace gives the next concrete target: hart0 continued issuing `SBI_SET_TIMER` through at least trace count `0xec01`, while hart1 stopped after `0x0c01`. Because Linux rearms the next timer event through SBI after handling a timer event, the runtime failure is now narrowed to hart1 ceasing to handle/rearm timer events. Instrument the dynamic path from CLINT `timer_irq_o[1]` through tile1 `time_irq_i`, the CVA6 machine-timer trap, BBL timer redirection, and the next `SBI_SET_TIMER`; do not return to RPC or UART-divisor debugging.

## 2026-06-11 -- Build 67 low-noise console image written to SD

- Reidentified the remote SD card before writing as `/dev/sdc`, model `Multi-Reader -1`, size `31914983424`, `TYPE=disk`, `TRAN=usb`, and `RM=1`; the other Kingston multi-reader slots remained 0-byte devices.
- Transferred the compressed image to the remote Ubuntu host, verified the compressed payload, decompressed it, and verified the original image SHA256 `539a1313326db2bde206cc1ab68a37b7de10015794e8bbcbede8cad4be5f9b1e`.
- Wrote all `201326592` bytes (`192 MiB`) of `huaprop3_linux_xsbench_2x1_low_noise_console.img` to the whole `/dev/sdc` device.
- Read back the complete 192 MiB written span from the SD card; its SHA256 was `539a1313326db2bde206cc1ab68a37b7de10015794e8bbcbede8cad4be5f9b1e`, matching the generated image.
- The card now contains the low-noise Build 67 test payload with keep-divisor earlycon, `keep_bootcon`, `ignore_loglevel`, and `initcall_debug`, without the high-frequency `B67S` firmware trace.

## 2026-06-10 -- Build 67 low-noise console image generated

- Generated the low-noise Build 67 2x1 SD image at `build/huaprop3/sd_images/huaprop3_linux_xsbench_2x1_low_noise_console.img` for the next P3 hardware run.
- The image keeps the keep-divisor UART setup (`earlycon=uart8250,mmio,0xfff0c2c000 console=ttyS0,115200n8`) and keeps `keep_bootcon loglevel=8 ignore_loglevel initcall_debug`, but removes the high-frequency `B67S` BBL/SBI diagnostic prints.
- Local hashes: image SHA256 `539a1313326db2bde206cc1ab68a37b7de10015794e8bbcbede8cad4be5f9b1e`, DTB SHA256 `8d0ad434739cc3b9dbc4fce1931f6981eb960a90132c24768ee2923f876847dc`, BBL payload SHA256 `58f98696e6a5eb575af3717b601209072c86a3fa00c413aac8e60b88f973e2c7`.
- Local validation confirmed the generated DTB still contains `cpu@1`, UART interrupt source 1, `riscv,ndev = <2>`, and the expected bootargs; binary inspection confirmed the low-noise BBL payload has no `B67S` markers.
- Hardware purpose: distinguish a real post-SunRPC kernel stop from a normal 8250 console handoff/output-path failure while avoiding the heavy SBI trace UART load.

## 2026-06-10 -- Build 67 console-handoff correction path

- Code inspection narrowed the practical fix to the UART/PLIC/console handoff contract, not SunRPC. In Linux `ariane-v0.7`, `init_sunrpc` is only an `fs_initcall`; `populate_rootfs`, `serial8250_init`, the OF 8250 probe, and `/bin/sh` can run after that line when early boot console output is kept alive.
- Updated the Build 67 image-generation scripts so newly generated 2x1 DTBs report `riscv,ndev = <2>`, matching the RTL `riscv_peripherals` instance with `NumSources=2` and `irq_sources = {net_interrupt, uart_interrupt}`. UART remains PLIC source 1; source 2 is the network interrupt line. Linux's SiFive PLIC driver uses `riscv,ndev` to size the IRQ domain and initialize source enables, so this DTB field must describe the hardware, not only the currently used UART node.
- The default 2x1 XSBench image and older handoff discriminators now use the keep-divisor early console form, `earlycon=uart8250,mmio,0xfff0c2c000`, instead of forcing an early `,115200n8` divisor rewrite. `console=ttyS0,115200n8` is still kept for the normal 8250 console.
- Added `scripts/p3_make_build67_low_noise_console_image.sh`, which reuses the clean-delay path but adds `keep_bootcon loglevel=8 ignore_loglevel initcall_debug` and still omits high-frequency `B67S` prints. This is the next SD-image-only test: if it reaches an interactable shell, the short-term Build 67 workaround is to keep the boot console during 8250 handoff while the real console/IRQ path is fixed; if it still stalls, the remaining fault is a real kernel or interrupt-path stop after SunRPC.

## 2026-06-10 -- Build 67 clean-delay image stalls at RPC

- Wrote `build/huaprop3/sd_images/huaprop3_linux_xsbench_2x1_clean_delay.img` to the remote SD card and verified the SD readback SHA256 matched `7936040478b26de64c54b6104469bb35bd5276b46d041be3ce3f17a4abce0241`.
- Reprogrammed the unchanged Build 67 PDI/LTX. Vivado reported `DONE bit: HIGH`, refreshed debug hub `0x3ffc0000000`, and enumerated four ILAs.
- UART log `~/p3_uart_logs/ttyUSB0_20260610_215853_clean_delay.log` showed the bootrom completing the full 65,536-block SD-to-DDR payload copy, the DDR/SD payload word comparison, BBL entry, Linux 5.1.0-rc7, and `smp: Brought up 1 node, 2 CPUs`.
- Without the `B67S` diagnostic prints and without `keep_bootcon`/`initcall_debug`, this clean-delay image again stopped at `RPC: Registered tcp NFSv4.1 backchannel transport module.`. The log did not grow after 22:10:05 CST, and slow UART writes of `echo P3_CLEAN_DELAY_OK` plus `uname -a` produced no echo or shell output.
- Current conclusion: the nonzero-hart delay alone is not sufficient. The earlier SBI trace run proved Linux can progress past RPC into `serial8250_init` and `/bin/sh`, but that progress depends on the debug image's extra console/initcall visibility. The next useful discriminator is a low-noise `keep_bootcon loglevel=8 initcall_debug` image without the heavy BBL/SBI `B67S` print stream.
- Code inspection against `pulp-platform/linux` `ariane-v0.7` shows why the RPC line is a misleading stop marker. The line is emitted from the SunRPC `fs_initcall(init_sunrpc)` path; the next relevant stages are `rootfs_initcall(populate_rootfs)`, then device initcalls including `serial8250_init` and the OF 8250 platform probe. `keep_bootcon` prevents `register_console()` from unregistering the early UART bootconsole when the real `ttyS0` console enables, while `initcall_debug` prints `calling ...` and `initcall ... returned ...` before and after each initcall. Therefore a low-noise test should keep those two bootargs to distinguish a real post-RPC hang from a normal-console handoff/output-path failure.

## 2026-06-10 -- Build 67 clean-delay image generated

- Added `scripts/p3_make_build67_clean_delay_image.sh`, a lower-noise Build 67 SD-image generator for the next hardware test.
- The image preserves the keep-divisor bootargs (`earlycon=uart8250,mmio,0xfff0c2c000 console=ttyS0,115200n8 rdinit=/bin/sh init=/bin/sh`) and the 2-hart DTB, keeps the small nonzero-hart BBL delay from the SBI trace run, and removes all `B67S` BBL/SBI diagnostic prints plus `keep_bootcon`, `ignore_loglevel`, and `initcall_debug`.
- Generated artifacts: `build/huaprop3/sd_images/huaprop3_linux_xsbench_2x1_clean_delay.img` SHA256 `7936040478b26de64c54b6104469bb35bd5276b46d041be3ce3f17a4abce0241`, DTB SHA256 `179005957261afe3ebbc37808bb4035b039612f0dd9fb2227b1161303fe76982`, and BBL payload SHA256 `189e0204bdae33192bdef48f8c8c93b03d76f5029b05ee85453ade181cf4611f`.
- Local validation confirmed the generated DTB still contains `cpu@1`, and `grep -a B67S` does not match the clean-delay BBL binary. Use this image to test whether the nonzero-hart delay alone is enough to reach an interactable shell without the heavy trace UART traffic.

## 2026-06-10 -- Build 67 SBI trace reaches shell

- Tested `huaprop3_linux_xsbench_2x1_sbi_trace.img` on the unchanged Build 67 PDI/LTX after writing it to the remote SD card and reinserting it in P3. The capture `~/p3_uart_logs/ttyUSB0_20260610_210818_sbi_trace_continue.log` reached `B67S linux_entry_ready hart=0`, `B67S linux_enter hart=0`, `Linux version 5.1.0-rc7`, `smp: Brought up 1 node, 2 CPUs`, and `riscv_timer_init_dt: Registering clocksource cpuid [0] hartid [0]`.
- The old keep-divisor stop at `RPC: Registered tcp NFSv4.1 backchannel transport module.` is not the final stop: `initcall init_sunrpc` returned, `populate_rootfs` completed, `serial8250_init` ran, the normal AXI16550 console registered at `0xfff0c2c000`, and `/bin/sh: can't access tty; job control turned off` printed.
- The BBL/SBI trace shows many `B67S ipi_enter ... event=2` records from the `SBI_REMOTE_FENCE_I` path (`IPI_FENCE_I=0x2`), but no long `B67S ipi_wait` records. The logged `ipi_wait_done` records mean the sender observed the target HLS/MSIP state clear, so this run does not prove a stuck remote-fence wait.
- A slow UART write of `echo P3_B67_SBI_TRACE_OK` and `uname -a` did not appear in the same capture. Treat the trace image as a discriminator that proves Linux continues past the previous stop into shell launch, not as the final interactable-shell/XSBench validation image; its heavy BBL prints can swamp the console path being tested.

## 2026-06-10 -- SBI trace diagnostic scope documented

- Clarified what `huaprop3_linux_xsbench_2x1_sbi_trace.img` can diagnose: BBL-to-Linux boot-hart ordering, per-hart SBI timer programming, SBI IPI/remote-fence waits, Linux initcall progress after the previous RPC/NFS stop, and early/normal console handoff loss.
- Added the matching limitation: the image does not directly prove a final RTL bug; it narrows the failing software-visible contract so the next firmware or RTL change has a concrete target.

## 2026-06-10 -- Build 67 SBI trace image generator added

- Added `scripts/p3_make_build67_sbi_trace_image.sh`, a software-only Build 67 discriminator that preserves the keep-divisor bootargs and adds `keep_bootcon loglevel=8 ignore_loglevel initcall_debug`.
- The script builds `build/huaprop3/sd_images/huaprop3_linux_xsbench_2x1_sbi_trace.img` by copying the keep-divisor BBL build directory, embedding a refreshed 2-hart DTB, and instrumenting the BBL/SBI code paths that are active only after multicore bring-up.
- The real code path under test is `init_first_hart()` -> `wake_harts()` -> `boot_other_hart()` in BBL, followed by Linux's hart lottery and SBI timer/IPI/remote-fence calls. The previous keep-divisor log already showed `riscv_timer_init_dt: Registering clocksource cpuid [0] hartid [1]`, so Linux CPU0 was running on hart1 in that boot.
- Generated image SHA256: `7bb6ed65ff3bf7e6b1baf3dc067fd32c52faa758a0023a3440e6550f44694543`. The embedded BBL binary contains `B67S` markers for Linux handoff, `SBI_SET_TIMER`, and `send_ipi_many()` wait loops; a future hardware run should distinguish a real SBI/timer/IPI stall from a console handoff/output-loss case after the RPC/NFS line.

## 2026-06-10 -- Multicore Linux boot notes added

- Added a Build 67 multicore Linux bring-up note to `wiki/concepts/p3-migration-guide.md` explaining why a 2x1-only boot failure can appear even when the single-core Build 66 path is validated.
- The note records the current interpretation: Build 67 keeps the same UART, SPI-SD, DDR, and debug-hub shape as Build 66, but adds 2-hart DTB contents, BBL `MAX_HARTS=2`, secondary-hart wakeup, and Linux SMP paths that exercise CLINT/PLIC, timer/IPI, SBI, scheduler/RCU, and console-lock behavior absent from 1x1 boot.
- It also captures the keep-divisor lesson: avoid the explicit earlycon baud suffix during current 2x1 debug because leaving the bootrom/BBL-programmed UART divisor alone produced stable Linux ASCII output and 2-hart bring-up, while previous marker runs could end in high-bit UART output after `mret`.

## 2026-06-10 -- Build 67 keep-divisor image boots Linux SMP then stalls

- Wrote `build/huaprop3/sd_images/huaprop3_linux_xsbench_2x1_keep_divisor.img` to the remote SD card and verified the SD readback hash matched `2fd755442425b508c787ff1b1e601f56dc90a52ea9b49215788e1a4b35f3a980`.
- Reprogrammed the unchanged Build 67 PDI/LTX. Vivado reported `DONE bit: HIGH`, refreshed debug hub `0x3ffc0000000`, and enumerated four ILAs.
- UART0 logs `~/p3_uart_logs/ttyUSB0_20260610_173702.log` and `~/p3_uart_logs/ttyUSB0_20260610_174508.log` showed the bootrom completing SPI-mode SD init, GPT parsing, the full 65,536-block payload copy, and DDR/SD payload word comparison before entering BBL.
- The keep-divisor bootargs removed the explicit earlycon baud field; this run no longer ended in fixed high-bit garbled bytes after BBL. It printed `Linux version 5.1.0-rc7`, brought up `1 node, 2 CPUs`, and reached kernel networking/RPC initialization.
- The run did not reach `/ #`: UART output stopped at `RPC: Registered tcp NFSv4.1 backchannel transport module.` and stayed unchanged for more than 3 minutes. A slow `echo P3_KEEP_DIV_PROBE` write to `/dev/ttyUSB0` produced no prompt or echo, so the target was not an interactable shell with a hidden prompt.
- Next software-only discriminator: build a keep-divisor image with `keep_bootcon loglevel=8 initcall_debug` while preserving `earlycon=uart8250,mmio,0xfff0c2c000` and `console=ttyS0,115200n8`, to decide whether the kernel is truly stuck after RPC init or the console handoff loses output.

## 2026-06-10 -- Build 67 keep-divisor SD image generated

- Generated a software-only Build 67 discriminator image at `build/huaprop3/sd_images/huaprop3_linux_xsbench_2x1_keep_divisor.img`; no Vivado synthesis or PDI change was involved.
- The embedded 2x1 DTB keeps the AXI16550 UART node and `console=ttyS0,115200n8`, but changes bootargs from `earlycon=uart8250,mmio,0xfff0c2c000,115200n8 ...` to `earlycon=uart8250,mmio,0xfff0c2c000 console=ttyS0,115200n8 rdinit=/bin/sh init=/bin/sh`. This tests whether explicit earlycon baud parsing was reprogramming the 16550 divisor before the normal OF 8250 driver takes over.
- Generated artifacts: DTB `build/huaprop3/huaprop3_2x1_keep_divisor.dtb`, BBL payload `build/huaprop3/bbl_build67_2x1_keep_divisor.bin`, and BBL work directory `build/huaprop3/ariane-sdk/build-bbl-build67-keep_divisor/`.
- SHA256: image `2fd755442425b508c787ff1b1e601f56dc90a52ea9b49215788e1a4b35f3a980`, DTB `179005957261afe3ebbc37808bb4035b039612f0dd9fb2227b1161303fe76982`, BBL `26b26e83cec0e1f852b26b9ec77542cbebd11e377df9bed7bead98fe7a681a06`.

## 2026-06-10 -- Build 67 post-garbled UART input probe produced no response

- With the board still in the Build 67 marker-image post-`mret` state, started a fresh short `/dev/ttyUSB0` capture and slowly sent `CR`, `echo P3_B67_INPUT_TEST`, and `uname -a` at `115200 8N1`.
- The probe log `~/p3_uart_logs/ttyUSB0_20260610_112608_b67_input_probe.log` remained 0 bytes after the commands, so the target did not emit a prompt, command echo, shell output, or further garbled bytes in response.
- This does not by itself distinguish a dead/hung Linux path from UART divisor corruption, because a corrupted 16550 divisor would affect receive sampling as well as transmit output. It does rule out the optimistic interpretation that the current high-bit stream is merely harmless console-output interleaving while a usable shell remains available at 115200.
- Continue with software-side discriminators before any new synthesis: either rewrite and retest the normal `huaprop3_linux_xsbench_2x1.img`, or build a minimal post-`mret` payload that prints through the existing AXI16550 path without Linux reinitializing the UART.

## 2026-06-09 -- Build 67 marker-image retest reaches mret but needs normal-image rerun

- Reprogrammed the unchanged Build 67 PDI/LTX after the SD card was inserted back into the P3 board. Vivado again reported `DONE bit: HIGH`, refreshed debug hub `0x3ffc0000000`, and enumerated four ILAs.
- UART log `~/p3_uart_logs/ttyUSB0_20260609_103059_b67_retest.log` showed the bootrom completing SPI-mode SD init, GPT parsing, the full 65,536-block payload copy, and DDR/SD payload word comparison before entering BBL.
- The active SD payload still appears to be the marker image, not the normal XSBench image: the log prints `B67M boot_loader ...` and `B67M mret hart_arg=0x0000000000000000 entry=0x0000000080200000 dtb=0x0000000081000000`.
- This particular retest stopped after `mret` with a fixed high-bit byte stream and did not grow into a stable `Linux version` or `/ #` shell capture, unlike the earlier complete Build 67 logs that did show Linux and `/bin/sh`.
- The remote USB reader currently reports only 0B multi-reader slots, confirming the SD card is in the P3 board and cannot be rewritten from the remote host. The next fastest validation is to insert the SD card into the remote Ubuntu reader, write `build/huaprop3/sd_images/huaprop3_linux_xsbench_2x1.img`, then reinsert it into P3 and repeat the long UART capture plus throttled shell input test.

## 2026-06-09 -- Build 67 handoff correction: Linux banner is present

- Re-read the complete remote UART logs instead of only the shortened capture/tail snippets. The earlier Build 67 handoff interpretation was too conservative.
- `~/p3_uart_logs/ttyUSB0_20260609_093656_b67_bbl_markers.log` contains the `B67M` BBL markers and then prints `Linux version 5.1.0-rc7`, proving that Linux entry and early console output do occur after BBL `mret`.
- `~/p3_uart_logs/ttyUSB0_20260608_195222_b67_retest.log` for the normal 2x1 image also prints `Linux version 5.1.0-rc7` and reaches `Run /bin/sh as init process`.
- The active Build 67 task is therefore no longer BBL-to-Linux entry. The next gate is a fresh long capture plus throttled UART input to verify the `/bin/sh` prompt, `uname -a`, CPU topology, `/dev/piton_sd2` mount, and an XSBench smoke command on the 2x1 system.

## 2026-06-09 -- Build 67 BBL marker image reaches Linux mret handoff

- Wrote and tested `huaprop3_linux_xsbench_2x1_bbl_markers.img` with the unchanged Build 67 PDI/LTX; Vivado again reported `DONE bit: HIGH`, refreshed debug hub `0x3ffc0000000`, and enumerated four ILAs.
- UART log `~/p3_uart_logs/ttyUSB0_20260609_093656_b67_bbl_markers.log` showed the bootrom completing SPI-mode SD init, GPT parsing, the full 65,536-block payload copy, and DDR/SD payload word comparison before entering `bbl loader`.
- The BBL marker prints were visible: `B67M boot_loader dtb_in=0x0000000080006708 dtb_out=0x0000000081000000 disabled=0x0000000000000000` followed by `B67M mret hart_arg=0x0000000000000000 entry=0x0000000080200000 dtb=0x0000000081000000`.
- The board then emitted the same fixed high-bit/garbled byte pattern and no `Linux version` or `/ #` prompt. This proves Build 67 reaches the BBL `mret` boundary and hands hart0 to Linux at `0x80200000` with the DTB at `0x81000000`.
- The current debug target is now post-BBL software: Linux entry/decompression/relocation, early trap behavior, early console UART setup, or kernel payload/DTB compatibility. Avoid new Vivado synthesis until a software-only post-`mret` discriminator has been tried.

## 2026-06-08 -- Build 67 nosmp discriminator still stops before Linux banner

- Wrote `huaprop3_linux_xsbench_2x1_nosmp.img` to the remote SD card (`/dev/sdc`) and reprogrammed the unchanged Build 67 PDI/LTX.
- Vivado again reported `DONE bit: HIGH`, refreshed debug hub `0x3ffc0000000`, and enumerated four ILAs, so this run did not change the validated hardware/programming path.
- UART log `~/p3_uart_logs/ttyUSB0_20260608_225030_b67_nosmp_retry.log` showed the bootrom completing SPI-mode SD init, GPT parsing, all 65,536 payload blocks, DDR/SD readback match, and BBL entry.
- The BBL DTB contained bootargs with `maxcpus=1 nosmp`, and BBL changed `cpu@1` to `status = "masked"`, proving the nosmp discriminator was the active SD payload.
- The run still stopped before `Linux version` or `/ #`, ending with the same fixed high-bit/garbled bytes after the BBL DTB dump. This makes plain hart1/Linux SMP bring-up unlikely as the sole root cause; the next discriminator is `bbl_markers` to see whether BBL reaches entry selection and `mret` into Linux.

## 2026-06-08 -- Build 67 Linux handoff discriminator images verified

- Regenerated the two Build 67 SD-image-only Linux handoff discriminators with `scripts/p3_make_build67_linux_handoff_images.sh`; this still does not launch Vivado synthesis.
- Fixed the marker BBL binary self-check to use direct binary `grep -a -q 'B67M'` instead of `strings | grep -q`, because `set -o pipefail` can turn a successful early `grep -q` match into a false failure when `strings` receives SIGPIPE.
- Verified `huaprop3_linux_xsbench_2x1_nosmp.img` embeds `cpu@1 status = "disabled"` plus `maxcpus=1 nosmp`; image SHA256 is `714e07231ebb85c83bb9c89e5233fd801182a6523ce311530e03238f5e31241d`.
- Verified `huaprop3_linux_xsbench_2x1_bbl_markers.img` now differs from the original 2x1 image, contains the `B67M` BBL markers in partition 1, and byte-compares against `bbl_build67_2x1_bbl_markers.bin` at LBA 2048; image SHA256 is `048904f50e8ed101f4b595de7cbfa2936e6924e460e57113165454832be54911`.
- Next hardware order remains: write/test `nosmp` first, then write/test `bbl_markers` only if `nosmp` still stops before `Linux version` or `/ #`.

## 2026-06-08 -- Build 67 Linux handoff image discriminators added

- Added `scripts/p3_make_build67_linux_handoff_images.sh` to generate two SD-image-only Build 67 discriminators without launching Vivado synthesis.
- The `nosmp` image keeps the 2x1 hardware but marks `cpu@1` disabled in the BBL embedded DTB and adds `maxcpus=1 nosmp` to bootargs, so BBL should leave hart1 in its `wfi` loop and hand only hart0 to Linux.
- The `bbl_markers` image keeps the 2-hart DTB but disables the long BBL DTB dump and adds short `B67M` prints before `entry_point` selection, per-hart boot handoff, and `mret`.
- Intended use: write one image at a time to the remote SD card, reprogram the same Build 67 PDI/LTX, and compare whether the stop is SMP/hart1-related or occurs before/at the BBL `mret` into Linux.

## 2026-06-08 -- Build 67 2x1 SD image retest reached BBL-to-Linux boundary

- Reprogrammed `huaprop3_build67_2x1_baseline/debug_build/p3_top_build67_2x1_normal_spi_sd_boot.pdi` with the matching `.ltx` after writing the intended `build/huaprop3/sd_images/huaprop3_linux_xsbench_2x1.img` SD image.
- Vivado 2024.2.2 selected `xcvp1902_1`, reported `DONE bit: HIGH`, refreshed debug hub `0x3ffc0000000`, and enumerated four ILAs.
- Remote UART capture `~/p3_uart_logs/ttyUSB0_20260608_195222_b67_retest.log` printed the 2x1 bootrom banner (`#X-Tiles: 2`, `#Y-Tiles: 1`, `#Cores: 2`), initialized SPI-mode SD, copied all 65,536 payload blocks to DDR, and printed matching DDR/SD payload words before entering `bbl loader`.
- The BBL DTB printed on hardware now includes both `cpu@0` and `cpu@1`, plus two-hart CLINT/PLIC interrupt wiring. This clears the previous stale-SD-image hypothesis.
- Current stop: after the BBL DTB dump, UART emits a short fixed high-bit/garbled byte stream and no `Linux version` or shell prompt appears. The active debug layer is now BBL-to-Linux payload handoff, Linux early console/serial setup, or 2-hart Linux/SMP compatibility, not PDI programming, UART hardware, SPI-SD transport, DDR copy, or the SD image hart count.

## 2026-06-05 -- Build 67 hardware reached BBL with stale single-hart SD payload

- Programmed `huaprop3_build67_2x1_baseline/debug_build/p3_top_build67_2x1_normal_spi_sd_boot.pdi` with the matching `.ltx` through `hw_server 100.93.77.36:3121` and XVC `202.197.4.99:2540`.
- Vivado selected `xcvp1902_1`, reported `Successfully programmed PDI`, confirmed `DONE bit: HIGH`, refreshed debug hub `0x3ffc0000000`, and enumerated four ILAs (`hw_ila_1` through `hw_ila_4`).
- Remote UART capture `~/p3_uart_logs/ttyUSB0_20260605_175423_b67.log` printed the OpenPiton+Ariane banner, initialized SPI-mode SD, parsed the GPT payload partition, copied all 65,536 payload blocks into DDR, and printed matching DDR/SD payload words before entering `bbl loader`.
- This validates the Build 67 PDI/LTX, board programming path, Versal debug runtime path, AXI16550 UART, SPI-mode SD block-read path, DDR payload copy, and BBL-entry handoff.
- The BBL DTB printed from the board still contained only `cpu@0`; it did not include `cpu@1`. Local inspection of `build/huaprop3/sd_images/huaprop3_linux_xsbench_2x1.img` confirms that the intended 2x1 image does contain `cpu@1` and 2-hart CLINT/PLIC interrupt wiring.
- Current conclusion: Build 67 hardware is good through BBL entry, but this run booted a stale single-hart SD payload from the card currently inserted in the P3 board. The remote USB reader currently shows only 0B slots, so the next validation step is to insert the SD card into the remote Ubuntu reader, write `huaprop3_linux_xsbench_2x1.img`, reinsert it into P3, and reprogram/capture Build 67 again.

## 2026-06-05 -- Build 67 2x1 implementation completed

- Reran Build 67 from a clean repository-local self-contained workspace `p3b67_2x1/source_snapshot/` using `scripts/p3_build67_2x1_normal_spi_sd_boot.tcl`.
- The 2x1 tile configuration now regenerates the full PyHP tile-dependent set before Vivado project creation, so `chip.tmp.v`, `chipset_impl.tmp.v`, coordinate helpers, and `define.tmp.h` agree on `PITON_X_TILES=2`, `PITON_Y_TILES=1`, and `PITON_NUM_TILES=2`.
- Vivado 2024.2.2 completed top synthesis, implementation, route, and `write_device_image` with 0 errors. Route status reported 258,405 routable nets, all fully routed, with 0 routing errors.
- Final timing met all user constraints with `WNS=16.312 ns`, `TNS=0`, `WHS=0.005 ns`, `THS=0`, `WPWS=0.063 ns`, and `TPWS=0`.
- Place utilization was 154,407 CLB LUTs, 102,308 CLB registers, 165 BRAM tiles, 4 URAMs, and 36 DSP slices.
- Published `huaprop3_build67_2x1_baseline/debug_build/p3_top_build67_2x1_normal_spi_sd_boot.pdi` with SHA256 `f979264a5e43e5f1e90061590da79ed0d54c080e7aac384762219f993c978ed0` and the matching `.ltx` with SHA256 `7b9c348e4d2695dfd90f700aabc8e4eee66288d07dd7a4c81ea636e33d2aeaaf`.
- Build 67 is now a hardware-validation candidate, not yet a replacement for the Build 66 validated baseline. The next gate is P3 programming, debug-hub/ILA refresh, UART boot capture, and Linux/SMP behavior with the 2-hart SD image.

## 2026-06-05 -- Build 67 tile PyHP define mismatch fixed

- The fresh repository-local Build 67 attempt reached top synthesis and failed during RTL elaboration, not implementation. Vivado reported out-of-range accesses in `source_snapshot/piton/design/chip/rtl/chip.tmp.v` for tile 1 interrupt/debug ports: `debug_req_i[1]`, `timer_irq_i[1]`, and `irq_i[3:2]`.
- Root cause: `chip.tmp.v` had been regenerated for the 2x1 mesh, but `source_snapshot/piton/design/include/define.tmp.h` still defined `PITON_NUM_TILES=1`, `PITON_X_TILES=1`, and `PITON_Y_TILES=1`. That made the chip instantiate tile 1 while Vivado sized the shared CLINT/PLIC/debug vectors for one tile.
- Updated the shared P3 Build 52/66/67 runner so PyHP regeneration treats the tile-dependent generated files as one consistency set: `define.tmp.h`, `chip.tmp.v`, `chipset_impl.tmp.v`, `flat_id_to_xy.tmp.v`, and `xy_to_flat_id.tmp.v`.
- Added early tile-define validation for both the live regenerated `piton/design/include/define.tmp.h` and the self-contained `<workdir>/source_snapshot/piton/design/include/define.tmp.h`, so future multicore builds fail before long Vivado runs if the generated tile macros do not match `PITON_X_TILES`, `PITON_Y_TILES`, and `PITON_NUM_TILES`.

## 2026-06-05 -- Build 66 recursive snapshot hardware validation passed

- Programmed the freshly rebuilt self-contained Build 66 artifact `huaprop3_build66_baseline/debug_build/p3_top_build66_normal_spi_sd_boot.pdi` with the matching `.ltx` through `hw_server 100.93.77.36:3121` and XVC `202.197.4.99:2540`.
- Vivado selected `xcvp1902_1`, reported `Successfully programmed PDI`, confirmed `DONE bit: HIGH`, refreshed debug hub `0x3ffc0000000`, and enumerated four ILA cores (`hw_ila_1` through `hw_ila_4`).
- Remote UART capture `~/p3_uart_logs/ttyUSB0_20260605_101439.log` printed the OpenPiton+Ariane banner, initialized SPI-mode SD, parsed/copy-loaded the 65,536-block payload, entered `bbl loader`, and booted Linux 5.1.0-rc7.
- Linux registered the AXI16550 console at `0xfff0c2c000`, enumerated `piton_sd1` and `piton_sd2`, reached `Run /bin/sh as init process`, and produced the `/ #` shell prompt.
- Slow UART input passed an interaction check: `echo P3_B66_SELF_OK` returned `P3_B66_SELF_OK`, and `uname -a` returned `Linux ariane-fpga 5.1.0-rc7 ... riscv64 GNU/Linux`.
- This closes the Build 66 baseline gate for Build 67: the current repository-local self-contained source snapshot flow, PDI/LTX publication, P3 programming path, UART, SPI-mode SD, DDR copy, BBL, Linux console, and shell input are all validated.

## 2026-06-05 -- Build 66 recursive snapshot rerun completed

- Reran Build 66 from a fresh repository-local `p3b66/` workspace after fixing recursive include snapshot copying. The project used `p3b66/source_snapshot/` sources; self-contained XPR/fileset validation passed with no live `piton/`, stale `Z:/tmp`, or old `D:/p3b*` references.
- Confirmed nested Ariane/CVA6 include files such as `register_interface/assign.svh` and `register_interface/typedef.svh` are present in `p3b66/source_snapshot/`, allowing top synthesis to complete cleanly.
- Vivado 2024.2.2 completed OOC IP/debug synthesis, top synthesis, implementation, routing, DRC, and `write_device_image` with 0 errors. Route status reported 151,579 routable nets, all fully routed, with 0 routing errors.
- Final timing met all user constraints with `WNS=16.514 ns`, `TNS=0`, `WHS=0.013 ns`, `THS=0`, `WPWS=0.063 ns`, and `TPWS=0`.
- Republished `huaprop3_build66_baseline/debug_build/p3_top_build66_normal_spi_sd_boot.pdi` at 10:03 local time with SHA256 `ee30fe4d052c763c9fc2fa72bf71cc8363173ad210ea8fb106533ac3081dd62d`; the matching LTX SHA256 remains `7b9c348e4d2695dfd90f700aabc8e4eee66288d07dd7a4c81ea636e33d2aeaaf`.
- Enhanced `scripts/p3_program_pdi.tcl` so the normal programming command can take an optional LTX, set `PROBES.FILE`, print the DONE bit, refresh the hardware device, and list discovered ILAs before UART boot validation.

## 2026-06-05 -- Build 66 self-contained include snapshot fix

- The first fresh self-contained Build 66 rerun from `p3b66/source_snapshot/` reached top synthesis but failed RTL elaboration because the snapshot copied only the first level of each include directory.
- Vivado reported missing Ariane/CVA6 relative includes `register_interface/assign.svh` and `register_interface/typedef.svh` from `corev_apu/register_interface/include/`, which left `REG_BUS_TYPEDEF_ALL`, `plic_req_t`, and `plic_rsp_t` undefined in `riscv_peripherals.sv`.
- Updated `scripts/p3_create_bd.tcl` so self-contained include snapshots recursively copy RTL/include files while preserving subdirectory paths. Build 66 must be relaunched from a fresh `p3b66/` workdir and pass the same no-live-path validation before any Build 67 work resumes.

## 2026-06-05 -- Build 66 self-contained baseline rebuild flow added

- Added `P3_SELF_CONTAINED_SOURCES=1` for Build 66 and Build 67 wrappers so baseline and scaling Vivado projects are created from a project-local `<workdir>/source_snapshot/` instead of live repository RTL paths.
- Updated `scripts/p3_create_bd.tcl` to copy RTL, headers, constraints, `p3_top.v`, `openpiton_wrapper.v`, XPM wrappers, and the Vivado unread shim into the snapshot before adding them to `sources_1` or `constrs_1`.
- Updated the Build 52-derived runner and prepare script to validate self-contained `.xpr`/fileset/include paths and to reject live `piton/`, stale `Z:/tmp`, or old `D:/p3b*` references for Build 66/67. Build 66 must be rebuilt and revalidated with this rule before Build 67 resumes.

## 2026-06-05 -- Build 67 repo-local project corruption diagnosed

- Ran the read-only `scripts/p3_probe_build67_project.tcl` probe against `p3b67_2x1/huaprop3_build67_2x1_baseline.xpr`.
- The probe failed during `open_project`, before RTL synthesis, because the previous interrupted creation left incomplete generated BD/IP output products under `p3b67_2x1`: AXI NoC child `.xci` files and several `axis_ila_*` nested IP files were missing.
- This identifies the current Build 67 blocker as a corrupt half-generated Vivado workspace, not a 2x1 OpenPiton/Ariane RTL compile failure. The next action is to discard the generated `p3b67_2x1` work directory and recreate it cleanly inside the repository root.

## 2026-06-05 -- Build 67 repo-local project probe added

- Added `scripts/p3_probe_build67_project.tcl` as a read-only sanity check for the repository-local Build 67 Vivado project before relaunching long synthesis.
- The probe opens `p3b67_2x1/huaprop3_build67_2x1_baseline.xpr`, reports the fileset top, Verilog defines, source/constraint counts, existing runs, and the four BD-owned `axis_ila_*` cells with their probe counts and data depths.
- This is intended to distinguish a repo-local Windows/WSL project-open or BD-load problem from an actual 2x1 RTL synthesis failure. It does not modify the project or regenerate output products.

## 2026-06-05 -- Build 67 switched back to repo-local workdir

- Updated `scripts/p3_build67_2x1_normal_spi_sd_boot.tcl` so Build 67 now creates the Vivado work project under the repository-local `p3b67_2x1/` directory by default, per the current workspace organization decision.
- Kept the Build 67 publication path unchanged: PDI/LTX artifacts still land under `huaprop3_build67_2x1_baseline/debug_build/`, and the 2x1 XSBench SD image remains `build/huaprop3/sd_images/huaprop3_linux_xsbench_2x1.img`.

## 2026-06-04 -- Build 67 moved to short Vivado work path

- First Build 67 Vivado launch entered the 2x1 Tcl flow, regenerated the `MAX_HARTS=2` bootrom, regenerated PyHP output, and created the project under `Z:/home/illya/openpiton/p3b67_2x1`, but the session stopped during early BD/IP setup at AXI NoC creation without synthesis artifacts.
- Updated `scripts/p3_build67_2x1_normal_spi_sd_boot.tcl` so the default work project is `D:/p3b67_2x1`, preserving the validated Build 66 short-path rule for Versal BD/IP generation. Published artifacts still go to `huaprop3_build67_2x1_baseline/debug_build/`.

## 2026-06-04 -- Build 67 2x1 multicore flow prepared

- Parameterized the shared P3 Build 52/66 Vivado runner and PyHP regeneration path so `PITON_X_TILES`, `PITON_Y_TILES`, and `PITON_NUM_TILES` can be supplied by a wrapper while defaulting to the existing 1x1 baseline.
- Added `scripts/p3_build67_2x1_normal_spi_sd_boot.tcl` as the first 2x1 Ariane wrapper. It keeps the Build 66 AXI16550 UART, SPI-mode SD block-read path, DDR address translation, and compact four-ILA debug topology, but publishes a separate `huaprop3_build67_2x1_baseline` project.
- Added `scripts/p3_rebuild_build67_2x1_normal_spi_sd_boot.sh` and `scripts/p3_make_build67_2x1_xsbench_image.sh` so the bootrom, forced AXI16550 BBL DTB, and XSBench SD image can all be regenerated with two harts before hardware validation.
- Fixed the Build 67 image script to pass `-fno-stack-protector -U_FORTIFY_SOURCE -D_FORTIFY_SOURCE=0` into the forced AXI16550 BBL rebuild; without those inherited CFLAGS the cross linker resolved stack-protector and fortified libc references that are not available in the freestanding BBL link.

## 2026-06-04 -- Build 66 repo-local PDI hardware boot verified

- Programmed the repo-local rerun artifact `huaprop3_build66_baseline/debug_build/p3_top_build66_normal_spi_sd_boot.pdi` through `hw_server 100.93.77.36:3121` and XVC `202.197.4.99:2540`. Vivado 2024.2.2 selected `xcvp1902_1`, reported `Successfully programmed PDI`, and confirmed `DONE bit: HIGH`.
- Captured `/dev/ttyUSB0` at 115200 8N1 before programming. The bootrom printed the OpenPiton+Ariane banner, initialized SPI-mode SD, parsed the GPT, copied the 65,536-block boot payload, and showed matching payload words at DDR `0x80000000` and SD sector 2048.
- BBL entered successfully, printed the AXI16550 DTB, and booted Linux 5.1.0-rc7 with `earlycon=uart8250,mmio,0xfff0c2c000,115200n8 console=ttyS0,115200n8 rdinit=/bin/sh init=/bin/sh`. Linux reached `/ #` and registered the console UART at `0xfff0c2c000`.
- Linux enumerated `piton_sd`, `piton_sd1`, and `piton_sd2`. Slow UART input mounted the XSBench ext2 partition with `mount -t ext2 -o ro /dev/piton_sd2 /mnt`; the kernel reported the filesystem mounted without a journal, and `/mnt/XSBench` plus `/mnt/run_xsbench.sh` were visible.
- An automated `/mnt/XSBench -s small -p 1 -l 1` launch was sent after mounting, but this run did not return an `XS_DONE` marker before the serial session was closed. Treat Build 66 hardware boot, Linux shell, SD block device, and ext2 mount as verified; treat XSBench completion timing as a separate benchmark-run procedure item rather than a hardware bring-up blocker.

## 2026-06-04 -- Build 66 repo-local rerun completed

- Reran the Build 66 baseline from the repository-root Vivado work directory `p3b66/` using `scripts/p3_build66_normal_spi_sd_boot.tcl -tclargs -jobs 1`. Vivado 2024.2.2 completed bootrom rebuild, PyHP regeneration, BD generation/validation, four BD-owned ILA OOC runs, top synthesis, implementation, route, and `write_device_image` with exit code 0.
- The implementation reused the seeded DDR PHY/IP cache from `p3b66_validated_snapshot/`; `opt_design` reported the cached `noc_mc_ddr4_phy` synthesis design with cache ID `dddc3f069a736d89`.
- Published the repo-local artifacts under `huaprop3_build66_baseline/debug_build/`: `p3_top_build66_normal_spi_sd_boot.pdi` has SHA256 `464fa3b4e3c96c52cfee87dff3b128bbd65efaeb77d81624c95a34c3f631eca5`, and `p3_top_build66_normal_spi_sd_boot.ltx` has SHA256 `7b9c348e4d2695dfd90f700aabc8e4eee66288d07dd7a4c81ea636e33d2aeaaf`.
- The regenerated LTX still contains `AXI_DEBUG_HUB_V1` at `0x000003FFC0000000`, access path `u_bd/openpiton_top_i/ps_wizard_0/PMC_AXI_NOC0`, and `axis_ila_0` through `axis_ila_3`.
- Route status reported 151,579 routable nets, all fully routed, and 0 routing errors. The final timing summary met all user constraints with `WNS=16.514 ns`, `TNS=0`, `WHS=0.013 ns`, `THS=0`, `WPWS=0.063 ns`, and `TPWS=0`.
- Place utilization for the repo-local rerun was 87,430 CLB LUTs, 62,786 CLB registers, 89.5 block RAM tiles, 2 URAMs, and 18 DSP slices.

## 2026-06-04 -- Build 66 repo-local rerun prepared

- Copied the full previously validated `/mnt/d/p3b66` Vivado workspace into the repository root as local ignored directory `p3b66_validated_snapshot/` for recovery and DDR PHY/IP cache seeding.
- Updated the Build 66 wrapper so fresh reruns use repository-root work directory `p3b66/` by default while still publishing PDI/LTX under `huaprop3_build66_baseline/debug_build/`.
- Added `P3_BUILD66_WORK_DIR` as the Build 66 override knob for path-specific Vivado debugging without changing the baseline project name.

## 2026-06-04 -- Build 66 baseline workspace cleanup

- Promoted Build 66 to the canonical P3 OpenPiton+Ariane baseline after Linux shell, SD ext2 mount, and XSBench smoke launch were verified.
- Created `huaprop3_build66_baseline/` for the local baseline artifacts and updated the Build 66 Vivado wrapper to publish future rebuilds there while using `D:/p3_baseline_build66` as the clean work directory.
- Archived historical repo-local debug projects under `/home/illya/p3_cleanup_archive/2026-06-04-build66-baseline/repo_dirs/` and old numbered D-drive Vivado workspaces under `/mnt/d/p3_cleanup_archive/2026-06-04-build66-baseline/workspaces/`.
- Kept `huaprop3onecore/` as the board-level Chipyard/Rocket reference for UART, SD, and ILA comparisons.

## 2026-06-04 -- Build 66 XSBench ext2 image boots and launches benchmark

- Replaced the first XSBench data partition experiment with a no-journal ext2 image, `build/huaprop3/sd_images/huaprop3_linux_xsbench_ext2.img`, to avoid the ext3/ext4 journal mount path observed in the earlier test. The full image is 192 MiB and has SHA256 `9f51e506ce65aabeaf09db18a026adf9bbba2437bc05cbe653f8b625c27271ea`.
- Wrote the ext2 image to the remote Ubuntu SD reader target `/dev/sdc` (`Multi-Reader -1`, 32 GB, USB removable) and verified the first 192 MiB readback SHA256 matched the image before reinserting the card into P3.
- Reprogrammed `huaprop3_build66_normal_spi_sd_boot/debug_build/p3_top_build66_normal_spi_sd_boot.pdi`; Vivado reported `Successfully programmed PDI` and `DONE bit: HIGH`. The bootrom initialized SPI-mode SD, parsed GPT, copied the 65,536-block BBL/Linux payload, printed matching DDR/SD payload words, entered BBL, booted Linux 5.1.0-rc7, and reached the `/ #` shell with `rdinit=/bin/sh init=/bin/sh`.
- Linux enumerated the SD block device as `piton_sd`, with partitions `piton_sd1` and `piton_sd2`. Manual setup of `devtmpfs`, `proc`, and `sysfs` exposed `/dev/piton_sd2`; `mount -t ext2 -o ro /dev/piton_sd2 /mnt` succeeded, with the kernel reporting `mounted filesystem without journal`.
- The mounted second partition contained `/XSBench` (717,224 bytes) and `/run_xsbench.sh`. `/mnt/XSBench -s small -l 100` launched successfully and printed the XSBench v20 banner and input summary, including `Particle Histories: 500,000`, `XS Lookups per Particle: 100`, and `Threads: 1`, before entering initialization.
- Manual PuTTY use should connect to `/dev/ttyUSB0` at `115200 8N1` with flow control disabled. Do not paste large command blocks at once: bulk UART input causes `ttyS0 input overrun(s)`. For a shortest smoke test, use `/mnt/XSBench -s small -p 1 -l 1`; `-p` controls particle histories and `-l` controls lookups per particle in history-based mode.

## 2026-06-04 -- Build 66 XSBench SD image prepared

- Generated `build/huaprop3/sd_images/huaprop3_linux_xsbench.img` as a 192 MiB follow-up to the verified Build 66 shell image. Partition 1 keeps the proven shell BBL payload from `huaprop3_linux_shell.img`; partition 2 is an ext3 test partition named `PITON_XSBENCH`.
- Copied the existing statically linked RISC-V XSBench binary from `build/a7203x/XSBench/openmp-threading/XSBench` into partition 2 as `/XSBench`, together with `/run_xsbench.sh`, which runs `./XSBench -s small -l 100`.
- Verified the image layout with `sfdisk`: partition 1 starts at LBA 2048 with 131,072 sectors, and partition 2 starts at LBA 135,168. `debugfs` confirmed `/XSBench` is executable and 717,224 bytes.
- Verified partition placement by extracting partition 2 from the whole image at LBA 135,168 and matching it against the standalone ext3 payload SHA256 (`bd80c66b1ba27b2fff36fcb6b24d0fe9ac2c6942ea2ae46886dd46303ebe762a`). The full image SHA256 is `1a811754b05fe01b3c5803a815fb112732f3033061cddea8406fd2b3443511c8`.

## 2026-06-04 -- Build 66 Linux shell interaction verified

- Reinserted the SD card into the P3 board and reprogrammed `huaprop3_build66_normal_spi_sd_boot/debug_build/p3_top_build66_normal_spi_sd_boot.pdi` through `hw_server 100.93.77.36:3121` and XVC `202.197.4.99:2540`. Vivado reported `Successfully programmed PDI` and `DONE bit: HIGH`.
- The shell-image boot completed past bootrom, GPT parsing, the 65,536-block SD-to-DDR copy, BBL entry, DTB handoff, Linux kernel boot, AXI16550 console registration, and userspace handoff. The active `/dev/ttyUSB0` log reached `Run /bin/sh as init process`, `/bin/sh: can't access tty; job control turned off`, and the `/ #` prompt.
- The forced bootargs were visible both in the BBL DTB dump and the Linux kernel command line: `earlycon=uart8250,mmio,0xfff0c2c000,115200n8 console=ttyS0,115200n8 rdinit=/bin/sh init=/bin/sh`.
- Bulk-writing multiple UART commands at once triggered `ttyS0 input overrun(s)` and corrupted input characters. Slow per-character writes at about 200 ms per byte worked: `echo P3_SLOW_OK` returned `P3_SLOW_OK`, and `uname -a` returned `Linux ariane-fpga 5.1.0-rc7 ... riscv64 GNU/Linux`.
- This closes the Build 66 hardware bring-up loop through an interactive Linux shell on the current P3 board. The earlier standard-image stop at `Initializing random number generator...` is now a rootfs init-script policy issue, not a UART, SD, DDR, BBL, kernel-entry, or console-output hardware blocker.

## 2026-06-04 -- Build 66 Linux userland and shell-image write

- Follow-up inspection of the Build 66 UART log corrected the earlier BBL-only interpretation. The same `/dev/ttyUSB0` capture did contain the Linux banner (`Linux version 5.1.0-rc7`), many duplicated `initcall_debug` lines, `Run /init as init process`, and userland output through `Starting logging: OK`.
- The current stop is in the rootfs init stage at `Initializing random number generator...`. This moves the active blocker above hardware transport and above BBL/Linux handoff: UART, SPI-SD init, GPT parsing, SD block copy, DDR write/readback, BBL entry, kernel entry, and Linux console output are all proven for Build 66.
- Prepared `build/huaprop3/sd_images/huaprop3_linux_shell.img`, a 128 MiB debug image whose BBL embedded bootargs use `rdinit=/bin/sh init=/bin/sh` to bypass normal init scripts. It is intended to distinguish a rootfs random-seed/init-script stall from a broader userspace or console-interaction fault.
- Wrote the local shell image to the SD card inserted in the remote Ubuntu host. The remote target was verified as `/dev/sdc`, size `31914983424`, `TYPE=disk`, `TRAN=usb`, `RM=1`, model `Multi-Reader -1`; the empty 0B reader slots were not used. The image SHA256 was `c65920c722f1c767b7538b0d2c9d5d1e7e3f00bb8ca7737113b5d1f085cfd66c`.
- The write used the remote `/tmp/huaprop3_linux_shell.img` copy and `dd if=/tmp/huaprop3_linux_shell.img of=/dev/sdc bs=4M conv=fsync status=progress`. A readback of the first 128 MiB from `/dev/sdc` produced the same SHA256, so the SD image write is verified.
- Added the remote SD-card image-write flow to `AGENTS.md`, including the requirement to identify the real removable disk with `lsblk`, avoid the 0B multi-reader slots, verify image hashes, write the whole disk device, and read back the written span.

## 2026-06-04 -- Build 66 normal SPI-SD boot reaches BBL

- Build 66 completed from `D:/p3b66` with 0 hard errors through synthesis, implementation, routing, and `write_device_image`. The published artifacts are `huaprop3_build66_normal_spi_sd_boot/debug_build/p3_top_build66_normal_spi_sd_boot.pdi` and `.ltx`.
- Programming through `hw_server 100.93.77.36:3121` and XVC `202.197.4.99:2540` succeeded; Vivado reported `DONE bit: HIGH`. UART capture was started before programming on the remote Ubuntu host. `/dev/ttyUSB0` is the active channel and `/dev/ttyUSB1` stayed silent.
- UART output proved the normal bootrom path now runs: it printed the OpenPiton+Ariane banner, initialized SPI-mode SD, read the GPT header and partition entry, copied the 65,536-block boot image from LBA 2048, and printed `done!`.
- The bootrom readback checks matched the SD payload in DDR: `DDR[0x80000000] = 0x340111731F80006F` and `SD[sect2048] = 0x340111731F80006F`. It then entered `bbl loader` and printed the debug device tree, including the AXI16550 `ns16550` UART node at `0xfff0c2c000`.
- No clean `Linux version` banner has appeared yet after the BBL device-tree print. The remaining issue is therefore above the basic hardware transport layer: BBL/Linux handoff, payload format, DTB/bootargs, or Linux console behavior. UART hardware, SPI-SD init, block reads, bootrom GPT copy, DDR writes, and BBL entry are no longer the current blockers.

## 2026-06-03 -- Build 66 normal SPI-SD boot build prepared

- Added Build 66 as the normal-boot successor to the validated Build 65 SPI-mode SD transport. Build 66 keeps the original AXI16550 UART path, the Build 65 SPI-SD block-read hardware path, DDR address translation, and the compact four-ILA layout.
- Build 66 replaces the Build 52/65 no-stack `asm_uart16550_sdprobe` bootrom with the normal C bootrom (`BOOTROM_MODE=normal`), so the expected runtime is GPT partition discovery, BBL copy to DDR, jump to `0x80000000`, then Linux/BBL UART output rather than the intentional repeated `A` loop.
- The Build 52-derived Vivado runner now accepts an override bootrom rebuild script and correctly propagates derived SPI-SD macros through both the runner and prepare scripts. Build 66 uses `P3_SPI_SD_BOOT P3_SPI_SD_BLOCK_DEBUG` and rejects UART-only assembly bootrom symbols during rebuild.
- Added a Build 66 ILA capture wrapper and CSV decoder so a failed boot can be separated between SD block-read response, DDR AXI response, core/L15 progress, and UART/bootrom handoff without widening probes before the first normal-boot test.

## 2026-06-03 -- P3 remote UART capture flow documented

- Added the P3 remote programming/UART capture workflow to `AGENTS.md`: start `/dev/ttyUSB0` and `/dev/ttyUSB1` capture on `illya@100.93.77.36` before programming, then program through `hw_server 100.93.77.36:3121` and XVC `202.197.4.99:2540`, and inspect saved UART logs with `cat -v`/`xxd`.
- Reprogrammed Build 65 while both remote FT2232 channels were being captured. `/dev/ttyUSB0` produced 112 bytes of ASCII `A`; `/dev/ttyUSB1` produced no output. This proves the board UART physical path is healthy and that `/dev/ttyUSB0` is the active channel.
- The repeated `A` output is not evidence of a wrong board. Build 65 inherits the Build 52 no-stack AXI16550 SD-probe flow, whose bootrom rebuild script uses `BOOTROM_MODE=asm_uart16550_sdprobe` and intentionally loops on `uart_putc 0x41` after the SD probe. Builds 57-65 are therefore SD-transport diagnostic images, not normal GPT/BBL/Linux boot images.

## 2026-06-03 -- P3 Linux SD images prepared

- Prepared two GPT-formatted 128 MiB SD-card images under `build/huaprop3/sd_images/` for the post-Build-65 boot handoff debug step. Both images use partition 1 starting at LBA 2048, matching the OpenPiton bootrom GPT loader expectation.
- `huaprop3_linux_standard.img` contains the existing AXI16550 Linux BBL payload from `build/huaprop3/bbl.bin`. The payload starts at LBA 2048 and was byte-compared against the source BBL.
- `huaprop3_linux_debug.img` contains `build/huaprop3/bbl_debug_axi16550_force_dtb.bin`, rebuilt from riscv-pk with `PK_PRINT_DEVICE_TREE` enabled. This debug BBL forces the embedded AXI16550 debug DTB so Linux receives `earlycon=uart8250,mmio,0xfff0c2c000,115200n8 console=ttyS0,115200n8 loglevel=8 ignore_loglevel initcall_debug` even if the bootrom-passed DTB lacks `bootargs`.

## 2026-06-03 -- Build 65 SPI SD block-read hardware PASS

- Build 65 completed from `D:/p3b65` and published `p3_top_build65_spi_sd_block_debug.pdi/.ltx`. Implementation had 0 hard errors; route status reported 151,476 routable nets, 151,476 fully routed nets, and 0 routing errors. Timing met constraints with `WNS=17.295 ns`, `TNS=0`, `WHS=0.016 ns`, `THS=0`, `WPWS=0.063 ns`, and `TPWS=0`.
- Programming through `hw_server 100.93.77.36:3121` plus XVC `202.197.4.99:2540` succeeded with `DONE bit: HIGH`. The Build 65 LTX refreshed the AXI debug hub at `0x3ffc0000000`, found all four `axis_ila_*` cores, and exported CSV captures.
- The decoder passed the full mapped SD block-read path: AXI AR fired, the `axi_sd_bridge` launched an SD request, the transaction manager accepted it, wrote block transaction type/control registers, observed transaction status done, copied RX bytes into cache, generated a bridge response, entered `SEND_RESP`, and returned an AXI R response to NoC. The capture also observed nonzero SD data and no transaction-manager read-error sticky bit.
- This closes SD pins, power/control, explicit `IOBUF`, SPI init command response, SPI block-read transport, cache fill, and AXI response as the current blockers. The next debug target is above the SD transport layer: SD image contents, GPT/BBL layout, bootrom copy/cache behavior, exception path, or later UART logging.

## 2026-06-03 -- Build 65 SPI SD block-read debug implemented

- Added Build 65 as the direct follow-up to the Build 64 normal SPI SD pass. It keeps the original AXI16550 UART, the Build 52 four-ILA BD shape, the explicit SD `IOBUF` pad boundary, and the normal OpenCores SPI SD path.
- Build 65 enables `P3_SPI_SD_BOOT P3_SPI_SD_BLOCK_DEBUG` from `D:/p3b65` and repacks the existing SD ILA payload around the post-initialization block-read path instead of the init-history path.
- The new sticky/debug buses distinguish AXI SD mapped-read launch, `axi_sd_bridge` cache miss handling, `sd_wishbone_transaction_manager` Wishbone register programming, block transaction status/error, RX FIFO byte copy, cache fill, and AXI read response back to NoC.
- A focused Verilator lint of `axi_sd_bridge` plus the transaction-manager dependency chain passed with only legacy incomplete-case warnings before launching Vivado. The expected Build 65 decision is whether the first mapped SD block read reaches an AXI read response; if it does, remaining boot debug moves to SD contents and bootrom/BBL handoff.

## 2026-06-03 -- Build 64 normal SPI SD path hardware PASS

- Build 64 completed from `D:/p3b64`, generated `p3_top_build64_spi_sd_iobuf_history_debug.pdi/.ltx`, and programmed successfully through `hw_server 100.93.77.36:3121` plus XVC `202.197.4.99:2540`; Vivado reported `DONE bit: HIGH`.
- Implementation completed with 0 errors. Route status reported 151,502 routable nets, 151,502 fully routed nets, and 0 routing errors. Post-route timing met constraints with `WNS=17.427 ns`, `TNS=0`, `WHS=0.012 ns`, `THS=0`, `WPWS=0.063 ns`, and `TPWS=0`.
- The Build 64 LTX refreshed the AXI debug hub at `0x3ffc0000000`, found all four `axis_ila_*` cores, triggered/uploaded them, and exported CSV files.
- The decoder passed for the normal OpenCores SPI SD path: NoC and Wishbone transactions fired, `spi_clk_toggle_seen=1`, `miso_low_seen=1`, `resp_tout=0`, and the init history reached `CMD0`, `CMD8`, `CMD55`, `ACMD41`, and `INIT_DONE`.
- This closes the first SD card-response boundary in the full OpenPiton shell while keeping the original AXI16550 UART path. The next debug target is later SPI block-read/data return and the bootrom/BBL handoff, not SD pins, power, explicit `IOBUF`, XVC/debug hub, or initial command-response timing.

## 2026-06-03 -- Build 63 hardware PASS and Build 64 normal SPI retry planned

- Build 63 completed implementation from `D:/p3b63`, generated `p3_top_build63_spi_sd_iobuf_ref_cmd_debug.pdi/.ltx`, and programmed successfully through `hw_server 100.93.77.36:3121` plus XVC `202.197.4.99:2540`; Vivado reported `DONE bit: HIGH`.
- The Build 63 LTX refreshed through the AXI debug hub at `0x3ffc0000000`, found all four ILAs, triggered/uploaded them, and exported CSV files. The reference SPI debug bus decoded as `tag=0x63`, `state=PASS`, `response_byte=0x04`, `miso_seen=1`, `pass=1`, `fail=0`.
- This proves the full OpenPiton shell, P3 SD pad constraints, explicit `IOBUF` boundary, SD card power/control pins, XVC/JTAG/debug hub, and AXI16550 UART baseline are not the current SD blocker. The remaining failure is in the normal OpenCores SPI SD path's command/response timing or sampling, not in the board-level SD physical route.
- Added Build 64 scripts to return to the normal `P3_SPI_SD_BOOT` OpenCores SPI path while keeping Build 63's explicit SD `IOBUF` boundary and the Build 60 history-debug packing: `scripts/p3_build64_spi_sd_iobuf_history_debug.tcl`, `scripts/p3_ila_capture_build64_spi_sd_iobuf_history_debug.tcl`, and `scripts/p3_decode_build64_ila_csv.py`. Build 64 uses `D:/p3b64`, keeps the original AXI16550 UART, and enables `P3_SPI_SD_BOOT P3_SPI_SD_HISTORY_DEBUG` without `P3_SPI_SD_REF_CMD_DEBUG`.

## 2026-06-03 -- Build 63 SD IOBUF fix after Build 62 DRC

- Build 62 completed synthesis, placement, and routing from `D:/p3b62`, but `write_device_image` stopped at Versal DRC `AVAL-352`: inferred `OBUFT` instances for `sd_cmd` and `sd_dat[3]` were driving top-level `inout` ports.
- The failure is a bitgen legality issue, not a timing, route, XVC, AXI16550 UART, BD debug hub, or SD reference sequencer failure. The routed checkpoint had 0 failed nets and positive timing before DRC.
- Added Build 63 with explicit `IOBUF` primitives for SD `CMD` and `DAT[3:0]` in `piton_spi_sd_top.v`. Both the normal SPI-mode path and `P3_SPI_SD_REF_CMD_DEBUG` now drive internal `{*_o,*_oe}` signals and sample `{*_i}` signals through the same pad buffers.
- Added `scripts/p3_build63_spi_sd_iobuf_ref_cmd_debug.tcl`, `scripts/p3_ila_capture_build63_spi_sd_iobuf_ref_cmd_debug.tcl`, and `scripts/p3_decode_build63_ila_csv.py`. Build 63 uses `D:/p3b63`, keeps the original AXI16550 UART and Build 52 four-ILA shape, and tags the reference SD debug bus as `0x63`.

## 2026-06-02 -- Build 62 full-shell reference SPI command probe implemented

- Added `P3_SPI_SD_REF_CMD_DEBUG` in `piton_spi_sd_top.v`. Under this debug macro the full OpenPiton shell keeps the AXI16550 UART, Build 52 ILA shape, and reference SD pin map, but the SD pads are driven by a local Build 56-style bit-banged SPI command sequencer instead of the OpenCores SPI master outputs.
- The Build 62 reference sequencer waits for power, sends idle clocks, drives CMD0 and then CMD8 if needed, and records sticky response status in the existing 16-bit SD ILA probe. The 64-bit SD bus is tagged `0x62` and reports state, command index, response byte, timeout, bit counters, DAT0/MISO, and pass/fail flags.
- Added `scripts/p3_build62_spi_sd_ref_cmd_debug.tcl`, `scripts/p3_ila_capture_build62_spi_sd_ref_cmd_debug.tcl`, and `scripts/p3_decode_build62_ila_csv.py`. The build uses `D:/p3b62` and enables `P3_SPI_SD_BOOT P3_SPI_SD_REF_CMD_DEBUG`.
- Verilator lint of the `HUAPROP3_BOARD`/Build 62 macro path completed without hard errors. Remaining diagnostics are pre-existing width, implicit-net, generate-label, and unused-signal warnings in the legacy AXI SD and NoC bridge files.

## 2026-06-02 -- Build 61 hardware result: OpenCores CMD0 reaches SD pad

- Programmed `p3_top_build61_spi_sd_pad_debug.pdi` successfully through `hw_server 100.93.77.36:3121` and XVC `202.197.4.99:2540`; Vivado reported `DONE bit: HIGH`.
- The Build 61 LTX refreshed through the AXI debug hub at `0x3ffc0000000`, found all four ILAs, and exported CSV successfully. The debug-hub runtime path is still healthy for the OpenPiton image.
- Decode reported `top_status=0xff01`, `core_seen=0x7fff`, `uart_seen=0x5c0b`, `spi_clk_toggle_seen=1`, and observed the NoC/Wishbone SPI init request path.
- The pad-level SD bus captured `cmd_bit_count=56` and `cmd_mosi56=0xff400000000095`, so the OpenCores SPI path launches the expected leading idle byte plus CMD0 at the internal pad observation point.
- The remaining failure is `miso_low_seen=0`: the SD card response start bit is still absent at FPGA input. The next build should keep AXI16550, bootrom stimulus, NoC/Wishbone, and OpenCores byte order fixed while comparing the external MISO/card-response boundary against the known-good Build 56 reference-SPI probe.

## 2026-06-02 -- Build 61 SPI SD pad-level debug implemented

- Rechecked the raw Build 60 `axis_ila_2` CSV rather than relying only on the decoder's last sample. The unique samples show `send_cmd` did reach response-wait states with `tx_count=62/63` and `last_tx=0xff`, so the OpenCores command path was not completely idle.
- The reliable Build 60 failure remains `miso_low_seen=0`: NoC, Wishbone, SPI init type, and SPI clock activity were observed, but the card never drove the MISO response start bit low.
- Added Build 61 with `P3_SPI_SD_PAD_DEBUG`. It keeps the original AXI16550 UART path, reference-pin SPI SD wiring, Build 52-derived small BD ILAs, and bootrom stimulus unchanged. The only RTL change repacks the 64-bit SD ILA bus to capture the first 56 MOSI bits after CS# falls.
- The expected OpenCores CMD0 launch sequence is `0xff400000000095`: one leading idle byte followed by CMD0. If Build 61 captures that sequence but `miso_low_seen` remains zero, the remaining fault is at the external card response boundary or signal sampling timing. If the captured MOSI sequence differs, the next fix should target the OpenCores byte/FIFO wire engine rather than the board, UART, DDR, or bootrom.

## 2026-06-02 -- Build 60 hardware result: SPI command path still gets no card response

- Programmed `p3_top_build60_spi_sd_history_debug.pdi` successfully through `hw_server 100.93.77.36:3121` and XVC `202.197.4.99:2540`; Vivado reported `DONE bit: HIGH`.
- The Build 60 LTX refreshed through the AXI debug hub at `0x3ffc0000000` and exported all four BD-owned ILA CSV files, so the debug runtime path is healthy for this image.
- Decode showed `top_status=0xff01`, `core_seen=0x7fff`, `spi_clk_toggle_seen=1`, NoC request handshakes, Wishbone transaction handshakes, and SPI init transaction writes. UART remains the original AXI16550 path.
- The SD history bus recorded `init_state=CMD0_WAIT`, `state_seen` including `CMD0_SEND`, `CMD0_DEL`, `CMD0_WAIT`, and `CMD0_CHECK`, plus `timeout_or_error_seen=1`. However `miso_low_seen=0`, so the card response start bit was never sampled.
- The lower history half also reported `send_cmd tx_count=0` while the current state was `CMD_REQ_RESP_FIN`. The next build should stop changing high-level boot flow and either expose the true byte-level launch pulses/pad waveform or bypass the OpenCores byte-FIFO `send_cmd` path with the known-good Build 56 bit-banged SPI command sequencer.

## 2026-06-02 -- Build 60 DDR PHY cache recovery

- The valid Build 60 synthesis completed with `PITON_UART16550`, `P3_SPI_SD_BOOT`, and `P3_SPI_SD_HISTORY_DEBUG` enabled, but two implementation retries stopped before `opt_design` could proceed. Both failures were in Vivado's generated `noc_mc_ddr4_phy` child synthesis, not in the SPI SD RTL.
- The child log showed `IPCACHE` returning an empty cache ID and then `validate_noc` failing to read `.Xil/.../realtime/bd_c5b9_MC0_ddrc_0_phy.tcl`. Build 59's successful implementation had generated and cached the same DDR PHY child IP as cache ID `dddc3f069a736d89`, while the shared Build 52 seed only copied the older `f19a7ef233cf09e1` entry.
- Updated the Build 52-derived implementation cache seeding so Build 60 also seeds `dddc3f069a736d89` from the successful `D:/p3b59` project before rerunning `impl_1` with `-skip_create -skip_prepare -reuse_synth`.

## 2026-06-02 -- Build 60 extra-define injection fixed

- The first two Build 60 attempts were stopped before implementation because the generated `D:/p3b60` project did not contain `P3_SPI_SD_BOOT` or `P3_SPI_SD_HISTORY_DEBUG` in `sources_1` even though the Build 60 wrapper set `P3_BUILD52_EXTRA_DEFINES`.
- Root cause: `p3_build52_sd_cd_mask.tcl` captured the extra define list, but `p3_prepare_build52_sd_cd_mask_ila.tcl` was the script that actually rewrote the Vivado `verilog_define` property. The prepare script was still using a fixed Build 52 define list, so derived wrappers silently lost their build-specific macros.
- Fixed the prepare script so it clears stale `P3_SPI_SD_*` macros, merges `p3_extra_defines`, and preserves the original AXI16550 UART path. Build 60 must now be restarted from a clean `D:/p3b60` project and the `Verilog defines:` log line must include both SPI-history macros before synthesis is considered valid.

## 2026-06-02 -- Build 60 SPI SD history debug planned

- Build 59 completed after the expected reuse-synthesis implementation retry, programmed successfully, and refreshed all four ILAs through AXI debug hub `0x3ffc0000000`.
- The Build 59 decode showed the intended MOSI-idle-high behavior (`spi_mosi=1`, `spi_cs_n=1`) and live clock/reset health, but still had `miso_low_seen=0`. The captured live initializer state was `POWER_WAIT`, while sticky SPI error state remained `0x01`, so the next build must distinguish a real power-wait/retry loop from a completed CMD0 timeout followed by a new retry.
- Build 60 keeps the same AXI16550 UART path, reference-pin SPI SD wiring, and four small BD-owned ILAs. It adds only debug-history packing under `P3_SPI_SD_HISTORY_DEBUG`: `init_sd_p3` exports a 25-bit state-history mask and request/timeout sticky bits, and `send_cmd` exports its current state, queued-byte count, last queued byte, response byte, and response timeout.
- The Build 60 decision point is whether OpenCores actually reaches `CMD0_SEND/CMD0_WAIT` and queues command bytes after the MOSI-idle fix. If it does and MISO still never goes low, the next change should replace the byte-FIFO command sender with the known-good Build 56 bit-banged command sequencer or capture pad-level waveforms externally.

## 2026-06-02 -- Build 59 SPI SD MOSI idle-high follow-up

- Build 58 completed after a reuse-synthesis implementation retry. The first implementation pass hit the known Vivado DDR PHY child-generation `rodin:slave3` failure, while the retry reused `synth_1`, generated the DDR PHY child IP, routed successfully, and published `p3_top_build58_spi_sd_power_debug.pdi/.ltx`.
- Build 58 programmed successfully and all four ILAs refreshed through AXI debug hub `0x3ffc0000000`. The decoded state was `top_status=0xff01`, `core_seen=0x7fff`, and the SD path showed NoC request, Wishbone transaction, init transaction, and SPI clock activity.
- The SD initializer remained in `CMD0_WAIT` with `cmd=0x40`, `resp=0xff`, `miso_low_seen=0`, and SPI error `0x01`. This narrows the OpenPiton SPI-SD failure to the external CMD0 response boundary, not UART, debug hub, NoC acceptance, or Wishbone command launch.
- Build 59 changes only the OpenCores `rwspi_wire_data` idle behavior so MOSI is driven high during reset and idle wait states. This matches the standalone Build 56 passing SPI probe, which kept CMD/MOSI high during power wait and idle clocks before asserting CS# for CMD0.
- While linting Build 59, the same file also had a legacy 3-bit zero assignment into the 4-bit `next_bit_cnt`; this was normalized to `4'h0` so the focused Verilator check can pass without suppressing a width warning.
- Added Build 59 wrappers and decoder: `p3_build59_spi_sd_mosi_idle_high.tcl`, `p3_ila_capture_build59_spi_sd_mosi_idle_high.tcl`, and `p3_decode_build59_ila_csv.py`.

## 2026-06-02 -- Build 58 SPI SD power-wait debug path

- Build 57 programmed and refreshed all four ILAs successfully, proving the OpenPiton SPI-SD debug hub path is reachable. The decoded SD path showed a NoC request, Wishbone transactions, an init transaction, and SPI clock activity, but `miso_low_seen=0` and `last_error=0x01`, so the full OpenPiton SPI path still did not observe a card response.
- Added Build 58 as a narrow follow-up rather than changing UART or reverting to native SD. `init_sd_p3` now waits 100 ms after the bootrom requests SD initialization before issuing idle clocks and CMD0, matching the standalone Build 56 probe's power-settle behavior more closely.
- Extended the P3 debug path so `spi_master` exports `init_sd_p3` state, current command byte, response byte, timeout bit, ready/request bits, chip-select state, and init error bits into the 64-bit SD ILA bus. The new decoder treats missing MISO response or non-zero SPI errors as hard failures so the next build can be selected automatically.
- Local checks passed for `init_sd_p3.v`, the SPI-master dependency chain with legacy OpenCores warnings suppressed, and the Build 58 Python decoder.

## 2026-06-02 -- Build 57 SPI init lint cleanup

- Added an explicit default recovery path to the P3 `init_sd_p3` initialization state machine so unexpected state encodings return to `START` instead of relying on incomplete-case behavior. The standalone Verilator lint check for `init_sd_p3.v` now passes before running Vivado.

## 2026-06-02 -- Build 57 SDHC SPI init wiring completed

- Connected the P3 SPI-mode SD path to a board-specific `init_sd_p3` module under `HUAPROP3_BOARD`. This keeps non-P3 platforms on the original OpenCores CMD0/CMD1 initializer, while P3 uses the modern SDHC sequence `CMD0 -> CMD8 -> CMD55/ACMD41`.
- Added `init_sd_p3.v` to the chipset RTL source lists used by both Vivado and the local `Flist.axi_sd_bridge` flow, so the Build 57 macro cannot select a module that is missing from synthesis.
- Changed the P3 Wishbone-to-SPI SD address programming to write `req_addr >> 9` as the 32-bit card argument. This matches SDHC block addressing and avoids accessing only LBA0 correctly while later boot sectors are interpreted as byte addresses.

## 2026-06-01 -- Build 57 SPI-mode SD flow hygiene

- Added `P3_SPI_SD_BOOT` to the Build 52-derived macro cleanup list so reruns and derived builds do not accidentally keep the SPI-SD selection macro when a later control build intends to fall back to the native SD path.
- Documented the Build 57 command flow and decision point: the UART remains the original AXI16550 path, while only the SD NoC window changes to the reference-pin SPI-mode `piton_spi_sd_top` path.

## 2026-06-01 -- Build 57 SPI-mode SD path implementation started

- Added the Build 57 direction after the Build 56 probe result: OpenPiton's SD NoC window will use the reference-pin SPI-mode path instead of the native 4-bit SD controller on P3.
- The implementation keeps the original AXI16550 UART path and the existing SD MMIO address window, but selects `piton_spi_sd_top` with `P3_SPI_SD_BOOT`. That wrapper reuses the existing `noc_axilite_bridge`, `axi_sd_bridge`, and `spi_master` chain from the repository's SD controller test path.
- Build 57 reuses the compact Build 52/55 BD-owned ILA shape and adds SPI-specific status on the SD debug bus: NoC request/response handshakes, AXI-Lite handshakes, Wishbone transactions, SPI clock/CS/MOSI/MISO, and SPI transaction error bits.

## 2026-06-01 -- Build 56 probe revision implemented

- Updated the standalone Build 56 SD probe RTL so the SPI no-response path checks timeout on every sampled bit instead of only at byte boundaries. This prevents `response_timeout` from underflowing and lets silent pin/protocol combinations record a failure and advance to the next case.
- Also made the debug-bus concatenations match their declared widths exactly, so the ILA decoder does not depend on implicit Vivado zero-extension for `p3_sd_status_i` or `p3_sd_bus1_i`.
- Local syntax checks passed with both Verilator lint and Icarus Verilog before rerunning the Vivado Build 56 flow.

## 2026-06-01 -- Build 56 first hardware result and probe timeout fix

- Build 56 completed from `D:/p3b56`, produced `p3_top_build56_sd_dual_probe.pdi/.ltx`, programmed successfully, and refreshed the standalone ILA through debug hub `0x3ffc0000000`.
- The first decoded capture proved the reference-project SD pin map in SPI mode is electrically alive: `ref_spi` completed with `pass=1`, `resp_seen=1`, and `fail_code=0x00`. The reference pin map in native CMD mode timed out (`fail_code=0x21`).
- The capture also exposed a bug in the Build 56 probe itself: when PHC3 SPI saw no response, the SPI response timeout counter could underflow between byte boundaries and keep the sequencer in `SPI_RESP_HIGH` instead of recording failure and moving to `phc3_native`.
- The next Build 56 revision keeps the same standalone experiment and fixes only the probe timeout bookkeeping so all four combinations produce sticky pass/fail results from one capture.

## 2026-06-01 -- Build 56 implementation adjustment: no VIO on VP1902

- The first standalone Build 56 attempt failed during BD creation because `xilinx.com:ip:vio:3.0` is not supported for the current VP1902 part in this Vivado environment.
- The Build 56 strategy remains the same, but the control path changes: instead of VIO-selecting one case at a time, the RTL will automatically run all four combinations in sequence after reset and store per-combination sticky pass/fail/timeout summaries for ILA capture.
- This keeps the experiment independent of OpenPiton, DDR, bootrom, UART, and manual Hardware Manager interaction while removing a Versal IP-catalog dependency.

## 2026-06-01 -- Build 56 dual-pin dual-protocol SD probe plan

- Build 55 showed that registering `sd_clk_out` at the IOB boundary is not sufficient: the native SD initializer still waits in `ST_ACMD41_CMD55_WAIT_INT` for a CMD55 response start bit.
- The remaining high-risk ambiguity is now physical/protocol alignment, not UART, DDR, debug hub, CPU fetch, or single SD-clock output style. The reference project proves a SPI-mode SD path on `CV57/DB57/DC56/DC55/CY56/CY55`, while `p3_io.md` lists a different PHC3 native SD mapping.
- Build 56 will be a standalone minimal design, not a full OpenPiton build. It will use the proven Versal debug-hub/ILA path and a small RTL SD command engine to test both pin maps and both protocol assumptions from one bitstream.
- VIO controls will select `pin_sel` (`reference` or `p3_io.md PHC3`) and `proto_sel` (`SPI` or native SD CMD). ILA captures will record CMD/DAT input/output/enable levels, response detection, timeout, state, and pass/fail code.
- Decision table: reference+SPI response means the next OpenPiton direction should use a SPI SD controller path; p3_io+native response means the next full build should only fix XDC pin mapping; reference+native response means the fault is inside the OpenPiton native controller integration; no response on all combinations means the SD power/reset/slot/card path must be verified externally.

## 2026-06-01 -- Build 54 Review: Analyzing CMD55 Timeout & SPI Wire-Crossing Hypothesis

- Evaluated Build 54's decoded ILA trace: the serial host stays stuck in `READ_WAIT` (0x08) and `ST_ACMD41_CMD55_WAIT_INT` (0x34) waiting for the response start bit. This rules out simple idle-line pull-up issues.
- Analyzed the mismatch between `constraints.xdc` and `p3_io.md`. However, UART works perfectly on `CW58/CW59` (B3/B2) instead of the manual's `CM59/CN59` (C8/C9), proving that the board routing differs from the manual's PHC3 section.
- Formulated the **SPI Wire-Crossing Hypothesis**: the reference project (`shell.xdc`) routed SD in SPI mode, where `DB57` served as SPI_CS (connected to Card DAT3) and `CY55` served as SPI_DI (connected to Card CMD). OpenPiton's native SD controller outputs `sd_cmd` on `DB57` and `sd_dat[3]` on `CY55`, physically misrouting CMD to DAT3 and DAT3 to CMD.
- Plan: Build 55 proceeds as scheduled to test the IOB SD clock register (`huaprop3_build55_sd_clk_iob_reg` in `D:/p3b55`).
- Plan: A new dedicated **Build 56** will be set up to run physical pin experiments:
  - Swap `sd_cmd` (CY55) and `sd_dat[3]` (DB57) under the reference pinout to test the SPI wire-crossing hypothesis.
  - Generate a secondary build mapped to `p3_io.md`'s PHC3 native SD pins to test the manual alignment.


## 2026-06-01 -- P3 Build 54 result and Build 55 SD clock output experiment


- Build 54 completed from `D:/p3b54`, programmed successfully, refreshed all four BD-owned ILAs through debug hub `0x3ffc0000000`, and exported CSV captures with the Build 54 LTX.
- The decoded live state is unchanged from Build 53: `top_status=0xff03`, `core_seen=0x7fff`, SD command sticky bits `0xffff`, `init_state=0x34` (`ST_ACMD41_CMD55_WAIT_INT`), `command_index=55`, command master `EXECUTE`, serial host `READ_WAIT`, `cmd_oe_o=0`, and `sd_cmd_dat_i=1`.
- Therefore weak pull-ups on `sd_cmd`, `sd_dat[0]`, and `sd_dat[3]` did not make the card return a native CMD response. The failure is still at the external SD command-response boundary, not UART, debug hub, NoC, Wishbone command launch, or the card-detect reset gate.
- Build 55 will keep the same AXI16550/no-stack bootrom and SD command ILA, but change the P3 SD clock output from a direct fabric assignment to an IOB-targeted output register sampled from the SD system clock. This tests the next physical-layer hypothesis: direct `sd_clk_out_internal` routing on Versal may not be a clean enough pad clock for the card response phase.

## 2026-06-01 -- P3 Build 53 hardware result and Build 54 native SD pull-ups

- Build 53 completed from `D:/p3b53`, routed with 0 failed/unrouted nets, generated `p3_top_build53_sd_cmd_debug.pdi/.ltx`, programmed with `DONE bit: HIGH`, and refreshed all four BD-owned ILAs through debug hub `0x3ffc0000000`.
- Two immediate captures several minutes apart decoded the same live state: `top_status=0xff03`, `core_seen=0x7fff`, SD command sticky bits `0xffff`, and SD init stuck at `0x34` (`ST_ACMD41_CMD55_WAIT_INT`).
- The live command-layer bus showed `command_index=55`, `cmd_timeout=0x4e20`, command master `EXECUTE`, serial host `READ_WAIT`, `cmd_oe_o=0`, and `sd_cmd_dat_i=1`. That means the controller is issuing CMD55 and waiting for the card to pull CMD low for the response start bit.
- Build 54 tests the most local physical-mode hypothesis before changing controller logic: enable weak PULLUP on `sd_cmd`, `sd_dat[0]`, and `sd_dat[3]` so native SD idle lines, especially DAT3/CS, cannot float low while CMD0 is issued. The Build 54 wrapper keeps Build 53's AXI16550/no-stack bootrom and SD command ILA.

## 2026-06-01 -- P3 Build 52 hardware result and Build 53 CMD-layer probes

- Build 52 completed from `D:/p3b52`, programmed with `DONE bit: HIGH`, refreshed debug hub `0x3ffc0000000`, and exported all four BD-owned ILA CSV files.
- The card-detect mask works as intended: raw `sd_cd=1` remains observable while internal SD reset is released (`sd_internal_rst=0`). Sticky bits also prove Wishbone ack activity, SD output-clock toggles, CMD output-enable activity, and command interrupts.
- Native SD initialization still does not reach `init_done`. The live initializer state is `0x34`, which is `ST_ACMD41_CMD55_WAIT_INT`: CMD0 and CMD8 have completed, but the ACMD41 pre-command CMD55 path is waiting for its completion interrupt.
- Added Build 53 command-layer probes under `P3_BD_SD_CMD_DEBUG_ILA`. The existing 64-bit SD ILA now captures CMD watchdog progress, command index, SD/WB interrupt status, timeout value, command-master state, serial-host state, start/finish pulses, CMD input, output enable, and the WB-domain interrupt.
- Build 53 reuses the Build 52 small-ILA shape and AXI16550/no-stack bootrom baseline. Its decision point is whether CMD55 never starts, waits forever for a CMD response start bit, times out without a WB-domain interrupt, or completes with an error status.

## 2026-06-01 -- P3 Build 52 SD card-detect mask implemented

- Added Build 52 as the direct follow-up to the Build 51 hardware result. Build 52 keeps the original Xilinx AXI16550 UART path, the same native OpenPiton SD controller, the same SD pins/clocks, the same no-stack SD-probe bootrom, and the same four BD-owned ILA shape.
- Added `P3_SD_IGNORE_CARD_DETECT_RESET` in `piton_sd_top`. Under this build-only macro, raw `sd_cd` is still probed, but it no longer contributes to the SD block's internal reset; `rst` becomes `sys_rst` for the SD controller/init path.
- Added Build 52 scripts using the short Windows path `D:/p3b52` by default: build/create/prepare/capture/decode/rebuild scripts publish `p3_top_build52_sd_cd_mask.pdi/.ltx` under `huaprop3_build52_sd_cd_mask/debug_build`.
- Expected decision point: if Build 52 shows internal SD reset released and SD clock/CMD/Wishbone ack activity, the current root cause is the P3 card-detect polarity/connection path. If reset releases but SD init still stalls, the next target is SD clock generation, OpenCores Wishbone ack, or CMD/DAT physical response.

## 2026-06-01 -- P3 Build 51 hardware result: SD card-detect holds native SD reset

- Build 51 completed from `D:/p3b51` and published `huaprop3_build51_sd_init_debug/debug_build/p3_top_build51_sd_init_debug.pdi` and `.ltx`. Top synthesis, placement, route, and `write_device_image` completed with 0 errors; route status had 0 failed nets and the post-route estimate was positive (`WNS=9.131 ns`, `WHS=0.006 ns`).
- Programming through `hw_server 100.93.77.36:3121` and XVC `202.197.4.99:2540` succeeded with `DONE bit: HIGH`. The debug hub refreshed through `PMC_AXI_NOC0` at `0x3ffc0000000`, and all four BD-owned ILAs exported CSV.
- Decode reported `top_status=0xff00`, `core_seen=0x7fff`, SD-init seen bits `0x00cb`, and DDR seen bits `0x8001`. The SD-init live bus was `0x0000280001f01066`.
- The decisive SD-init bits are `sd_cd=1`, aggregated SD reset `rst=1`, `init_done=0`, `wb_stb=1`, `wb_we=1`, `wb_ack=0`, `sd_clk_toggle=0`, and no CMD/DAT output-enable activity. Because `piton_sd_top` defines its internal reset as `sys_rst | sd_cd`, the observed high card-detect input holds the SD controller and init FSM in reset.
- This explains the Build 49/50 symptom `buf_sd_noc2_valid=1` with `sd_buf_noc2_ready=0`: the SD request path cannot accept CPU requests until `init_done` rises, and `init_done` cannot rise while the SD block is reset by card-detect. The next build should mask `sd_cd` out of the internal reset while still probing the raw card-detect input.
